# 第一部分：C++ 基础

## 一、问答题

### 1. 命名空间（namespace）的作用是什么？分别说明"作用域限定符"、"using 编译指令"、"using 声明机制"的用法和优缺点，并指出在头文件中禁止使用哪种方式？为什么？

**原题**：命名空间（namespace）的作用是什么？分别说明"作用域限定符"、"using 编译指令"、"using 声明机制"的用法和优缺点，并指出在头文件中禁止使用哪种方式？为什么？

**答案**：

命名空间（namespace）的作用是**解决全局命名冲突**。当多人协作或引入多个第三方库时，全局作用域中的同名标识符会互相冲突；命名空间把一组标识符（变量、函数、类型、常量等）封装到一个"命名作用域"中，靠 `命名空间名::标识符` 来区分。

三种访问方式的对比：

| 方式              | 写法                   | 优点                             | 缺点                                                         |
| ----------------- | ---------------------- | -------------------------------- | ------------------------------------------------------------ |
| 作用域限定符 `::` | `std::cout << "hi";`   | 最明确、无二义性、不会污染作用域 | 每次都要写全名，啰嗦                                         |
| using 编译指令    | `using namespace std;` | 一次性引入整个命名空间，代码简洁 | 引入范围过大，极易与本地名字/其它命名空间冲突；作用域污染严重 |
| using 声明        | `using std::cout;`     | 只引入需要的个别名字，冲突风险小 | 每个名字要单独写一句                                         |

**头文件中禁止使用 `using namespace std;`（using 编译指令）**。原因：头文件会被 `#include` 到大量其他源文件，一旦在头文件里写了 `using namespace std;`，就会把 std 的全部名字**强制扩散**到每一个包含它的翻译单元，等于把"全局污染"传播到整个工程，导致难以排查的名字冲突。实践中，头文件里甚至 `using std::cout;` 这种声明也建议少用（头文件内部要引用 std 名字时，规范做法是直接写 `std::xxx`）。C++ 核心指南（CppCoreGuidelines）也明确建议**头文件中不要使用 using 指令**。

**通俗解释**：命名空间就像给每个人分了一个"抽屉"，抽屉里可以有同名的东西，靠"抽屉名+名字"区分。`using namespace` 相当于"把所有抽屉全打开摊在桌上"，方便但容易把别人同名的东西搞混；`using std::cout` 相当于"只把自己常用的那件拿出来"；`::` 相当于"每次要东西都报抽屉名"，最不会错。头文件是"发给所有人的图纸"，图纸上不能写"把 std 全摊开"，否则所有看图纸的人桌上都乱了。

---

### 2. const 修饰内置类型变量时，为什么必须初始化？const 与 #define 宏常量有哪些主要区别（时机、类型检查、作用域）？

**原题**：const 修饰内置类型变量时，为什么必须初始化？const 与 #define 宏常量有哪些主要区别（时机、类型检查、作用域）？

**答案**：

**为什么 const 必须初始化**：`const` 表示"该对象在生命周期内不可修改"。C++ 中一个变量只有两个写入口：**初始化**（定义时给初值）和**赋值**（运行时改写）。const 禁止了"赋值"，那么初值就必须在定义的那一刻通过初始化提供，否则这个对象将永远是一个"没有值且永远无法被赋值"的僵尸对象。所以编译器强制要求 `const int a;` 这种写法直接报错。

**const 与 #define 的主要区别**：

| 维度                  | const                                                 | #define 宏                                             |
| --------------------- | ----------------------------------------------------- | ------------------------------------------------------ |
| 处理时机              | 编译期，参与编译、语义分析                            | 预处理期，纯文本替换，编译前就已替换完                 |
| 类型检查              | 有完整类型检查，编译器能发现类型不匹配                | 无类型概念，不做任何类型检查，隐患多                   |
| 作用域                | 遵循 C++ 作用域规则（局部/命名空间/类内），有内部链接 | 从定义处到文件末尾都有效，无视作用域，易污染           |
| 是否可调试            | 是，调试器能看到符号和值                              | 否，替换后符号消失                                     |
| 是否占用内存          | 通常占用（或编译器优化到符号表），可取地址            | 不占存储、无地址                                       |
| 是否产生二义性/副作用 | 不会                                                  | 若宏参数不带括号（`#define SQR(x) x*x`），求值极易出错 |
| 安全性                | 类型安全                                              | 无类型安全                                             |

一个典型的宏缺陷：`#define MAX(a,b) a>b?a:b` 在 `MAX(i, j++)` 或复杂表达式里会重复求值、产生副作用，而 const 值没有这个问题。

**补充（现代规范）**：C++ 里更推荐使用 `constexpr`（编译期常量，可用于数组大小、模板参数等 const 做不到的场合）来替代传统宏常量。C++11 起 `const` 对象在**命名空间作用域**默认具有内部链接（等价于 `static`），只有显式加 `extern` 才有外部链接，这进一步保证了头文件中定义 const 不会导致多文件重定义。

**通俗解释**：`const` 就像"出生证上写死了血型"，必须出生时就填好，之后不能改；`#define` 像"考前背的口诀"，考试（编译）前老师就帮你把口诀抄好了（替换成字面量），抄的过程中既不查对错也不管上下文，抄错了就错。

---

### 3. 解释以下指针声明的含义，并说明哪些操作合法/非法

**原题**：解释以下指针声明的含义，并说明哪些操作合法/非法：`const int *p1; int const *p2; int * const p3; const int * const p4;`

**答案**：

记忆口诀：**`const` 修饰它"左边紧挨着"的内容（若 const 在类型最前面，则修饰它右边的内容）**。

1. `const int *p1` —— p1 是指向 **const int** 的指针（**指向的内容不可变，指针本身可变**）。
   - 合法：`p1 = &other;`（换指向）；读 `*p1`。
   - 非法：`*p1 = 10;`（试图通过 p1 修改指向的值）。
2. `int const *p2` —— 与 `const int *p1` **完全等价**（`const` 移到类型名右边只是风格差异）。同样"内容只读、指针可变"。
3. `int * const p3` —— p3 是 **const 指针**（**指针本身不可变，指向的内容可变**）。
   - 合法：`*p3 = 10;`
   - 非法：`p3 = &other;`（试图让 const 指针改指）。
4. `const int * const p4` —— **const 指针指向 const int**（**两者都不可变**）。
   - 合法：读 `*p4`。
   - 非法：`p4 = &other;` 与 `*p4 = 10;` 都不行。

注意 `const int *p` 与 `int * const p` 是**两种完全不同的语义**（一个锁"内容"，一个锁"指针自己"），这是最常见的易混点。

**通俗解释**：把指针想象成"一个写着地址的小纸条"。"`const int *`" = 纸条可以换，但纸条指向的那块黑板不许用这支笔改字；"`int * const`" = 纸条是焊死的不能换，但黑板上的字随便改；"`const int * const`" = 纸条焊死、黑板上的字也不许改。

---

### 4. new/delete 与 malloc/free 的区别有哪些？请至少列出 4 点

**原题**：new/delete 与 malloc/free 的区别有哪些？请至少列出 4 点。

**答案**（列出 6 点）：

1. **性质不同**：`new/delete` 是 **C++ 运算符**（可用 `operator new/delete` 重载）；`malloc/free` 是 **C 库函数**（`<cstdlib>`）。
2. **是否调用构造/析构**：`new` 分配内存后会**自动调用构造函数**，`delete` 释放前**自动调用析构函数**；`malloc/free` 只做原始内存的分配/释放，**绝不调用任何构造/析构**。
3. **内存大小计算**：`new` 由编译器根据类型自动计算字节数（`new T` 即 `sizeof(T)`）；`malloc` 必须手动写 `sizeof(T) * n`，写错就有隐患。
4. **返回类型**：`new` 返回**类型化指针**（`T*`），无需强转；`malloc` 返回 `void*`，需手动 `static_cast<T*>`（C 中可隐式，C++ 中必须显式）。
5. **失败处理**：`new` 失败**抛出 `std::bad_alloc` 异常**（不检查返回值）；`malloc` 失败**返回 NULL**，需手动判断。
6. **配套方式**：`new`/`delete` 配 `[]` 处理数组（`new T[n]` / `delete[]`）；`malloc` 可配 `realloc` 扩容；`new` 还支持 **placement new**（在指定地址构造，不分配内存）。

**关键注意**：`new` 出来的内存必须用 `delete` 释放，`malloc` 出来的必须用 `free` 释放，**不能混用**（混用是未定义行为，常见于 free 掉 new 的对象导致析构不调用、或 delete 掉 malloc 的内存）。

**通俗解释**：`malloc` 只负责"给你一块空地"，`new` 还负责"在空地上帮你把房子（对象）盖好、家具（成员）摆好"。收房（delete）时 `new` 还会帮你先"搬家清场（调析构）"再拆地。所以管理 C++ 对象必须用 new/delete。

---

### 5. 什么是引用？引用有哪些特性（必须初始化、不能改绑定、类型匹配）？引用在底层本质是什么？

**原题**：什么是引用？引用有哪些特性（必须初始化、不能改绑定、类型匹配）？引用在底层本质是什么？

**答案**：

**引用（reference）** 是已存在对象的**别名**。定义后，引用和它绑定的对象在语法上是"同一个东西"，对引用的所有操作都作用于原对象。

三大特性：

1. **必须初始化**：`int &r;` 是编译错误。引用没有"空引用"，定义时必须绑定到一个已存在的对象（或另一个引用）。
2. **不能改绑定**：引用一旦绑定某个对象，终生绑定，不能重新指向别的对象。`r = other;` 不是"改绑定"，而是"把 other 的值赋给 r 绑定的那个对象"。
3. **类型必须完全匹配**：`double d; int &r = d;` 非法（`int&` 不能绑定 `double`）。唯一例外：**`const` 引用**（`const int& r = 3.14;`）可以绑定临时对象/字面量，因为此时会生成一个临时量。还有基类引用可以绑定派生类对象（向上转型安全）。

**底层本质**：引用在**逻辑上**是别名、不额外占用内存；但**实现上**（大多数编译器）引用通常就是一个**指针常量**——内部存的是绑定对象的地址，语法上却按"原对象本身"使用。所以引用本质 = "编译器替你隐藏了 `*` 解引用的常指针"。`sizeof(引用)` 得到的是**所绑定对象**的大小，而不是指针大小，这也印证了它语法上的"别名"身份。

**通俗解释**：引用相当于"给对象起了个外号"。外号必须是在给某个人起的（必须初始化）；起好外号后，这个外号永远只指同一个人（不能改绑定）；你不能给一个"身高单位是米的人"起"他的身高单位是英尺"这种错配的外号（类型匹配）。底层就是一张写着地址的小纸条，但平时你不觉得它是纸条，直接喊外号就行。

---

### 6. 引用与指针的联系和区别（至少 3 点）

**原题**：引用与指针的联系和区别（至少 3 点）。

**答案**：

**联系**：
- 两者都能间接访问对象，常用于函数参数/返回值以"操作原对象"。
- 底层实现都是"地址"，引用可看作编译器封装的常指针。
- 都能与 `const` 配合；引用绑定派生类对象、指针指向派生类对象都构成向上转型。

**区别**（列举 5 点）：
1. **初始化**：引用必须初始化，不能存在"空引用"；指针可以暂不初始化（但不推荐，应为 nullptr），可以指向空。
2. **可重绑定性**：引用终生绑定，不能改指；指针可以随时改变指向。
3. **空值**：指针可以是 `nullptr`，使用前需判空；引用永远绑定一个对象，无需判空（更安全）。
4. **运算**：指针支持算术（`++p` 移动一个元素）、支持多级指针（`int**`）；引用没有指针运算，也没有"引用的引用"。
5. **使用形式与 sizeof**：引用直接用名字操作（编译期自动解引用）；`sizeof(引用)` 是被引用对象的大小，`sizeof(指针)` 是指针本身大小（32 位 4 字节、64 位 8 字节）。

**现代建议**：函数参数"需要修改实参或避免拷贝"时**优先用引用**（更安全、无空指针风险）；需要"可选/可重指/数组遍历"等场合才用指针。CppCoreGuidelines 明确"优先引用而非指针，除非需要表示'无对象'"。

**通俗解释**：引用是"本人身份证号"，永远是同一个人；指针是"名片上的地址"，可以擦掉改成别人的，也可以空白（空指针）。用引用操作不会出现"名片是空的、一打电话就出错"的风险。

---

### 7. C++ 的四种强制类型转换（static_cast、const_cast、dynamic_cast、reinterpret_cast）各适用于什么场景？为什么说 reinterpret_cast 最危险？

**原题**：C++ 的四种强制类型转换（static_cast、const_cast、dynamic_cast、reinterpret_cast）各适用于什么场景？为什么说 reinterpret_cast 最危险？

**答案**：

| 转换                     | 适用场景                                                     | 特点/风险                                                    |
| ------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| `static_cast<T>(x)`      | 编译期、**相关类型之间**的转换：如 `int→double`、`void*→T*`、枚举↔整型、**向上转型**（派生类→基类）、算术类型转换 | 有基本的编译期检查，比 C 风格安全；但**向下转型**不检查运行时类型，用错了是未定义行为 |
| `const_cast<T>(x)`       | 去掉/加上 `const`（或 `volatile`）属性                       | 唯一能改 cv 限定符的转换；对"真正 const 的对象"去 const 后写入是未定义行为，通常用于调用第三方的非 const 接口 |
| `dynamic_cast<T>(x)`     | **运行时**安全的**向下转型** / 跨继承层次转型，`T` 必须是指针或引用，且要求源类**多态**（含虚函数） | 靠运行时类型信息（RTTI）检查；指针转换失败返回 `nullptr`，引用转换失败抛 `std::bad_cast`；有运行时开销 |
| `reinterpret_cast<T>(x)` | **位级重新解释**：任意指针↔任意指针、指针↔整数、函数指针↔指针等 | **不进行任何类型/对齐检查**，纯粹把二进制按新类型解释，极易导致未定义行为 |

**为什么 reinterpret_cast 最危险**：
- 它**绕过整个类型系统**，不检查目标类型与源类型是否相关、是否对齐、是否安全，直接把某块内存的二进制"重新贴标签"。
- 一个典型错误：把 `int` 的地址 reinterpret 成 `double*` 再解引用——内存大小、对齐、表示方式全错，结果是**未定义行为**，可能拿到垃圾值、甚至崩溃/安全问题。
- 它的危险性"不报错"，编译器完全放行，错误要等到运行期甚至更晚才暴露，极难排查。
- 实践上仅用于：硬件寄存器映射、序列化时按字节存取、以及 `dynamic_cast` 无法覆盖的"已知肯定安全"的底层场景。

**通俗解释**：`static_cast` 像"把人民币换成美元"（单位相关、官方柜台办理）；`const_cast` 像"撕掉包装上'勿拆'的标签"；`dynamic_cast` 像"验明正身后才放行（带安检）"；`reinterpret_cast` 像"把这份 Word 文档直接当音频文件播放"——格式完全不同也硬放，不出声/爆炸都说不准。

---

### 8. 函数重载的条件是什么？为什么返回类型不同不能构成重载？C++ 实现重载的原理是什么（名字改编）？

**原题**：函数重载的条件是什么？为什么返回类型不同不能构成重载？C++ 实现重载的原理是什么（名字改编）？

**答案**：

**函数重载条件**（必须在**同一作用域**内）：
1. 函数名相同；
2. 参数列表不同——参数**个数**不同、或参数**类型**不同、或参数**顺序**不同（类型序列不同即可）。
3. 仅 `const`/引用限定符等 cv 差异也参与重载（`void f(int)` 与 `void f(const int&)` 是重载），但顶层 const（`int` vs `const int` 作为值参数）**不构成**重载，因为值传递时二者签名相同。

**为什么返回类型不同不能构成重载**：调用函数时**返回值常常被忽略**（`add(1,2);` 直接丢掉结果）。若两个函数仅返回类型不同，编译器在调用点**无法从语法上判断该调用的是哪一个**（比如 `f();` 到底调用 `int f()` 还是 `double f()`？），重载决议会失败。C++ 规定"返回类型不参与函数签名"，所以不能仅靠返回类型区分重载。

**实现原理——名字改编（name mangling）**：编译器把"函数名 + 参数类型信息"编码成一个**唯一的内部符号名**，使同名不同参数的重载函数在目标文件中互不冲突。例如（Itanium ABI，GCC/Clang）：
- `int foo(int, double)` → `_Z3fooid`（`i`=int，`d`=double）
- `int foo(int)` → `_Z3fooi`

MSVC 用 `?foo@@YAHH@Z` 这类格式。可以用 `nm` 或 `c++filt` 查看/还原。`extern "C"` 的作用就是**关闭名字改编**，让函数以 C 符号（通常仅函数名）导出，从而能被 C 代码链接调用——代价是该函数**不能重载**。

**通俗解释**：重载 = 同一个名字 + 不同的"收货清单（参数）"，仓库按清单发货。仓库不能只靠"你要发苹果还是橘子（返回类型）"来发货，因为你可能根本没说要什么（返回值被丢弃），所以必须看清单。名字改编 = 给每个不同清单的函数贴一个独一无二的货架编号，避免放混。

---

### 9. 默认参数的设置规则（从右向左连续）是什么？默认参数与函数重载同时使用时可能产生什么冲突？

**原题**：默认参数的设置规则（从右向左连续）是什么？默认参数与函数重载同时使用时可能产生什么冲突？

**答案**：

**设置规则**：
1. 默认参数必须**从右向左连续**设置：一旦某个参数给了默认值，它右边所有的参数都必须有默认值（否则调用时无法从右往左补齐）。例如 `void f(int a, int b=2, int c=3);` 合法；`void f(int a=1, int b, int c=3);` 非法。
2. **默认值只能在"声明或定义之一"给出，不能两处都写**。通常写法是：声明（头文件）里写默认值，定义（.cpp）里**不写**。若两处都写同一默认值，C++11 前属于错误；现代编译器也会报警告/错误（同一作用域重复默认实参）。
3. 默认实参可以是常量表达式、全局变量、甚至前面的参数（如 `void f(int a, int b = a);`，较新标准支持）。

**与重载的冲突（二义性）**：
- 最典型：`void f(int);` 与 `void f(int, int = 0);` 同时存在时，调用 `f(10)` 两个函数都能匹配（一个是单参，一个用默认参数），**产生二义性编译错误**。
- 另一例：`void f(int, int = 0);` 与 `void f(int);` 同作用域也会冲突。
- 现代建议：**避免"重载 + 默认参数"混用**；要扩展参数个数时优先用单一函数 + 默认参数，或用函数重载二选一。

**通俗解释**：默认参数 = "点餐时菜单上写了默认辣度"，必须从最后一道菜往前的连续几道都标了默认，顾客才能只点前面的。而"重载+默认参数"的冲突 = 菜单上同一个菜既标了"默认加辣"又单独列了"不辣版"，顾客只说"来个这个菜"时服务员不知道到底上哪个。

---

### 10. bool 类型的取值范围？任何非零值转换为 bool 都是 true 吗？sizeof(bool) 通常是多大？

**原题**：bool 类型的取值范围？任何非零值转换为 bool 都是 true 吗？sizeof(bool) 通常是多大？

**答案**：

1. **取值范围**：bool 只有两个值——`true` 和 `false`。在内存中通常以 `0`（false）和 `1`（true）存储。
2. **任何非零值转 bool 都是 true**：是的。C/C++ 规则：`bool b = 任意非零值;` → `true`；`bool b = 0;`（或 `nullptr`）→ `false`。所以 `bool b = -1;` 也是 `true`。（注意：C++ 中 `bool` 是**一等公民类型**，不是整型；但可隐式转换，转换规则就是"零→false，非零→true"。）
3. **sizeof(bool)**：标准未强制规定，但**几乎所有主流编译器（GCC/Clang/MSVC）中都是 1 字节**。`true` 通常存为 1，`false` 存为 0。
4. 实践注意：虽然 bool 只占 1 字节，但作为**成员**时受对齐影响可能被 padding；`std::vector<bool>` 是特化，做了**位压缩**（每个 bool 占 1 bit），它返回的是代理对象而非 bool&，这是容易踩坑的点。

**通俗解释**：bool 就是"是/否"两个字的开关。只要"灯亮着"（非零）就算"是"，哪怕亮度是 -5 也算亮。占 1 个字节是因为电脑最小存储单位是 1 字节，虽然开关只需要 1 位，但标准库/内存通常按字节访问。

---

### 11. inline 函数与宏函数、普通函数的对比（优缺点、替换时机、是否可调试）。inline 函数定义通常放在哪里？为什么？

**原题**：inline 函数与宏函数、普通函数的对比（优缺点、替换时机、是否可调试）。inline 函数定义通常放在哪里？为什么？

**答案**：

**三者对比**：

| 维度                        | inline 函数                                       | 宏函数 `#define`               | 普通函数                   |
| --------------------------- | ------------------------------------------------- | ------------------------------ | -------------------------- |
| 处理阶段                    | 编译期（建议性内联展开）                          | 预处理期纯文本替换             | 编译成独立代码，调用点调用 |
| 类型检查                    | 有                                                | 无                             | 有                         |
| 副作用                      | 无（形参只求值一次）                              | 有（如 `MAX(i++,j)` 重复求值） | 无                         |
| 作用域                      | 遵循作用域规则                                    | 全局文本替换，易污染           | 遵循作用域规则             |
| 可调试                      | 可（展开后仍保留符号/行号信息，debug 下常不展开） | 不可（替换后符号消失）         | 可                         |
| 函数调用开销                | 展开则无调用开销                                  | 无                             | 有压栈/跳转/返回开销       |
| 代码体积                    | 每个调用点都展开 → 代码膨胀                       | 每个调用点替换 → 代码膨胀      | 只有一份                   |
| 是否允许递归/循环等复杂逻辑 | 建议只用于短小函数；复杂函数内联意义小            | 不适合复杂逻辑                 | 无限制                     |

**替换时机**：适合"**短小、频繁调用**、且函数体不大"的函数（如访问器 getter/setter、简单比较函数）。函数体很大或包含循环、递归、复杂分支时，内联会导致严重代码膨胀且收益甚微，编译器也倾向不展开。注意 **inline 只是"建议"**，编译器有权拒绝（如函数体过大、取函数地址、递归等场景）；release 下编译器还会自动内联很多非 inline 的小函数（`-O2` 的 LTO/内联优化）。

**定义放哪里、为什么**：inline 函数的**完整定义必须放在头文件中**（或定义在使用它的每个翻译单元中可见）。原因：
- 编译器**在调用点展开**必须"看得到完整函数体"；
- inline 函数默认**内部链接**（每个翻译单元各自有一份），所以头文件被多个 .cpp 包含**不会产生多重定义链接错误**（这是它与普通函数"声明放头文件、定义放 .cpp"的根本区别）。
- 现代规范：对需要内联的短函数，直接**在类体内定义**（自动成为 inline），或在头文件中以 `inline` 显式定义。

**通俗解释**：宏像"考试时把口诀抄在草稿纸上再照抄"——抄的过程不检查对错（没类型检查）、还会把口诀抄错位置（副作用）；inline 像"老师把这道题的解题步骤直接印在你卷子上"（在调用处展开，但步骤是检查过的、正确的）；普通函数像"去图书馆查书"——书只有一本但每次都要跑一趟（有调用开销）。

---

### 12. 异常处理中 try、catch、throw 的用法，catch 匹配的是类型还是值？catch(...) 的作用？

**原题**：异常处理中 try、catch、throw 的用法，catch 匹配的是类型还是值？catch(...) 的作用？

**答案**：

**语法与流程**：
- `throw 表达式;` —— 抛出一个**异常对象**（可以是任意类型，但规范上抛类对象/标准异常），控制流立即离开当前函数，沿调用栈逐层查找匹配的 catch。`throw;`（无操作数）只能在 catch 内使用，表示**重新抛出当前异常**（保留原类型）。
- `try { 可能抛异常的代码 }` —— 划定监视区域。
- `catch (类型/值 形参) { 处理 }` —— 捕获并处理。可以连续多个 `catch`，**按书写顺序**匹配，匹配成功即停止，不自动回溯。

**catch 匹配的是"类型"**：catch 的匹配规则是——被抛异常对象的**静态类型**与 catch 参数类型匹配（精确匹配或存在合法的标准转换：基类匹配派生类对象、`void*`、const 可加、派生类指针→基类指针等）。注意：
- 按**值捕获**会**拷贝异常对象**（可能造成派生类对象的**切片**，丢失派生部分信息）；
- 按**引用捕获**（`catch (const std::exception& e)`）**不拷贝、不切片**，是最佳实践，还能用 `e.what()`；
- catch 的"参数类型匹配"不是运行时比较对象内容，而是编译期按类型体系匹配。

**catch(...)（捕获一切）**：
- 作用：兜底捕获**任何类型**的异常，语法上是 `catch (...)`。
- 通常放在**所有 catch 的最后一个**，做"清理资源、记日志、终止/包装后再抛"等兜底操作。
- 缺点：无法获取异常对象本身（不知道具体类型和内容）。所以一般不单独依赖它做业务处理，常配合 `throw;` 重抛。

**补充（现代规范）**：C++11 起 `noexcept` 取代旧式 `throw()` 声明；析构函数默认 noexcept，**析构函数里抛异常会导致 std::terminate**，因此析构中应捕获并处理异常。另外 C++17 起 `throw()` 已移除。异常规范推荐：可能抛异常的函数不写 noexcept，绝不可能抛的加 noexcept。

**通俗解释**：`throw` 像"施工队发现图纸错了，大声喊停并扔出问题报告"；`try` 像"这个施工区域设立了呼叫站"；`catch` 像"接电话的专员，看报告类型决定谁处理"。匹配的是"报告的类型（Type）"而不是报告的具体内容。`catch(...)` 像"专线兜底：不管什么报告都先接住，但接住后你也拆不开信封看内容"。

---

### 13. 画出 32 位 Linux 进程虚拟地址空间的布局（栈、堆、全局/静态区、文字常量区、代码区），并说明各区域存放的内容。

**原题**：画出 32 位 Linux 进程虚拟地址空间的布局（栈、堆、全局/静态区、文字常量区、代码区），并说明各区域存放的内容。

**答案**：

32 位 Linux 下，虚拟地址空间为 4GB：高 1GB（`0xC0000000`~`0xFFFFFFFF`）是**内核空间**（用户不可直接访问），低 3GB 是**用户空间**。典型布局（经典 ELF，x86）：

```
0xFFFFFFFF ┌─────────────────────────┐
           │      内核空间 (1GB)      │  ← 系统调用入口、内核数据
0xC0000000 ├─────────────────────────┤
           │  命令行参数、环境变量     │
           │ ┌───────────────────┐   │
           │ │    栈区 (向下增长)   │   │  ← 局部变量、函数参数、返回地址
           │ │      ↓ 栈底→栈顶     │   │     每次函数调用压栈
           │ └───────────────────┘   │
           │    (共享库/mmap 映射区)   │  ← 动态库、mmap 文件映射
           │ ┌───────────────────┐   │
           │ │    堆区 (向上增长)   │   │  ← new/malloc 动态内存
           │ │      ↑               │   │
           │ └───────────────────┘   │
           │  BSS 段 (未初始化数据)   │  ← 未初始化的全局/静态变量（零初始化）
           │  数据段 (已初始化数据)   │  ← 已初始化的全局/静态变量
           │  文字常量区 (.rodata)    │  ← 字符串常量、const 常量（只读）
           │  代码区 (.text)         │  ← 机器指令
0x08048000 ├─────────────────────────┘
```

（注：上面是经典 i386 布局示意，具体基址因发行版/ASLR/ELF 头而异，堆与栈之间通常还有 mmap 区，堆往高地址长、栈往低地址长，二者相向而行。）

**各区域内容**：

| 区域                  | 存放内容                                                     | 特点                                                         |
| --------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 栈（stack）           | 局部变量、函数参数、返回地址、临时对象                       | 向下增长（低地址方向），由编译器自动分配/释放，快；有栈溢出风险（深递归） |
| 堆（heap）            | `new`/`malloc` 分配的动态内存                                | 向上增长，手动管理（RAII/智能指针）；慢于栈；可扩充          |
| 全局/静态区           | 全局变量、`static` 变量（BSS=未初始化零初始化的，data=已初始化的） | 程序启动时创建、程序结束才销毁，生命周期最长                 |
| 文字常量区（.rodata） | 字符串字面量（如 `"hello"`）、`const` 常量、只读数据         | **只读**，写入会段错误（segfault）；注意 `char* p="abc"` 指向这里，改它=崩溃 |
| 代码区（.text）       | 编译后的机器指令                                             | 只读+可执行，禁止运行时改写                                  |

**补充说明**：`const` 局部变量在栈上（编译器优化时也可能放 .rodata），只有**字符串字面量**和 `const` **全局/静态**对象才必然落在只读区。现代 64 位 Linux 布局思路相同，只是地址更大（用户空间近 128TB）、且有 ASLR 随机化。嵌入式背景也常要关注：栈/堆是物理 RAM，.text/.rodata 常在 flash 中 XIP（片上执行），这与此处虚拟内存模型互为参照。

**通俗解释**：可以把进程地址空间想象成一条从高到低的街道：最顶层是"市政府（内核）"不可进；往下是"临时寄存处（栈）"——随用随放、往里堆东西时向下伸；中间是"长期仓库（堆）"，要多少申请多少、往上涨；再往下是"出生档案室（全局/静态）"——一出生就登记、一辈子不注销；"名人名言墙（文字常量区）"只许看不许涂改；最底下是"车间（代码区）"，CPU 在这里执行指令。

---

## 二、编程题

### 1. 编写代码展示命名空间三种使用方式的异同，并演示嵌套命名空间的访问。

**原题**：编写代码展示命名空间三种使用方式的异同，并演示嵌套命名空间的访问。

```cpp
#include <iostream>
#include <string>

namespace Outer {                  // 外层命名空间
    int value = 100;
    void hello() { std::cout << "Outer::hello\n"; }

    namespace Inner {              // 嵌套命名空间
        int value = 200;
        void hello() { std::cout << "Outer::Inner::hello\n"; }
    }
}

namespace Alias = Outer::Inner;    // 命名空间别名

int value = 999;                   // 全局变量，用于对比冲突

void using_directive() {           // 方式1：using 编译指令
    using namespace Outer;         // 引入 Outer 全部名字
    std::cout << "directive: value = " << value << '\n';  // 与全局 value 冲突时：优先局部(命名空间)还是全局？
    hello();
}

void using_declaration() {         // 方式2：using 声明
    using Outer::hello;            // 只引入 hello
    // using Outer::value;         // 若取消注释：与外层 value 冲突? 本函数无局部value，这里OK
    std::cout << "declaration: ";
    hello();                       // 直接调用
}

int main() {
    std::cout << "方式3 作用域限定符:\n";
    std::cout << "Outer::value = " << Outer::value << '\n';        // :: 显式访问
    Outer::hello();
    std::cout << "Outer::Inner::value = " << Outer::Inner::value << '\n';
    Outer::Inner::hello();

    using_directive();
    using_declaration();

    std::cout << "别名访问: ";
    Alias::hello();                // 别名 Outer::Inner
    std::cout << "全局 value = " << ::value << '\n';               // ::value 表示全局
    return 0;
}
```

要点：`using namespace` 会把名字引入当前作用域，若与局部变量/全局变量冲突，**局部优先**，易踩坑；`::value` 显式指全局。嵌套命名空间访问要逐层写 `Outer::Inner::xxx`，或用别名缩短。

---

### 2. 定义 const 常量、const 指针指向普通变量和常量，演示通过指针修改值的合法性。

**原题**：定义 const 常量、const 指针指向普通变量和常量，演示通过指针修改值的合法性。

```cpp
#include <iostream>

int main() {
    const int c = 10;        // const 常量（必须初始化）
    // c = 20;               // 错误：不能修改 const 对象
    std::cout << "c = " << c << '\n';

    int a = 1, b = 2;
    const int k = 100;

    // 1) const int *p：指向 const int 的指针 —— 内容只读，指针可变
    const int* p1 = &a;      // 可以指向普通变量
    // *p1 = 99;             // 错误：*p1 是 const int，不能通过 p1 改
    p1 = &k;                 // 可以：p1 换指向
    p1 = &b;                 // 也可以指向另一个普通变量
    std::cout << "*p1 = " << *p1 << '\n';

    // 2) int* const p2：const 指针 —— 指针不可变，内容可变
    int* const p2 = &a;
    *p2 = 55;                // 合法：内容可改
    // p2 = &b;              // 错误：p2 是 const 指针，不能改指向
    std::cout << "a = " << a << '\n';

    // 3) const int* const p3：都不能改
    const int* const p3 = &k;
    // *p3 = 1;  // 错误
    // p3 = &a;  // 错误

    // 4) 重要：把 const 对象的地址赋给普通指针 int* 是非法的
    // int* p = &k;           // 错误：丢掉 const 限定
    const int* p4 = &k;       // 必须用 const int*
    std::cout << "p4 = " << *p4 << '\n';
    return 0;
}
```

---

### 3. 使用 new 为 int 和 char* 动态分配内存，并用 delete 释放，展示 valgrind 检测内存泄漏的基本用法。

**原题**：使用 new 为 int 和 char* 动态分配内存，并用 delete 释放，展示 valgrind 检测内存泄漏的基本用法。

```cpp
#include <iostream>
#include <cstring>

int main() {
    // 动态分配一个 int
    int* p = new int(42);
    std::cout << "*p = " << *p << '\n';

    // 动态分配字符数组
    char* s = new char[32];
    std::strcpy(s, "hello, cpp");
    std::cout << "s = " << s << '\n';

    delete p;      // 释放单个对象
    delete[] s;    // 释放数组（必须配 []）
    return 0;
}
```

**valgrind 用法**：
```bash
g++ -g -o demo demo.cpp     # 编译时必须加 -g，valgrind 才能定位到行号
valgrind --leak-check=full --show-leak-kinds=all ./demo
```

输出中 `HEAP SUMMARY` 显示分配/释放字节；`definitely lost` 为 0、`ERROR SUMMARY: 0 errors` 表示**无泄漏**。若注释掉某行 `delete`，valgrind 会报告 `definitely lost` 并指出分配点。**现代补充**：valgrind 适合完整内存检查但较慢；嵌入式/CI 里更常用 **AddressSanitizer（ASan）**：`g++ -fsanitize=address -g demo.cpp && ./a.out`，同样能抓内存泄漏和越界，速度更快。

---

### 4. 实现一个 swap 函数，分别用指针和引用两种方式实现，并比较调用形式。

**原题**：实现一个 swap 函数，分别用指针和引用两种方式实现，并比较调用形式。

```cpp
#include <iostream>

// 方式一：指针
void swap_ptr(int* a, int* b) {
    int tmp = *a;
    *a = *b;
    *b = tmp;
}

// 方式二：引用
void swap_ref(int& a, int& b) {
    int tmp = a;
    a = b;
    b = tmp;
}

int main() {
    int x = 1, y = 2;

    swap_ptr(&x, &y);        // 指针：调用要取地址，函数内要解引用
    std::cout << x << ' ' << y << '\n';   // 2 1

    swap_ref(x, y);          // 引用：直接传变量，函数内直接用
    std::cout << x << ' ' << y << '\n';   // 1 2
    return 0;
}
```

**对比**：调用上指针写 `swap_ptr(&x,&y)`，引用写 `swap_ref(x,y)`（更自然）；实现上指针要 `*a` 解引用，引用直接用 `a`（编译器自动解引用）。引用版本**不可能传 NULL**、更安全，因此现代 C++ 优先引用。标准库已有 `std::swap`，日常直接用即可。

---

### 5. 编写重载函数 add，分别处理 int、double、string 类型，并测试。

**原题**：编写重载函数 add，分别处理 int、double、string 类型，并测试。

```cpp
#include <iostream>
#include <string>

int         add(int a, int b)                  { return a + b; }
double      add(double a, double b)            { return a + b; }
std::string add(const std::string& a,
                const std::string& b)          { return a + b; }

int main() {
    std::cout << add(3, 4) << '\n';                          // 7 (int)
    std::cout << add(1.5, 2.25) << '\n';                     // 3.75 (double)
    std::cout << add(std::string("Hello "), std::string("World")) << '\n'; // Hello World

    // 注意：add(1, 2.5) 会因"无法唯一确定重载"而报错吗？
    // 1 是 int，2.5 是 double：两个重载都无法完美匹配 → 尝试隐式转换
    // int版本: (1→int, 2.5→int)可行；double版本: (1→double, 2.5→double)可行 → 二义性!
    // std::cout << add(1, 2.5) << '\n';   // 编译错误：二义性调用
    return 0;
}
```

要点：`add(1, 2.5)` 会二义性报错——这正好呼应前面"函数重载"的知识点。

---

### 6. 定义一个带有默认参数的函数，并在声明和定义分离时正确设置默认值。

**原题**：定义一个带有默认参数的函数，并在声明和定义分离时正确设置默认值。

```cpp
// demo.h —— 声明，这里给出默认值
#ifndef DEMO_H
#define DEMO_H
int power(int base, int exp = 2);   // 默认值写在声明中
#endif

// demo.cpp —— 定义，不写默认值
#include "demo.h"
int power(int base, int exp) {      // 定义处不能重复写 = 2
    int r = 1;
    for (int i = 0; i < exp; ++i) r *= base;
    return r;
}

// main.cpp
#include <iostream>
#include "demo.h"
int main() {
    std::cout << power(3) << '\n';      // 3^2 = 9
    std::cout << power(3, 4) << '\n';   // 3^4 = 81
    return 0;
}
```

关键：**默认值只能出现一次**（声明或定义，二选一；规范是放声明/头文件），定义处再写 `=2` 属于重复默认实参（编译报错）。若没有头文件、单文件内先声明后定义同理。

---

### 7. 使用 inline 实现一个求最大值的函数，并对比宏 MAX 的副作用（如 MAX(i++, j)）。

**原题**：使用 inline 实现一个求最大值的函数，并对比宏 MAX 的副作用（如 MAX(i++, j)）。

```cpp
#include <iostream>

// 宏：看似安全的写法，实际仍有副作用
#define MAX(a, b) ((a) > (b) ? (a) : (b))

// inline 函数：无副作用，形参只求值一次
inline int max(int a, int b) { return a > b ? a : b; }

int main() {
    int i = 1, j = 10;

    // 宏：i++ 被求值两次！
    int r1 = MAX(i++, j);   // 展开后 ((i++)>(10)?(i++):(10)) → i 被自增两次
    std::cout << "r1 = " << r1 << ", i = " << i << '\n';  // r1=10, i=3（自增了两次）

    i = 1;
    int r2 = max(i++, j);   // 参数只求值一次
    std::cout << "r2 = " << r2 << ", i = " << i << '\n';  // r2=10, i=2（只自增一次）

    // 再看类型问题：MAX(3.5, 4) 宏没问题（无类型），max(3.5, 4) 会触发 int 转换/二义
    std::cout << MAX(3.5, 4) << '\n';   // 宏替换没问题，但结果是 double 3.5？实际 (3.5>4?3.5:4) → 4
    return 0;
}
```

宏的三大问题：**重复求值**（`i++` 求两次）、**无类型检查**、**优先级陷阱**（即使加括号也可能在复杂表达式中出错）。inline 函数三者皆无。**现代规范**：C++ 中求最大值直接用标准库 `std::max`（`<algorithm>`），比手写更安全、可移植。

---

### 8. 编写一个除法函数，当除数为 0 时抛出异常，在 main 中捕获并处理。

**原题**：编写一个除法函数，当除数为 0 时抛出异常，在 main 中捕获并处理。

```cpp
#include <iostream>
#include <stdexcept>   // std::runtime_error

double divide(double a, double b) {
    if (b == 0)
        throw std::runtime_error("除数不能为 0");
    return a / b;
}

int main() {
    try {
        std::cout << divide(10, 2) << '\n';   // 5
        std::cout << divide(10, 0) << '\n';   // 抛异常，跳到 catch
    } catch (const std::runtime_error& e) {   // 按引用捕获，避免拷贝/切片
        std::cerr << "捕获异常: " << e.what() << '\n';
    } catch (...) {                           // 兜底
        std::cerr << "未知异常\n";
    }
    std::cout << "程序继续运行\n";
    return 0;
}
```

要点：抛出的是"异常对象"，catch 用 `const std::exception&` 或派生类型引用捕获（多态，`e.what()` 取描述）；`catch (...)` 做兜底。规范上应抛标准异常派生类（`std::runtime_error`、`std::invalid_argument` 等），并让 main 的 catch 尽量窄、可预期。

---

### 9. 编写程序打印全局变量、局部变量、静态变量、堆变量、字符串常量的地址，观察内存布局。

**原题**：编写程序打印全局变量、局部变量、静态变量、堆变量、字符串常量的地址，观察内存布局。

```cpp
#include <iostream>
#include <cstdio>

int g_global = 1;              // 全局（已初始化 → 数据段）
static int g_static = 2;       // 全局静态（已初始化 → 数据段）
static int g_uninit;           // 未初始化静态 → BSS
const char* g_literal = "hello"; // 指向文字常量区的字符串

int main() {
    int local = 3;                          // 局部变量 → 栈
    static int s_local = 4;                 // 局部静态 → 数据段
    int* heap = new int(5);                 // 堆变量
    const char* lit = "world";              // 字符串字面量 → 只读常量区
    const int const_local = 6;              // 局部 const（优化后可能放寄存器/栈）

    std::printf("代码区?  main         : %p\n", (void*)&main);
    std::printf("常量区  字符串 \"world\" : %p\n", (const void*)lit);
    std::printf("全局     g_global     : %p\n", (void*)&g_global);
    std::printf("全局静态 g_static     : %p\n", (void*)&g_static);
    std::printf("未初始化 g_uninit(BSS): %p\n", (void*)&g_uninit);
    std::printf("局部静态 s_local      : %p\n", (void*)&s_local);
    std::printf("堆       heap         : %p\n", (void*)heap);
    std::printf("栈       local        : %p\n", (void*)&local);

    delete heap;
    return 0;
}
```

预期观察（32 位进程，从高到低）：`local`(栈) 地址最高 > `heap`(堆) > 全局/静态(数据段/BSS) > 字符串常量(.rodata) > `main` 函数指针(.text) 最低。若用 `-O2` 编译，`main` 可能被优化成一次调用无意义地址——可用 `volatile` 或 `-O0` 观察更直观。**注意**：现代系统默认开 ASLR，每次运行地址会随机，但"相对高低关系"不变。

---

# 第二部分：面向对象基础

## 一、问答题

### 1. 面向对象与面向过程的核心理念区别是什么？

**原题**：面向对象与面向过程的核心理念区别是什么？

**答案**：

- **面向过程（Procedure-Oriented）**：以"**算法/函数**"为中心，围绕"先做什么、再做什么"组织程序。数据（变量）和操作数据的函数**分离**，函数操作的是全局/传进来的数据。代表：C 语言。它强调"步骤"，代码易读但数据和操作耦合松散，大项目难维护、难扩展。
- **面向对象（Object-Oriented）**：以"**对象**"为中心，把**数据（属性）+ 操作数据的方法（行为）**封装成一个整体（类/对象）。通过**封装、继承、多态**三大特性组织程序。代表：C++/Java/Python。它强调"谁来做"，把"数据+行为"绑定，模拟现实世界的事物，便于大规模复用和扩展。

**核心区别一句话**：面向过程是"**以函数为核心，数据被函数操作**"；面向对象是"**以对象为核心，数据与操作它自身的函数绑定在一起**"。

**补充（现代视角）**：现实中两者常融合——面向过程适合算法密集、状态少的场景（音视频编解码里的逐帧处理、信号处理 DSP 常是面向过程/数据流式的）；面向对象适合系统建模、状态丰富、需要复用的场景（播放器框架、界面、插件体系）。C++ 本身是"多范式"语言，混合使用很常见。

**通俗解释**：面向过程像"做菜的步骤清单"：切菜→放油→下锅→翻炒，每一步只关心动作；面向对象像"把厨房做成几个小部门"：切菜组（食材+切法）、炒菜组（菜+调味）、洗碗组（餐具+清洗），每个部门自己管自己的东西和方法，要新增"蒸菜组"只要再招一组人，不用把整条流程重写。

---

### 2. 类的定义中，public、protected、private 访问修饰符的作用是什么？class 与 struct 在 C++ 中的默认访问权限有何不同？

**原题**：类的定义中，public、protected、private 访问修饰符的作用是什么？class 与 struct 在 C++ 中的默认访问权限有何不同？

**答案**：

**三个访问修饰符**（控制"谁能访问成员"）：

| 修饰符    | 类内部（本类成员函数） | 派生类 | 类外（对象/普通代码） |
| --------- | ---------------------- | ------ | --------------------- |
| public    | ✅                      | ✅      | ✅（完全公开）         |
| protected | ✅                      | ✅      | ❌（仅派生类可见）     |
| private   | ✅                      | ❌      | ❌（仅本类可见）       |

作用：**封装性**——对外只暴露必要的接口（public），内部实现细节（private）对外隐藏，降低耦合、防止误用、便于后续修改内部实现而不影响外部代码。

**class 与 struct 的默认访问权限**：
- `struct` 默认**public**（成员和继承默认都是 public）。
- `class` 默认**private**（成员和继承默认都是 private）。
- 除此之外两者在 C++ 中**完全相同**（都可以有构造/析构/成员函数/模板/继承等）。
- 现代规范：**语义约定**——想表达"只有数据、无封装逻辑"的 POD/值对象用 `struct`；想表达"有私有状态和接口封装"的类用 `class`。这是风格约定，不是语言强制。

**通俗解释**：`private` 像"保险柜（只有本人钥匙）"，`protected` 像"传给子女的备用钥匙（外人没有）"，`public` 像"大门（谁都能进）"。struct 默认门是开的，class 默认门是锁的。

---

### 3. 构造函数的特点（无返回值、与类同名、自动调用）？什么是默认构造函数？如果类中没有显式定义任何构造函数，编译器会怎么做？

**原题**：构造函数的特点（无返回值、与类同名、自动调用）？什么是默认构造函数？如果类中没有显式定义任何构造函数，编译器会怎么做？

**答案**：

**构造函数特点**：
1. **与类同名**，无返回类型（也不能写 `void`，连返回类型声明都没有）；
2. 在对象创建时**自动调用**（栈对象出作用域、new 时），程序员不能手动调用（但可以用 placement new 显式"再构造"）；
3. 可**重载**（多个不同参数的构造函数）；
4. 作用：**初始化成员**、申请资源；
5. 不能用 `const`/`virtual`/`static` 修饰（构造函数不能是虚函数，见多态部分）。

**默认构造函数（default constructor）**：**无参**（或所有参数都有默认值）的构造函数。`A() {}` 或 `A(int x = 0)` 都是默认构造。

**如果类中没有定义任何构造函数，编译器会做什么**：
- 编译器会**隐式生成一个默认构造函数**（合成默认构造），它按成员默认初始化规则处理（内置类型成员**不初始化**（垃圾值），类类型成员调用其默认构造）。
- **注意**：只要用户定义了**任何一个**构造函数（比如只写了 `A(int)`），编译器**就不再生成**默认构造——此时 `A a;` 会**编译报错**（无匹配构造函数）。
- 现代规范：显式写 `A() = default;` 让编译器生成（语义清晰）；禁止隐式转换的构造写 `explicit`。

**通俗解释**：构造函数像"新员工入职时自动办的入职手续（发工牌、配工位）"，和岗位同名（类同名）、不用提醒自动办、可以办不同套餐（重载）。如果公司没规定任何入职流程，默认就"草草入职（成员不初始化）"；但一旦你自定义了任一流程（哪怕只写了个带参数版），原来那个"默认草草流程"就没了，直接 `A a;` 这种"纯默认入职"会报错。

---

### 4. 构造函数初始化列表的作用？哪些数据成员必须在初始化列表中初始化（常量成员、引用成员、类对象成员）？

**原题**：构造函数初始化列表的作用？哪些数据成员必须在初始化列表中初始化（常量成员、引用成员、类对象成员）？

**答案**：

**初始化列表作用**：
1. 在构造函数的**函数体执行之前**完成成员初始化（成员先初始化、再进入函数体），效率高于"函数体里赋值"（后者是"先默认构造、再赋值"两步）；
2. 对某些成员是**唯一合法**的初始化途径；
3. 初始化顺序是**成员在类中的声明顺序**（与初始化列表书写顺序无关！这常是坑：按声明顺序初始化，若列表里顺序写反会有潜在问题）。

**必须在初始化列表初始化的成员**（三者都不能靠"函数体赋值"，因为它们在创建时就必须有初值、不允许"先默认构造再赋值"）：
1. **const 成员**：`const int x;` 必须在初始化列表给初值（const 不能赋值）。
2. **引用成员**：`int& r;` 必须在初始化列表绑定对象（引用必须初始化）。
3. **类类型成员且没有默认构造**：如成员 `Obj m;` 而 `Obj` 只有 `Obj(int)`，则 `m` 必须在初始化列表用 `m(参数)` 初始化；否则无法构造。即使 `Obj` 有默认构造，也推荐用初始化列表（避免"默认构造+赋值"两遍开销，尤其对象构造开销大时）。

**现代规范**：能用初始化列表就用初始化列表；C++11 起还支持**就地成员初始化**（`int x{0};`、`std::string s;` 类内默认值），与初始化列表规则并存（初始化列表优先于类内初始值）。

**通俗解释**：初始化列表像"入职前 HR 就把工牌、账号都备好了（成员一创建就有值）"；在函数体里赋值像"先进公司报到（先默认构造了个空壳）再补办手续（再赋值）"，多一道工序还慢。而 const/引用成员这类"只能出生就定"的，就像身份证号、工号——出生时就必须有，不能出生后再填。

---

### 5. 对象的大小由哪些因素决定？成员函数是否影响对象大小？内存对齐的规则是什么？

**原题**：对象的大小由哪些因素决定？成员函数是否影响对象大小？内存对齐的规则是什么？

**答案**：

**对象大小的决定因素**：
1. **非静态数据成员**本身的大小（总和）；
2. **内存对齐产生的 padding（填充字节）**；
3. **虚函数指针 vptr**：含虚函数的类对象首部有一个隐藏的 vptr（指向虚表），占一个指针大小（32 位 4B / 64 位 8B）；多继承多个虚基类对象可能多个 vptr；
4. **虚继承相关指针**（虚基类指针/偏移量，实现相关）；
5. **空类**：`sizeof(空类) == 1`（编译器给 1 字节占位，保证不同对象地址不同；空基类优化 EBO 例外）。

**成员函数不影响对象大小**：函数代码存放在**代码区**，所有对象共享同一份函数代码，**不存储在对象内部**。所以加多少成员函数，`sizeof` 都不变。静态数据成员也不影响对象大小（它属于类，不随对象存放）。

**内存对齐规则**：
1. **每个成员的起始地址必须是其对齐值（alignment，通常等于自身大小，如 int 4、double 8）的整数倍**；编译器会在成员之间插入 padding；
2. **结构体/类整体大小必须是"最大成员对齐值"的整数倍**（尾部补 padding），保证数组里每个元素都能对齐；
3. 对齐受编译器 `#pragma pack(n)`、`alignas` 影响；x86 上对齐主要影响性能（不对齐可能更慢），某些平台（ARM、x86 SSE 指令）要求严格对齐否则崩溃；
4. **结论/经验**：把**大的成员（double/指针）放前面**、小的放后面，通常能减少 padding、让 `sizeof` 更小（排序优化）。

举例：
```cpp
struct S1 { char c; int i; };      // 可能 8 字节（c 后 padding 3）
struct S2 { int i; char c; };      // 也可能 8 字节（尾部 padding 3）
struct S3 { char c; char d; int i; };// 8 字节（c,d 后 padding 2）
```
顺序会影响具体 padding 多少。

**通俗解释**：对象像一个"收纳盒"，CPU 喜欢从"对齐的格子"开始读数据（比如 4 字节的数据必须从 4 的倍数地址开始），所以盒子里要放一些"空泡沫（padding）"把数据摆齐。成员函数像"使用说明书"，放仓库里大家共用，不占每个盒子空间。

---

### 6. 析构函数的特征（无参、无返回值、不能重载）？析构函数在哪些时机自动调用？手动调用析构函数会有什么后果？

**原题**：析构函数的特征（无参、无返回值、不能重载）？析构函数在哪些时机自动调用？手动调用析构函数会有什么后果？

**答案**：

**析构函数特征**：
1. 名字为 `~类名`，**无参数**、**无返回类型**（连 void 都不写）；
2. **不能重载**（全类只有一个析构函数）；
3. **每个类至多一个**；
4. 作用：**释放资源**（delete 堆内存、关闭文件、解锁等），执行对象销毁前的清理；
5. 可以定义为 `virtual`（**基类析构推荐虚析构**，见多态部分）；C++11 起默认 `noexcept`，析构里抛异常会 `std::terminate`。

**自动调用的时机**：
1. **栈对象离开其作用域**时；
2. **堆对象被 `delete`** 时；
3. **临时对象**在表达式结束/临时生命周期结束时；
4. **异常栈展开**（unwinding）时：抛出异常、对象所在栈帧被销毁时；
5. 程序正常结束、静态/全局对象销毁时（逆序）。
6. 容器销毁时（vector 析构会销毁内部元素）。

**手动调用析构函数的后果**：
- `obj.~T();` 是**合法语法**，但它只执行"析构逻辑"，**并不释放内存**；之后该对象的存储仍存在。
- 更危险的是：如果手动调用后，对象**正常生命周期结束时还会被再次自动析构**——**析构被调用两次**（double destroy），若析构里有 `delete`/关闭资源，会产生**重复释放（double free）**，是未定义行为、常崩溃。
- 正确做法：**永远不要手动调用析构**（唯一例外是 placement new 配合手动析构的极少数底层场景：先 `p->~T();` 再释放原始内存）。这也是 RAII 与智能指针帮你规避的坑。

**通俗解释**：析构函数像"离职时的退房清点（交钥匙、关设备）"，是系统自动帮你做的。你非要在正常流程之外手动"先交一次钥匙"，等系统到时再"交第二次"——第二次就没钥匙可交了（双重释放），乱套。

---

### 7. 堆对象（new）的创建和销毁过程（operator new + 构造函数，析构函数 + operator delete）。

**原题**：堆对象（new）的创建和销毁过程（operator new + 构造函数，析构函数 + operator delete）。

**答案**：

`new T(args)` 的**两个步骤**：
1. **分配内存**：调用 **`operator new(sizeof(T))`**（全局或类内重载版本）从堆上分配原始内存（失败抛 `std::bad_alloc`）。
2. **构造对象**：在这块内存上调用**构造函数** `T(args)`，完成成员初始化。

`delete p` 的**两个步骤**（与 new 严格对称）：
1. **调用析构函数** `p->~T()`，执行清理（释放成员资源）；
2. **释放内存**：调用 **`operator delete(p)`** 归还内存给堆。

所以 `new`/`delete` 是"**分配内存 + 构造**"、"**析构 + 释放内存**"的封装组合；而 `malloc/free` 只做第 1/2 步中的"纯内存"部分。这也解释了两者不能混用。

**相关补充**：
- **placement new**：`new (ptr) T(args)` —— 不分配内存，只在**指定地址** `ptr` 上调用构造函数（用于自定义内存池、容器原地构造如 `std::vector` 扩容、嵌入式/音视频的共享内存场景）。对应销毁要**手动调用析构** `ptr->~T()`，再释放/归还内存池。
- 数组 `new T[n]` 额外记录元素个数，`delete[]` 需要它来逐个析构（所以 `new[]` 必须配 `delete[]`）。
- **现代规范**：直接 `new/delete` 属于"裸指针管理"，易泄漏/重复释放；现代 C++ 推荐用 **unique_ptr/shared_ptr** 自动管理（同样内部走 operator new + 构造、析构 + operator delete 的完整流程，只是由智能指针在作用域结束时自动触发 delete）。

**通俗解释**：`new` = "先找地皮（operator new）再盖房（构造）"；`delete` = "先拆房（析构）再还地皮（operator delete）"。placement new = "地皮是别人（内存池）给的，你只负责在上面盖房"，拆房时也要自己手动拆。

---

### 8. 什么是 this 指针？它的类型是什么？在成员函数中如何显式使用？this 指针存储在何处？

**原题**：什么是 this 指针？它的类型是什么？在成员函数中如何显式使用？this 指针存储在何处？

**答案**：

- **this 指针**：**非静态成员函数**内部隐含的一个指针，**指向调用该函数的那个对象**。它让成员函数知道"我在为哪个对象工作"，从而访问该对象的成员。
- **类型**：
  - 非 const 成员函数中：`ClassName* const this`（this 本身是 const，不能改指向，但可改指向的对象内容）；
  - const 成员函数中：`const ClassName* const this`（指向的对象也 const，只能读不能改）。
- **显式使用**：平时编译器自动加上 `this->`，可以显式写：
  - 成员与参数同名时区分：`this->x = x;`
  - 返回对象自身的引用/指针：`return *this;`（重载 `operator=`、`operator+=` 时返回 `*this` 实现链式调用）或 `return this;`
  - 判断是否同一对象：`if (this == &other)`
- **存储位置**：this **不存储在对象内部**（它是成员函数被调用时编译器通过某种方式传入的——通常放在**寄存器**（如 x86 的 ECX / x64 的 RCX）或**栈**上传递），也可以理解为一个"隐含参数"。`sizeof(对象)` 不受 this 影响。
- 注意：**静态成员函数没有 this**（它不依附于某个具体对象），所以不能直接访问非静态成员。

**通俗解释**：this 就像"工牌上写着自己的工号"。每个员工（成员函数）上班时都会带上工牌（this），知道自己是"哪位"才能去自己的工位（对象数据）干活。静态成员函数像"公司公告"，属于全公司，不带个人工牌，所以不能碰某个人的工位。

---

## 二、编程题

### 1. 定义一个 Computer 类，包含品牌（char* 动态分配）和价格（double），编写构造函数（初始化列表分配内存）、析构函数（释放内存）、print 成员函数。在 main 中创建栈对象和堆对象，验证析构调用。

**原题**：定义一个 Computer 类，包含品牌（char* 动态分配）和价格（double），编写构造函数（初始化列表分配内存）、析构函数（释放内存）、print 成员函数。在 main 中创建栈对象和堆对象，验证析构调用。

```cpp
#include <iostream>
#include <cstring>

class Computer {
    char* _brand;   // 动态分配的字符串
    double _price;
public:
    Computer(const char* brand, double price)
        : _price(price) {                 // 初始化列表
        _brand = new char[std::strlen(brand) + 1];
        std::strcpy(_brand, brand);
        std::cout << "构造: " << _brand << '\n';
    }
    ~Computer() {                          // 析构：释放堆内存
        std::cout << "析构: " << _brand << '\n';
        delete[] _brand;
    }
    void print() const {
        std::cout << _brand << ", 价格 " << _price << '\n';
    }
};

int main() {
    Computer stackComp("联想", 5999.0);    // 栈对象
    stackComp.print();

    Computer* p = new Computer("苹果", 12999.0);  // 堆对象
    p->print();
    delete p;                               // 必须手动 delete 才会析构

    std::cout << "main 结束，栈对象自动析构\n";
    return 0;
}
```

观察输出顺序：堆对象 `delete` 时析构一次，main 结束栈对象自动析构。若忘了 `delete p` 则堆对象永不析构（泄漏）——这正是裸 new/delete 的坑，后续用智能指针解决。

---

### 2. 定义一个 Point 类（坐标 int），编写无参构造、有参构造、拷贝构造，打印构造顺序，并测试 sizeof(Point) 与内存对齐（观察不同成员顺序对大小的影响）。

**原题**：定义一个 Point 类（坐标 int），编写无参构造、有参构造、拷贝构造，打印构造顺序，并测试 sizeof(Point) 与内存对齐（观察不同成员顺序对大小的影响）。

```cpp
#include <iostream>

class Point {
    int _x, _y;      // 8 字节，天然对齐，无 padding
public:
    Point() : _x(0), _y(0) { std::cout << "无参构造\n"; }
    Point(int x, int y) : _x(x), _y(y) { std::cout << "有参构造 (" << _x << ',' << _y << ")\n"; }
    Point(const Point& o) : _x(o._x), _y(o._y) { std::cout << "拷贝构造\n"; }
    ~Point() { std::cout << "析构\n"; }
    int getX() const { return _x; }
};

// 对比对齐效果：不同成员顺序
struct A { char c; int i; char d; };        // 1+3pad+4+1+3pad = 12
struct B { int i; char c; char d; };        // 4+1+1+2pad = 8
struct C { char c; double d; };             // 1+7pad+8 = 16
struct D { double d; char c; };             // 8+1+7pad = 16

int main() {
    Point p1;                  // 无参
    Point p2(3, 4);            // 有参
    Point p3 = p2;             // 拷贝构造
    std::cout << "sizeof(Point) = " << sizeof(Point) << '\n';     // 8
    std::cout << "A=" << sizeof(A) << " B=" << sizeof(B)
              << " C=" << sizeof(C) << " D=" << sizeof(D) << '\n'; // 12 8 16 16
    return 0;
}
```

演示要点：成员顺序影响 padding；`double` 成员导致对齐到 8。把大成员放前面能减少 padding（本例 A 12 字节 vs B 8 字节）。

---

### 3. 定义一个包含 const int 和 int& 成员的类，在初始化列表中正确初始化，并验证不能赋值。

**原题**：定义一个包含 const int 和 int& 成员的类，在初始化列表中正确初始化，并验证不能赋值。

```cpp
#include <iostream>

class Widget {
    const int _id;   // const 成员
    int& _ref;       // 引用成员
public:
    Widget(int id, int& r) : _id(id), _ref(r) {}   // 必须在初始化列表
    void show() const { std::cout << "id=" << _id << ", ref=" << _ref << '\n'; }
};

int main() {
    int n = 10;
    Widget w(100, n);
    w.show();          // id=100, ref=10

    // 编译错误示例：
    // w._id = 200;    // const 不能赋值
    // w._ref = n;     // 这是"给 n 赋值"（w._ref 就是 n），不是改绑定，合法！
    // 要想"不能赋值"验证，可在类外访问: 成员私有不可访问；改成 public 后 _id 赋值报错
    return 0;
}
```

要点：`const int _id;` 和 `int& _ref;` **离开初始化列表就无法构造**——这是必须在初始化列表初始化的经典案例。若要直观看到"const 不能赋值"编译错误，把成员改 public 后 `w._id = 1;` 会报错。

---

### 4. 定义一个 Line 类，包含两个 Point 对象成员，在 Line 的构造函数初始化列表中初始化它们，并验证构造和析构顺序。

**原题**：定义一个 Line 类，包含两个 Point 对象成员，在 Line 的构造函数初始化列表中初始化它们，并验证构造和析构顺序。

```cpp
#include <iostream>

class Point {
    int _x, _y;
public:
    Point(int x = 0, int y = 0) : _x(x), _y(y) {
        std::cout << "Point(" << _x << ',' << _y << ") 构造\n";
    }
    ~Point() { std::cout << "Point(" << _x << ',' << _y << ") 析构\n"; }
    void show() const { std::cout << '(' << _x << ',' << _y << ") "; }
};

class Line {
    Point _p1, _p2;      // 成员按声明顺序构造
public:
    Line(int x1, int y1, int x2, int y2)
        : _p1(x1, y1), _p2(x2, y2) {       // 初始化列表
        std::cout << "Line 构造\n";
    }
    ~Line() { std::cout << "Line 析构\n"; }
    void show() const { _p1.show(); _p2.show(); std::cout << '\n'; }
};

int main() {
    Line line(0, 0, 5, 5);
    line.show();
    std::cout << "--- main 结束 ---\n";
    return 0;
}
```

观察顺序：
- 构造：`Point(0,0)` → `Point(5,5)`（**按成员声明顺序** `_p1,_p2`，与初始化列表书写顺序一致这里）→ `Line`；
- 析构（**严格逆序**）：`Line` → `Point(5,5)` → `Point(0,0)`（成员逆序销毁）。

如果把初始化列表写成 `_p2(...), _p1(...)`，**实际构造仍按声明顺序 `_p1` 先构造**——这是"初始化顺序与列表书写顺序无关"的活演示。

---

### 5. 利用 new/delete 工作步骤，实现一个只能生成栈对象（或只能生成堆对象）的类，并测试。

**原题**：利用 new/delete 工作步骤，实现一个只能生成栈对象（或只能生成堆对象）的类，并测试。

```cpp
#include <iostream>
#include <cstdlib>

// 只能生成栈对象：把 operator new / operator delete 设为私有
class OnlyStack {
    void* operator new(std::size_t) = delete;   // 或 private 声明
    void operator delete(void*) = delete;
public:
    OnlyStack() { std::cout << "OnlyStack 构造\n"; }
    ~OnlyStack() { std::cout << "OnlyStack 析构\n"; }
};

// 只能生成堆对象：把析构函数设为私有
// （栈对象离开作用域需要自动析构，析构私有则无法在栈上创建）
class OnlyHeap {
    ~OnlyHeap() { std::cout << "OnlyHeap 析构(私有)\n"; }
public:
    OnlyHeap() { std::cout << "OnlyHeap 构造\n"; }
    void destroy() { delete this; }   // 由成员函数调用私有析构来销毁
};

int main() {
    OnlyStack s;                 // 栈对象 OK
    // OnlyStack* p = new OnlyStack;  // 错误：operator new 私有/删除

    // 注意：new OnlyHeap 需要访问析构吗？new 本身不需要，delete 才需要
    OnlyHeap* h = new OnlyHeap;  // OK（new 只走 operator new + 构造）
    // OnlyHeap h2;              // 错误：栈对象离开作用域无法调用私有析构
    h->destroy();                // 手动销毁
    return 0;
}
```

要点：`new` 的步骤是"operator new（分配）+ 构造函数"，`delete` 的步骤是"析构 + operator delete（释放）"——所以把 `operator new` 私有能阻止 `new`（不能分配）；把**析构**私有能阻止栈对象（编译器在离开作用域时需要调用析构）。C++11 起更简洁的是用 `= delete`。**实用提醒**：这种模式主要用于强制资源生命周期管理（如禁止栈上大缓冲、只允许池化分配），日常编码用得不多；现代更常见的是"禁止拷贝/移动"（`= delete`）配合 RAII。

---

# 第三部分：拷贝构造、赋值、静态成员与友元

## 一、问答题

### 1. 拷贝构造函数的定义形式为什么必须是 类名(const 类名&)？如果参数不是引用会发生什么？为什么需要 const？

**原题**：拷贝构造函数的定义形式为什么必须是 类名(const 类名&)？如果参数不是引用会发生什么？为什么需要 const？

**答案**：

**标准形式**：`ClassName(const ClassName& other)`。

**为什么参数必须是引用（否则无限递归）**：
- 拷贝构造是"用一个同类型对象初始化另一个对象"。如果参数改为**按值** `ClassName(ClassName other)`，那么传入实参时本身就要"拷贝构造"一个形参——而形参的构造又要调用拷贝构造……形成**无限递归**，编译直接报错（"recursive definition"）。所以拷贝构造参数必须是**引用**（或 `ClassName&&` 移动构造场景下的右值引用）。
- 引用绑定不会触发拷贝，从而切断递归。

**为什么需要 const**：
1. 拷贝构造**不应该修改源对象**，用 const 表达"只读拷贝"的语义；
2. 允许用**常量对象**、**临时对象**来拷贝初始化（`const A&` 能绑定右值/临时量），更通用；
3. 便于编译器做优化，也符合 CppCoreGuidelines 的 const 正确性。

**补充**：参数还可以是 `const volatile A&`（极少用）。C++11 起还有**移动构造** `A(A&&)`，二者并存时"右值优先移动、左值拷贝"。

**通俗解释**：拷贝构造 = "复印机复印 A 出来一份 B"。复印机的"进纸口（参数）"必须用"看原件的方式（引用）"——如果进纸口是"再放一台复印机（按值）"，那这台复印机复印时又要进纸口、又要有复印机……没完没了。const 则要求"复印时不许在原件上写字"。

---

### 2. 拷贝构造函数的调用时机有哪些？值传递和值返回会触发拷贝构造，如何避免不必要的拷贝？

**原题**：拷贝构造函数的调用时机有哪些？值传递和值返回会触发拷贝构造，如何避免不必要的拷贝？

**答案**：

**调用时机**：
1. **用一个对象初始化另一个对象**：`A b = a;`、`A b(a);`（注意是**初始化**，不是赋值）；
2. **函数按值传参**：`void f(A x)` 传实参时拷贝；
3. **函数按值返回**：`A f() { return a; }` 返回时（在未优化情况下）拷贝；
4. **放入容器**：`vec.push_back(a)` 在 vector 中拷贝构造元素；`std::pair/make_pair` 等；
5. **异常抛出/捕获**（按值抛/捕获时）。
6. 初始化列表、`{}` 列表初始化等。

**注意区分"赋值"**：`A b; b = a;` 是**赋值运算符**（对象已存在），不是拷贝构造。

**如何避免不必要的拷贝（现代优化手段）**：
1. **引用传参/返回**：`void f(const A&)`、返回引用（注意不能返回局部对象引用）；
2. **返回值优化（RVO）/具名返回值优化（NRVO）**：编译器在 C++11 前就允许 elide（省略）拷贝，C++17 起**强制**保证 `return A(...)` 的纯右值场景不发生拷贝（mandatory copy elision）；
3. **移动语义**：C++11 起按值返回局部对象时，若不能省略拷贝，会**移动**（调用移动构造）而非深拷贝；传参也可以用 `std::move` 显式移动；
4. **`push_back` 用 `emplace_back`** 原地构造，避免临时对象+拷贝；
5. 智能指针/容器按值传递语义时用 `std::shared_ptr` 等避免复制底层大数据。

**通俗解释**：拷贝构造被触发 = "每次都要复印一份"。避免方式：把文件传给别人看时只给"借阅证（引用）"而不是复印（引用传参）；用完直接还回来的就"原地转手（RVO/移动）"而不是复印再销毁原件。

---

### 3. 赋值运算符函数的标准形式是什么？为什么返回值是 类名&？为什么参数是 const 类名&？

**原题**：赋值运算符函数的标准形式是什么？为什么返回值是 类名&？为什么参数是 const 类名&？

**答案**：

**标准形式**：
```cpp
ClassName& operator=(const ClassName& other) {
    if (this != &other) {          // 自赋值检查
        // 释放旧资源，拷贝新资源（深拷贝）
    }
    return *this;
}
```

**为什么返回值是 `ClassName&`**：
1. **支持链式赋值**：`a = b = c;` 等价于 `a.operator=(b.operator=(c))`，要求 `b.operator=(c)` 返回一个可继续赋值的 `b`（引用），否则链式断掉；
2. **支持 `if ((a = b) == c)` 等表达**；
3. 返回引用避免"返回值=拷贝一份"的多余开销。
   （也有极少数约定返回 `void`/const 的，但标准惯例是 `T&`。）

**为什么参数是 `const ClassName&`**：
1. 不修改源对象（const 语义正确）；
2. 引用避免按值传参触发拷贝构造（浪费且可能递归问题）；
3. 允许用临时/常量对象赋值。

**补充要点**：
- **自赋值保护**：`if (this == &other) return *this;` 防止 `a = a` 时"先释放再拷贝自己"导致崩溃；
- **自赋值安全（modern）**：用"先拷贝副本再交换（copy-and-swap）"惯用法 `swap` 可天然处理自赋值与异常安全；
- C++11 起还有**移动赋值** `operator=(T&&)`，以及 `= default`/`= delete` 显式控制。

**通俗解释**：赋值运算符 = "把 a 的内容整体改成 b 的内容"。返回"a 本人（引用）"是为了支持 `a=b=c` 这种"连环复制"——复制完 b←c 后，还要能把 b 接着复制给 a，所以每一步都要把"本人"交出去；参数用 const 引用 = "看 b 的原件但不复印、也不许改 b"。

---

### 4. 浅拷贝和深拷贝的区别？当类中含有指针成员并申请堆内存时，为什么必须自定义拷贝构造和赋值运算符？

**原题**：浅拷贝和深拷贝的区别？当类中含有指针成员并申请堆内存时，为什么必须自定义拷贝构造和赋值运算符？

**答案**：

- **浅拷贝（shallow copy）**：**按字节拷贝成员**，指针成员只复制"指针值（地址）"，**两个对象共享同一块堆内存**。编译器默认生成的拷贝构造/赋值就是浅拷贝。
- **深拷贝（deep copy）**：指针成员**重新申请独立内存**，并把源对象指向的数据**逐字节复制**过去，两个对象**各自拥有独立内存**，互不影响。

**为什么含指针+堆内存的类必须自定义（编译器默认浅拷贝的灾难）**：
- 假设 `String s1("abc")` 内部 `_ptr = new char[4]`。默认浅拷贝 `String s2 = s1;` 后，`s1._ptr == s2._ptr` **指向同一块内存**，问题接踵而至：
  1. **双重释放（double free）**：两个对象析构时都 `delete[] _ptr` → 同一内存被释放两次 → 未定义行为、崩溃；
  2. **相互干扰**：修改 `s2` 的内容会同时"改" `s1`（因为同一块内存）；
  3. **泄漏**：赋值时若先释放旧内存，可能释放的是别人的内存或造成已释放区又被使用；
  4. **悬空/野指针**：对象生命周期结束相互影响。
- 因此必须自定义**拷贝构造 + 拷贝赋值 + 析构**（三合成），实现**深拷贝语义**，让每个对象持有自己的堆内存。

**通俗解释**：浅拷贝像"两人各拿一张写着同一个储藏室地址的纸条"——纸片是各自的，但门是同一扇，一个人进去翻乱了，另一个人看到的也是乱的；两人退房时都要去关那扇门，结果关两次。深拷贝像"给每个人单独复制一间一模一样的储藏室"——各管各的，互不干扰。

---

### 5. 三合成原则（Rule of Three）是什么？为什么三者通常需要一起定义？

**原题**：三合成原则（Rule of Three）是什么？为什么三者通常需要一起定义？

**答案**：

**Rule of Three（三合成原则）**：如果类**显式声明了**以下三者中的**任意一个**，那么**通常必须同时显式定义另外两个**：
1. 析构函数（destructor）
2. 拷贝构造函数（copy constructor）
3. 拷贝赋值运算符（copy assignment operator）

**为什么通常要一起定义**：
- 三者本质上是**同一套资源管理语义**的三面：析构负责"释放资源"，拷贝构造负责"基于现有对象生成独立副本"，拷贝赋值负责"把副本交给已有对象"。
- 如果你**显式写了析构**（说明类管理着某种资源：堆内存、文件、锁），那么默认生成的**浅拷贝拷贝构造/赋值**就是错的（双重释放、共享内存），必须自己实现深拷贝；
- 反之，如果你自定义了拷贝构造，通常说明"深拷贝需要特殊处理"，那么赋值和析构也需要一致处理；
- 三者不一致会导致**资源泄漏、双重释放、悬空指针**等严重 bug。

**现代规范（C++11 起扩展）**：
- **Rule of Five**：还要考虑**移动构造函数**和**移动赋值运算符**（有自定义析构/拷贝时，编译器不再隐式生成移动操作）；
- **Rule of Zero**（首选）：设计类时尽量用 RAII 成员（`std::string`、`std::vector`、`unique_ptr`、`shared_ptr` 等），让这些资源管理类自动处理复制/移动/析构，你的类**一个都不需要自定义**，即"零特殊成员"。这是现代 C++ 推荐的方向。

**通俗解释**：三合成像"开餐馆的三本账（进货-结账-盘点）"：只要你有"拆店（析构）"的规矩，就必须配套"克隆店（拷贝构造）"和"接手店（拷贝赋值）"的规矩，否则账目对不上（资源乱套）。而 Rule of Zero 更进一步：直接用"连锁托管公司（智能指针/容器）"替你管这三本账，自己当甩手掌柜。

---

### 6. 静态数据成员的特征（属于类、所有对象共享、必须在类外初始化）？静态成员函数的特点（无 this 指针，只能访问静态成员）？

**原题**：静态数据成员的特征（属于类、所有对象共享、必须在类外初始化）？静态成员函数的特点（无 this 指针，只能访问静态成员）？

**答案**：

**静态数据成员特征**：
1. **属于类、不属于某个对象**：所有对象**共享同一份**静态数据成员，不随对象创建而创建、不随对象销毁而销毁；
2. **不占用对象大小**：`sizeof` 不含它；
3. **必须在类外定义/初始化**（C++17 之前）：类内只是声明，类外要写 `int ClassName::_count = 0;`（必须带类型和作用域限定）。**C++17 起可用 `inline` 在类内初始化**：`static inline int _count = 0;`，简化了写法；
4. 生命周期：程序启动时创建、结束时销毁；
5. 可以是 const（`static const` 可用作编译期常量，如类内数组大小，在 C++11 `constexpr` 之前很常见）。

**静态成员函数特征**：
1. **无 this 指针**：它不依附于某个对象，调用形式 `ClassName::func()`，无需对象；
2. **只能访问静态成员**（静态数据成员、静态函数）；不能直接访问非静态成员（因为没有 this 无法定位到具体对象）；若要访问非静态成员，必须通过传入的对象/指针（如 `static void func(const ClassName& o)`）；
3. 不能是**虚函数**、不能是 `const` 成员函数（因为都依赖 this）；
4. 调用方式：`ClassName::func()` 或用对象调用（`obj.func()` 也允许，但语义上仍是静态的）。

**典型用途**：对象计数（`_count++`）、工厂函数、工具函数、单例实例的获取。

**通俗解释**：静态数据成员像"全公司的打卡总表"，每个员工（对象）共用一张表，不属于任何一个人的工位；静态成员函数像"公司广播"，对全公司喊话不需要指名道姓到某个员工（无 this），所以广播里也不能直接说"去某某的工位抽屉里拿东西"（不能访问非静态成员）。

---

### 7. 友元的三种形式（普通函数、成员函数、友元类）及其优缺点。友元是否破坏封装性？是否可继承？是否单向？

**原题**：友元的三种形式（普通函数、成员函数、友元类）及其优缺点。友元是否破坏封装性？是否可继承？是否单向？

**答案**：

**三种形式**：
1. **友元普通函数**：`friend void distance(const Point&, const Point&);` —— 让一个外部普通函数能访问类的私有成员；
2. **友元成员函数**：`friend void Line::show(const Point&);` —— 只授权**另一个类的某个成员函数**访问私有成员；
3. **友元类**：`friend class Line;` —— 授权**整个类**的所有成员函数访问。

**优缺点**：
- 优点：允许"关系密切"的函数/类绕过封装直接高效访问私有数据（如运算符重载 `operator<<`、数学计算类互相配合），不用公开一堆 getter；
- 缺点：**破坏封装性**——私有成员对友元完全敞开，外部能直接操作内部实现；友元使类的"朋友圈"扩散，降低模块独立性，增加耦合与维护成本。

**三个经典问题**：
1. **是否破坏封装性**：是——友元是 C++ 封装的"后门"，把私有访问权授予外部实体。但它属于**类的自主授权**（由类自己声明谁是朋友），比"全公开"仍强；规范上应**慎用、最小化授权**（能用友元成员函数就别用友元类）。
2. **是否可继承**：**否**——友元关系**不继承**，也不传递：基类的友元不是派生类的友元；派生类的友元也不能访问基类私有成员（除非基类也声明）。
3. **是否单向**：**是**——友元是**单向**的：A 声明 B 是朋友，意味着 B 能访问 A 的私有成员；但 **A 不能访问 B 的私有成员**（除非 B 也声明 A 为友元）。"友谊"不互惠。

**通俗解释**：友元 = "主人（类）主动把家里钥匙配给特定的人（友元）"。三个问题：钥匙给了外人当然就没那么私密（破坏封装）；朋友的孩子不等于朋友（不继承）；我给了你钥匙不等于你也会给我钥匙（单向）。

---

### 8. const 成员函数的作用？const 对象只能调用 const 成员函数，非 const 对象优先调用非 const 版本（若重载），为什么？

**原题**：const 成员函数的作用？const 对象只能调用 const 成员函数，非 const 对象优先调用非 const 版本（若重载），为什么？

**答案**：

**const 成员函数**：在函数声明后加 `const`（如 `void show() const;`），表示该函数**承诺不修改对象的非静态成员**（内部 `this` 是 `const ClassName* const`）。作用：
1. 表达"只读操作"语义，让常量对象也能调用；
2. 便于编译器优化、提高代码可读性/安全性；
3. 是实现 `const` 正确性（const-correctness）的核心手段。

**为什么 const 对象只能调用 const 成员函数**：
- const 对象 = "只读对象"，其 `this` 是 `const ClassName*`。
- 若调用非 const 成员函数，相当于"通过 const 指针调用可能修改对象的函数"，编译器无法保证对象不被修改，因此**编译期直接禁止**（除非用 const_cast 强制去掉 const，那是危险操作）。
- 而 const 成员函数承诺不改，所以允许。

**为什么非 const 对象优先调用非 const 版本（若重载）**：
- 若类同时有 `void show() const;` 和 `void show();`（const 与非 const 构成**重载**），非 const 对象调用时，**非 const 版本是更精确匹配**（不需要把对象当 const 用），因此优先；
- 只有 const 对象/通过 const 引用或指针访问时，才调用 const 版本；
- 这种"两个版本"常用于：非 const 版本内部转调 const 版本实现，然后做修改（如返回可变引用）；const 版本提供只读访问。

**现代规范**：能声明 const 就声明 const；访问器默认 const；返回内部指针/引用时 const 版本返回 `const T&`。`mutable` 成员可被 const 成员函数修改（如缓存计数），是"const 中的例外"。

**通俗解释**：const 成员函数 = "只读说明书（只读不写）"；const 对象 = "展示柜里不许碰的展品（只读对象）"。展品只允许看（只读说明书）；普通物件两本说明书都行，但既然能碰，就用"可动手版（非 const 版）"更顺手——因为普通版能做更多事。

---

## 二、编程题

### 1. 完善 Computer 类，显式定义拷贝构造函数和赋值运算符函数（深拷贝），并在析构函数中释放内存，验证三合成原则。

**原题**：完善 Computer 类，显式定义拷贝构造函数和赋值运算符函数（深拷贝），并在析构函数中释放内存，验证三合成原则。

```cpp
#include <iostream>
#include <cstring>

class Computer {
    char* _brand;
    double _price;
    void copyFrom(const Computer& o) {           // 深拷贝辅助
        _brand = new char[std::strlen(o._brand) + 1];
        std::strcpy(_brand, o._brand);
        _price = o._price;
    }
public:
    Computer(const char* brand, double price) : _price(price) {
        _brand = new char[std::strlen(brand) + 1];
        std::strcpy(_brand, brand);
        std::cout << "构造 " << _brand << '\n';
    }
    Computer(const Computer& o) {                // 深拷贝构造
        copyFrom(o);
        std::cout << "拷贝构造 " << _brand << '\n';
    }
    Computer& operator=(const Computer& o) {     // 深拷贝赋值
        if (this != &o) {                        // 自赋值保护
            delete[] _brand;                     // 释放旧资源
            copyFrom(o);                         // 拷贝新资源
        }
        std::cout << "赋值运算 " << _brand << '\n';
        return *this;
    }
    ~Computer() {
        std::cout << "析构 " << _brand << '\n';
        delete[] _brand;
    }
    const char* brand() const { return _brand; }
};

int main() {
    Computer a("联想", 5000);
    Computer b = a;          // 拷贝构造（深拷贝，两个独立品牌字符串）
    Computer c("戴尔", 6000);
    c = a;                   // 赋值运算（深拷贝）
    std::cout << "b=" << b.brand() << ", c=" << c.brand() << '\n';
    // 三者析构各释放自己的内存，无双重释放
    return 0;
}
```

验证三合成：析构、拷贝构造、拷贝赋值三者齐全且都做深拷贝。注释掉任一都会出问题（双重释放或共享内存）。

---

### 2. 定义一个 String 类（动态 char*），实现默认构造、有参构造、拷贝构造、赋值运算符、析构函数，并测试复制和赋值。

**原题**：定义一个 String 类（动态 char*），实现默认构造、有参构造、拷贝构造、赋值运算符、析构函数，并测试复制和赋值。

```cpp
#include <iostream>
#include <cstring>

class String {
    char* _str;
public:
    String(const char* s = "") {                 // 默认+有参（带默认值）
        _str = new char[std::strlen(s) + 1];
        std::strcpy(_str, s);
    }
    String(const String& o) {                    // 拷贝构造（深拷贝）
        _str = new char[std::strlen(o._str) + 1];
        std::strcpy(_str, o._str);
    }
    String& operator=(const String& o) {         // 赋值（深拷贝）
        if (this != &o) {
            delete[] _str;
            _str = new char[std::strlen(o._str) + 1];
            std::strcpy(_str, o._str);
        }
        return *this;
    }
    ~String() { delete[] _str; }
    const char* c_str() const { return _str; }
    void set(const char* s) {
        delete[] _str;
        _str = new char[std::strlen(s) + 1];
        std::strcpy(_str, s);
    }
};

int main() {
    String s1("Hello");
    String s2(s1);           // 拷贝构造
    String s3;               // 默认
    s3 = s1;                 // 赋值

    s1.set("Changed");
    std::cout << "s1=" << s1.c_str() << ", s2=" << s2.c_str()
              << ", s3=" << s3.c_str() << '\n';
    // s2、s3 仍是 "Hello" —— 证明深拷贝（互不影响）
    return 0;
}
```

**现代规范提示**：这里手写的 String 仅用于理解底层机制；实际工程直接 `std::string` 即可，且**移动构造/移动赋值 + copy-and-swap** 是现代手写类的最佳实践（见第九部分）。

---

### 3. 编写一个 Point 类，添加静态数据成员 _count 记录对象个数，在构造和析构中更新，并提供静态函数 getCount。

**原题**：编写一个 Point 类，添加静态数据成员 _count 记录对象个数，在构造和析构中更新，并提供静态函数 getCount。

```cpp
#include <iostream>

class Point {
    int _x, _y;
    static int _count;                     // 静态数据成员（声明）
public:
    Point(int x = 0, int y = 0) : _x(x), _y(y) { ++_count; }
    Point(const Point& o) : _x(o._x), _y(o._y) { ++_count; }  // 拷贝也计数！
    ~Point() { --_count; }
    static int getCount() { return _count; }   // 静态成员函数
};
int Point::_count = 0;                     // 类外定义/初始化（必需）

int main() {
    std::cout << "初始 count = " << Point::getCount() << '\n';   // 0
    Point a, b(1, 2);
    {
        Point c = a;                       // 拷贝构造，+1
        std::cout << "块内 count = " << Point::getCount() << '\n'; // 3
    }                                      // c 析构，-1
    std::cout << "块外 count = " << Point::getCount() << '\n';   // 2
    return 0;
}
```

要点：拷贝构造也要 `++_count`，否则"被拷贝出来的对象"没计数；类外 `int Point::_count = 0;` 是 C++17 前必须的类外定义。C++17 起可改成 `static inline int _count = 0;` 在类内完成。

---

### 4. 将一个普通函数 distance 声明为 Point 的友元，计算两点距离；再将一个 Line 类的成员函数设为友元，并演示。

**原题**：将一个普通函数 distance 声明为 Point 的友元，计算两点距离；再将一个 Line 类的成员函数设为友元，并演示。

```cpp
#include <iostream>
#include <cmath>

class Line;   // 前置声明

class Point {
    int _x, _y;
public:
    Point(int x = 0, int y = 0) : _x(x), _y(y) {}
    void show() const { std::cout << '(' << _x << ',' << _y << ')'; }

    // 1) 友元普通函数
    friend double distance(const Point& a, const Point& b);
    // 2) 友元成员函数：Line::length 可以访问 Point 私有成员
    friend double Line::length(const Point&, const Point&);   // 需 Line 完整声明后
};

class Line {
public:
    double length(const Point& a, const Point& b) {           // 友元成员函数定义
        int dx = a._x - b._x;      // 能访问 Point 私有成员
        int dy = a._y - b._y;
        return std::sqrt(dx * dx + dy * dy);
    }
};

double distance(const Point& a, const Point& b) {             // 友元普通函数
    int dx = a._x - b._x;          // 能访问 Point 私有成员
    int dy = a._y - b._y;
    return std::sqrt(dx * dx + dy * dy);
}

int main() {
    Point p1(0, 0), p2(3, 4);
    std::cout << "distance = " << distance(p1, p2) << '\n';   // 5
    Line line;
    std::cout << "length   = " << line.length(p1, p2) << '\n'; // 5
    return 0;
}
```

（注意：友元成员函数需要在 Line 定义完整之后再定义 Point 内的 friend 声明才能编译，实际工程中把 Point 的 friend 声明放在 Line 定义之后，或用"先声明 Line 再写 Point，最后定义 Line 成员函数"的组织方式。上面示例为讲解合并，实际需调整声明顺序。）

**通俗解释**：`friend` 像"给这两个特定的外部函数发了把钥匙"，让它们能直接看 Point 的"坐标（私有成员）"。

---

### 5. 编写一个 const 成员函数 print，尝试修改成员变量观察编译错误；定义 const 对象并调用 print。

**原题**：编写一个 const 成员函数 print，尝试修改成员变量观察编译错误；定义 const 对象并调用 print。

```cpp
#include <iostream>

class Point {
    int _x;
    mutable int _accessCount = 0;   // mutable：const 函数里也允许改
public:
    Point(int x = 0) : _x(x) {}

    void print() const {            // const 成员函数
        std::cout << "x=" << _x << ", 被访问 " << ++_accessCount << " 次\n";
        // _x = 10;                 // 编译错误：const 成员函数不能修改普通成员
    }

    void setX(int x) { _x = x; }    // 非 const 版本
};

int main() {
    const Point cp(5);              // const 对象
    cp.print();                     // 合法：const 对象调用 const 成员函数
    // cp.setX(9);                  // 编译错误：const 对象不能调用非 const 函数

    Point np(1);
    np.print();                     // 非 const 对象，两个版本都有时会优先非 const
    np.setX(100);                   // 合法
    np.print();
    return 0;
}
```

演示要点：取消 `_x = 10;` 的注释会得到编译错误（assignment of member in read-only object）；`mutable` 成员允许 const 函数修改（如访问计数）。

---

# 第四部分：运算符重载、类型转换、嵌套类与单例

## 一、问答题

### 1. 运算符重载的规则（不能改变优先级、操作数个数、不能臆造新运算符）？哪些运算符不能重载（.、::、sizeof、?: 等）？

**原题**：运算符重载的规则（不能改变优先级、操作数个数、不能臆造新运算符）？哪些运算符不能重载（.、::、sizeof、?: 等）？

**答案**：

**重载规则**：
1. **不能改变优先级和结合性**：重载只是给已有运算符赋予新的语义，`+` 的优先级、结合方向不变；
2. **不能改变操作数个数**：一元 `++` 还是一元，二元 `+` 还是二元；
3. **不能臆造新运算符**：只能重载 C++ 已有的运算符，不能发明 `**`、`<>` 之类；
4. **至少有一个操作数是类类型/枚举类型**（不能重载成纯内建类型运算，如"给 int+int 换行为"是非法的）；
5. 部分运算符要求**必须是成员函数**（`= [] () ->` 和类型转换运算符）；部分**最好/必须是非成员**（`<<` `>>`）；`::` `.*` `.` `?:` 等根本不能重载；
6. 不能改变"短路求值"等内建语义（`&&` `||` `,` 重载后**不再短路**——这是重载它们要小心的点，也因此规范不推荐重载 `&&` `||` `,`）；
7. 重载不改变运算对象的求值顺序等语言特性。

**不能重载的运算符**：`.`（成员访问）、`::`（作用域限定）、`.*`（成员指针访问）、`?:`（三目）、`sizeof`（以及 C++ 里的 `typeid`、`alignof`、`noexcept` 运算符、`#` `##` 预处理运算符）。原因：这些运算符的操作数/语义与"某个对象"绑定太紧密或由语言核心机制决定，重载会破坏语言基础语义（如 `.` 若可重载会破坏"访问真实成员"的确定性，`?:` 的惰性求值难以模拟等）。

**通俗解释**：运算符重载 = "给现成的符号重新定义用法"，但只能"换菜谱"，不能改"上菜顺序（优先级/结合性）"、不能改"几人份（操作数个数）"、也不能发明新菜名。而 `.`、`::` 这类是"语法标点"，属于语言骨骼，不能动。

---

### 2. 重载 + 运算符时，友元函数和成员函数形式分别怎么写？哪种更合适？为什么？

**原题**：重载 + 运算符时，友元函数和成员函数形式分别怎么写？哪种更合适？为什么？

**答案**：

以 `Complex`（复数）为例：

**成员函数形式**：
```cpp
Complex operator+(const Complex& rhs) const {
    return Complex(_real + rhs._real, _imag + rhs._imag);
}
// 使用：c1 + c2 → c1.operator+(c2)；但 3 + c2 非法（左侧不是 Complex）
```

**友元/非成员函数形式**：
```cpp
friend Complex operator+(const Complex& a, const Complex& b) {
    return Complex(a._real + b._real, a._imag + b._imag);
}
// 使用：c1 + c2、3 + c2（若 Complex 有非 explicit 单参构造，3 可隐式转换）、c2 + 3 都可以
```

**哪种更合适？——友元（非成员）形式更合适**，原因：
1. **支持对称性（交换律场景）**：`c + 3`（成员版也能处理，`c.operator+(3)` 右侧可隐式转换）没问题，但 `3 + c` 成员版**不行**——因为 3 不是 Complex 对象、没有成员函数可调用；非成员版则两个操作数都能参与隐式转换，`3 + c` 也成立；
2. **不破坏封装**：非成员函数若不访问私有成员就不必是 friend（用公有 getter 即可），比成员函数更"外化"，符合 CppCoreGuidelines"优先非成员非友元函数"的原则（减少对私有实现的依赖、便于扩展，如对既有类型加运算符）；
3. 成员版能访问私有成员方便，但把二元对称运算符做成成员会破坏左右操作数的对称性。

**结论**：**对称二元运算符（+ - * / == != 等）推荐非成员（必要时友元）实现**；**需要修改左操作数本身的复合运算符（+= -=）和一元/赋值类用成员函数**（见下题）。

**通俗解释**：`c1 + c2` 像"甲和乙握手"，谁在左谁在右都行；但"3 + c"成员版规定"必须左边那个是自己人（对象）才能伸手"，3 不是对象就握不了。非成员版让两边的身份都能灵活换算（隐式转换），更公平。

---

### 3. 重载 += 运算符时，为什么通常采用成员函数形式？返回值是什么类型？

**原题**：重载 += 运算符时，为什么通常采用成员函数形式？返回值是什么类型？

**答案**：

**为什么用成员函数**：
- `+=` 会**修改左操作数本身**（`c1 += c2` 要改 c1），而"被修改的对象"正是成员函数所依附的 `this` 对象，语义天然契合；
- 左操作数**必须是本类对象**（复合赋值不允许把内建类型放左边做隐式转换），所以不存在"3 += c"这种对称性问题，用成员函数最自然；
- 与非成员实现相比，成员实现无需额外 friend 声明、代码更紧凑。

**返回值类型**：**`Complex&`（本类引用）**，返回 `*this`。原因：
1. 支持链式：`(a += b) += c;`；
2. 语义上 `a += b` 的结果就是"更新后的 a 本身"，返回引用不产生多余拷贝；
3. 与内建 `+=` 语义（`a += b` 等价于 `a = a + b`，结果是 a 的左值）保持一致。

**标准写法**：
```cpp
Complex& operator+=(const Complex& rhs) {
    _real += rhs._real;
    _imag += rhs._imag;
    return *this;
}
```

**现代规范**：复合赋值（`+= -= *= /=`）用成员函数并返回 `T&`；二元运算符（`+`）可以用"成员 += "的组合在非成员里实现（`Complex operator+(Complex a, const Complex& b){ return a += b; }`），减少代码重复。

**通俗解释**：`+=` 是"给自己身上加东西"，当然得是自己伸手（成员函数）；加完还把自己（引用）交出去，方便连环加。

---

### 4. 前置 ++ 和后置 ++ 重载如何区分（后置加 int 占位参数）？返回值有什么不同？

**原题**：前置 ++ 和后置 ++ 重载如何区分（后置加 int 占位参数）？返回值有什么不同？

**答案**：

**如何区分**：用**哑元参数 `int`** 区分。前置 `++` 无参，后置 `++` 带一个 `int` 占位参数（编译期只用来区分重载，无实际意义）：
```cpp
Complex& operator++();        // 前置 ++c
Complex   operator++(int);    // 后置 c++（int 是占位）
```

**返回值不同**：
- **前置 `++`**：先自增，**返回自增后的对象本身（引用 `T&`）**——返回 `*this`，无拷贝、高效；语义 `++c` 结果是"已加完的 c"。
- **后置 `++`**：先返回**自增前的旧值副本**，再自增——必须**按值返回一个临时对象**（保存旧状态），不能返回引用（不能返回局部临时量的引用）。语义 `c++` 结果是"加之前的 c"。有拷贝/移动开销（对类类型，后置更贵）。

**标准实现**：
```cpp
Complex& operator++() {           // 前置
    ++_real;
    return *this;
}
Complex operator++(int) {         // 后置：基于前置实现
    Complex tmp(*this);           // 保存旧值
    ++(*this);                    // 复用前置逻辑
    return tmp;                   // 返回旧值
}
```

**补充**：`operator--` 同理；重载 `operator++` 时通常应同时实现前置和后置两个版本（CppCoreGuidelines 建议）。内置类型对 `++` 有明确语义，重载后置时注意"拷贝旧值"的开销。

**通俗解释**：前置 = "先迈步再说 '到哪了'"，后置 = "先报 '原来的位置'，再迈步"。后置必须"拍张照（拷贝旧值）"再前进，所以照片（临时对象）得拿着走，代价更高。那个 `int` 占位参数纯粹是"给两个同名函数分编号用"的。

---

### 5. 重载 [] 运算符需要注意什么（返回引用、边界检查）？

**原题**：重载 [] 运算符需要注意什么（返回引用、边界检查）？

**答案**：

**注意要点**：
1. **必须返回引用**：`T& operator[](size_t i)` 才能支持 `obj[i] = value;` 的**写访问**（返回引用让"左值语义"成立）；若只返回 `T`（值），则只能读不能写；
2. **一般提供 const 版本**：`const T& operator[](size_t i) const` 用于 const 对象/只读访问（返回 const 引用，防修改）；
3. **必须是非静态成员函数**（`[]` 要求成员函数形式）；
4. **边界检查**：C++ 内建 `[]` 与 `std::vector::operator[]` 默认**不做边界检查**（越界是未定义行为）；如果你自己实现，建议决定是否检查——安全实现可在越界时抛 `std::out_of_range`（类似 `std::string::at()`），或返回哨兵；`operator[]` 是否检查取决于设计（做检查有开销）；
5. **返回引用时的生命周期**：要确保返回的引用在操作后仍然有效（不要返回指向临时对象的引用）；
6. 注意 `operator[]` 不能用来区分"读/写"（同一个函数既用于读也用于写），返回引用即可；若要"写时检查"可配合代理对象（proxy）做惰性检查。

**标准写法**：
```cpp
class CharArray {
    char* _data; size_t _size;
public:
    char& operator[](size_t i) { return _data[i]; }              // 可写
    const char& operator[](size_t i) const { return _data[i]; }  // 只读
    char at(size_t i) const {                                    // 带边界检查
        if (i >= _size) throw std::out_of_range("index");
        return _data[i];
    }
};
```

**通俗解释**：`[]` 就像"数组的快递柜编号"。要往里放东西（写），必须给你"柜门钥匙（引用）"；只读时就给"隔着玻璃看（const 引用）"。内建 `[]` 越界不报警（自己心里要有数），`at()` 像装了"报警器（越界抛异常）"。

---

### 6. 重载输出流 operator<< 为什么必须是非成员函数？参数和返回值如何设计？

**原题**：重载输出流 operator<< 为什么必须是非成员函数？参数和返回值如何设计？

**答案**：

**为什么必须是非成员函数**：
- `cout << obj` 的写法等价于 `operator<<(cout, obj)`，**左操作数是 `std::ostream&`（cout）**；
- 若做成成员函数，等价于 `cout.operator<<(obj)`，就需要给 `std::ostream` 添加成员函数——但我们**不能修改标准库类**；
- 若在本类里做成员 `void operator<<(std::ostream&)`，则调用形式变成 `obj << cout`（对象在左），不符合习惯；
- 所以 `operator<<` 必须是非成员函数（通常设为类的 **friend**，以便访问私有成员）。

**参数与返回值设计**：
```cpp
friend std::ostream& operator<<(std::ostream& os, const Point& p) {
    os << '(' << p._x << ", " << p._y << ')';
    return os;                       // 返回 os 引用，支持链式 cout << a << b;
}
```
- 第一个参数：`std::ostream& os`（输出流，非 const 引用）；
- 第二个参数：`const T& obj`（要输出的对象，const 引用：只读、避免拷贝）；
- 返回值：`std::ostream&`，**返回传入的 os 本身**，实现**链式输出** `cout << a << b << c`（因为每次 `<<` 的结果又作为下一个 `<<` 的左操作数）。
- 对应的 `operator>>`（输入）同理：`friend std::istream& operator>>(std::istream& is, T& obj)`，obj 为**非 const 引用**（要写入）。

**通俗解释**：`cout << obj` 语法上"cout 在左边"，而 cout 是标准库的东西、不能给它加函数，所以只能写成"站在外面的函数，左手接 cout、右手接 obj"。返回 os 是为了"左手接完还能继续往左传"——像传送带，每次输出完把传送带还给下一个 `<<`。

---

### 7. 什么是类型转换函数（operator 目标类型()）？它的特征是什么？隐式转换与类型转换函数的优先级如何（赋值运算符 > 类型转换 > 构造函数转换）？

**原题**：什么是类型转换函数（operator 目标类型()）？它的特征是什么？隐式转换与类型转换函数的优先级如何（赋值运算符 > 类型转换 > 构造函数转换）？

**答案**：

**类型转换函数（conversion operator）**：在类中定义 `operator 目标类型() const`，定义"把本类对象**隐式**转换成其他类型"的规则。例如：
```cpp
class Fraction {
    int _num, _den;
public:
    operator double() const { return double(_num) / _den; }  // Fraction → double
};
Fraction f(1, 4);
double d = f;   // 隐式转换，d = 0.25
```

**特征**：
1. 形式 `operator 目标类型() const`：**没有返回类型声明**（目标类型写在 operator 后面就是"返回类型"）、**无参数**；
2. 只能定义**成成员函数**；
3. 通常 `const`（转换是只读操作）；
4. 不能定义成转换到数组/函数类型（可以转指针/引用）；
5. 会被编译器在**隐式转换**需要时自动调用（函数传参、赋值、表达式等）。

**隐式转换与类型转换函数的优先级（题面框架）**：
- 题面给出的次序是：**赋值运算符 > 类型转换函数 > 构造函数转换**——这个表述要**谨慎理解**：它指的是"当同时存在多条隐式转换路径时，优先选择哪条"的一种经验总结，**C++ 标准中并没有这样一条严格的、全局的优先级规则**。
- 实际情况：重载决议/隐式转换序列的"好坏"由**用户定义转换序列（UDC）**的步数决定——标准只允许"最多一次用户定义转换"，当多条路径都能到达目标类型时，会用候选集 + 转换序列排序（排序规则：标准转换序列 > 用户定义转换序列等），**多条 UDC 并存会优先选"更匹配的转换序列"，仍歧义则报错**。
- 经验上：若同时存在"拷贝赋值运算符可直接匹配"、"类型转换函数"、"转换构造函数"三种路径，直接匹配（赋值运算符）优先；但类型转换函数与转换构造函数的相对优先在标准里**不是绝对的**，很可能产生**二义性**。所以更准确的说法是：**不要依赖"优先级"猜测，应显式控制（如给转换函数/构造函数加 `explicit`）避免二义性**。
- C++11 起给类型转换函数加 **`explicit`** 可禁止隐式转换，只允许显式 `static_cast<T>(x)`——这已成为现代 C++ 推荐做法（CppCoreGuidelines：**尽量避免非 explicit 的隐式转换运算符**，只保留 `explicit operator bool()` 这类安全用法）。

**通俗解释**：类型转换函数 = "给对象写了一张'当别的类型用'的说明书"。比如分数对象自带"当 double 用"的用法。`explicit` 相当于"说明书上写'必须明说要用才给用'"，防止编译器偷偷把对象变成别的类型造成意外。

---

### 8. 嵌套类（内部类）的定义及其访问外部类成员的权限（内部类默认是外部类的友元）。

**原题**：嵌套类（内部类）的定义及其访问外部类成员的权限（内部类默认是外部类的友元）。

**答案**：

**嵌套类（nested class）**：定义在另一个类内部的类。
```cpp
class Outer {
    int _data;
public:
    class Inner {                  // 嵌套类
    public:
        void f(Outer& o) { o._data = 1; }   // 可访问 Outer 私有成员
    };
};
```

**访问权限（关键知识点）**：
1. **内部类默认是外部类的友元**：内部类的成员函数可以**访问外部类的私有/protected 成员**（通过外部类对象/指针/引用），无需额外 friend 声明；
2. **反之不成立**：外部类**不是**内部类的自动友元——外部类不能访问内部类的私有成员（除非内部类显式声明外部类为友元）；
3. **作用域**：内部类处于外部类的作用域内，对外可见性受外部类访问修饰符影响（`public` 嵌套类可通过 `Outer::Inner` 使用）；内部类不影响外部类对象大小（嵌套类只是命名作用域），不占外部类实例空间；
4. 内部类**没有**外部类对象的 this（两者相互独立），访问外部成员需持有外部类对象引用/指针；
5. 从 C++11 起，内部类能访问外部类成员也**不需要**外部类对象作为"外层存在"（旧标准曾是限制，C++11 已放开）。

**典型用途**：实现细节类（如容器的迭代器、单例的自动释放辅助类）、与外部强耦合的小工具类，既能逻辑分组又能借用外部类的私有访问权。

**通俗解释**：嵌套类像"公司内部的子公司"——子公司（内部类）的人能进母公司（外部类）的保险柜（私有成员），因为"默认是母公司的自己人"；但母公司的人不能随便翻子公司的保险柜（反之不成立）。子公司有自己独立的名片（作用域），只是登记在母公司名下。

---

### 9. 单例模式的目的？懒汉式与饿汉式的区别？如何保证单例对象自动释放（利用嵌套类 + 静态对象、atexit、pthread_once）？

**原题**：单例模式的目的？懒汉式与饿汉式的区别？如何保证单例对象自动释放（利用嵌套类 + 静态对象、atexit、pthread_once）？

**答案**：

**单例模式目的**：保证一个类**全局只有一个实例**，并提供一个**全局访问点**（通常是静态成员函数 `getInstance()`）。适用场景：配置管理器、日志系统、线程池、设备管理器、音视频引擎（播放器内核/编解码上下文）等"全局唯一资源"。

**懒汉式 vs 饿汉式**：

| 维度     | 饿汉式（Eager）                                  | 懒汉式（Lazy）                         |
| -------- | ------------------------------------------------ | -------------------------------------- |
| 创建时机 | 程序**启动时**（静态成员在 main 前初始化）就创建 | **第一次调用 getInstance()** 时才创建  |
| 线程安全 | 天然安全（main 前单线程完成初始化）              | **不安全**，多线程并发首次访问需要同步 |
| 启动开销 | 启动即分配资源（即使没用上）                     | 用时才建，省启动开销                   |
| 适用     | 单例必然会被用、启动耗时不敏感                   | 单例可能用不到、启动要快               |

**实现方式与自动释放**：
1. **饿汉式 + 嵌套类 + 静态对象自动释放**：
```cpp
class Singleton {
    static Singleton* _p;
    class GC {                       // 嵌套辅助类
    public:
        ~GC() { delete Singleton::_p; _p = nullptr; }
    };
    static GC _gc;                   // 静态 GC 对象，程序结束时析构 → 删除单例
public:
    static Singleton* instance() { return _p; }
};
Singleton* Singleton::_p = new Singleton;   // 饿汉：main 前创建
Singleton::GC Singleton::_gc;               // 保证 GC 存在，结束自动释放
```
原理：静态局部/全局对象在程序结束时按构造逆序自动析构，嵌套类 GC 的析构里 `delete` 单例，实现"自动释放"。
2. **atexit**：注册 `atexit(cleanup)`，程序退出时调用 cleanup 释放单例。
3. **pthread_once**（懒汉 + 线程安全）：
```cpp
class Singleton {
    static Singleton* _p;
    static pthread_once_t _once;
    static void init() { _p = new Singleton; }   // 只执行一次
public:
    static Singleton* instance() {
        pthread_once(&_once, init);
        return _p;
    }
};
Singleton* Singleton::_p = nullptr;
pthread_once_t Singleton::_once = PTHREAD_ONCE_INIT;
```

**现代规范（重要纠正，题面偏老）**：
- **Meyer's Singleton（C++11 推荐）**：用**函数局部静态对象**，C++11 起标准保证其初始化是**线程安全**的（magic statics）：
```cpp
class Singleton {
    Singleton() {}
public:
    static Singleton& instance() {
        static Singleton s;      // 首次调用时创建，线程安全，程序结束时自动析构
        return s;
    }
};
```
无需手写 pthread_once/双检锁/嵌套 GC。**这是现代 C++（11/14/17/20）最推荐的单例写法**——简洁、线程安全、自动释放。
- 线程安全替代：`std::call_once` + `std::once_flag`（C++11）也可以替代 pthread_once（跨平台更好）。
- 注意事项：单例不要有循环依赖（其析构可能访问已销毁的静态对象）；在音视频/嵌入式多线程场景要特别关注初始化时序。

**通俗解释**：饿汉式像"公司一开张就安排保安上岗（不管今天有没有人偷东西）"；懒汉式像"第一个访客来才叫保安（没人来就不花钱）"。现代做法（Meyer's）像"保安值班室门口的感应灯——第一次有人走近才亮，而且灯本身有自动断电保护（线程安全+自动析构）"，不用自己操心。

---

## 二、编程题

### 1. 实现一个复数类 Complex，重载 +、+=、前置++、后置++、<< 和 >> 运算符。

**原题**：实现一个复数类 Complex，重载 +、+=、前置++、后置++、<< 和 >> 运算符。

```cpp
#include <iostream>

class Complex {
    double _r, _i;
public:
    Complex(double r = 0, double i = 0) : _r(r), _i(i) {}

    Complex operator+(const Complex& o) const {          // +
        return Complex(_r + o._r, _i + o._i);
    }
    Complex& operator+=(const Complex& o) {              // +=
        _r += o._r; _i += o._i;
        return *this;
    }
    Complex& operator++() {                              // 前置++
        ++_r;
        return *this;
    }
    Complex operator++(int) {                            // 后置++
        Complex tmp(*this);
        ++(*this);
        return tmp;
    }

    friend std::ostream& operator<<(std::ostream& os, const Complex& c) {
        os << c._r << (c._i >= 0 ? "+" : "") << c._i << "i";
        return os;
    }
    friend std::istream& operator>>(std::istream& is, Complex& c) {
        is >> c._r >> c._i;      // 输入格式：实部 虚部
        return is;
    }
};

int main() {
    Complex a(1, 2), b(3, -4);
    Complex c = a + b;                 // +
    std::cout << "a+b = " << c << '\n';

    a += b;                            // +=
    std::cout << "a+=b = " << a << '\n';

    std::cout << "++a = " << ++a << '\n';   // 前置
    std::cout << "a++ 返回旧值 = " << a++ << ", 之后 a = " << a << '\n'; // 后置

    std::cout << "请输入复数的实部和虚部：";
    std::cin >> c;
    std::cout << "读入 = " << c << '\n';
    return 0;
}
```

---

### 2. 实现 CharArray 类，重载 []，实现读写访问，并提供 const 版本防止修改。

**原题**：实现 CharArray 类，重载 []，实现读写访问，并提供 const 版本防止修改。

```cpp
#include <iostream>
#include <cstring>

class CharArray {
    char* _data;
    size_t _size;
public:
    CharArray(size_t n) : _size(n) {
        _data = new char[n];
        std::memset(_data, 0, n);
    }
    ~CharArray() { delete[] _data; }

    char& operator[](size_t i) { return _data[i]; }                // 可写
    const char& operator[](size_t i) const { return _data[i]; }    // 只读
    size_t size() const { return _size; }
};

int main() {
    CharArray arr(5);
    arr[0] = 'H'; arr[1] = 'i';          // 写：走非 const 版本
    std::cout << arr[0] << arr[1] << '\n';

    const CharArray& ca = arr;
    std::cout << "只读访问 ca[0] = " << ca[0] << '\n';
    // ca[0] = 'x';   // 编译错误：const 版本返回 const char&，不能写
    return 0;
}
```

要点：非 const 版本返回 `char&` 支持写；const 版本返回 `const char&`，const 对象/const 引用调用时自动走 const 版，写操作被编译期拦截。

---

### 3. 实现 Point 到 int 的类型转换函数（返回距离）和 Point 到 Complex 的转换，测试隐式转换和优先级。

**原题**：实现 Point 到 int 的类型转换函数（返回距离）和 Point 到 Complex 的转换，测试隐式转换和优先级。

```cpp
#include <iostream>
#include <cmath>

class Complex {                        // 简化复数
    double _r, _i;
public:
    Complex(double r = 0, double i = 0) : _r(r), _i(i) {}
    void show() const { std::cout << '(' << _r << ", " << _i << ')'; }
};

class Point {
    int _x, _y;
public:
    Point(int x = 0, int y = 0) : _x(x), _y(y) {}

    // 类型转换函数1：Point → int（返回到原点的距离，取整）
    operator int() const { return static_cast<int>(std::sqrt(_x*_x + _y*_y)); }
    // 类型转换函数2：Point → Complex（实部=x, 虚部=y）
    operator Complex() const { return Complex(_x, _y); }
};

int main() {
    Point p(3, 4);

    int d = p;                          // 隐式调用 operator int → 5
    std::cout << "p → int = " << d << '\n';

    Complex c = p;                      // 隐式调用 operator Complex
    std::cout << "p → Complex = ";
    c.show();
    std::cout << '\n';

    // 显式转换也可用 static_cast
    int d2 = static_cast<int>(p);
    std::cout << "static_cast<int> = " << d2 << '\n';
    return 0;
}
```

**关于"优先级"的说明（与题面相衔接）**：这里没有同时定义"赋值运算符"，也没有给 Point 加"转换构造函数"（从别的类型构造），所以转换路径唯一、无冲突。若同时有"从 int 构造 Complex 的构造函数"、"Point 的 operator Complex()"，隐式转换会有多条 UDC 路径，可能产生二义性。**现代建议**：给类型转换函数加 `explicit`（除非 `operator bool` 这类安全用法），从根源避免隐式转换带来的歧义。

---

### 4. 定义 Line 类，内部嵌套 Point 类，实现 Line 的 print 方法，并重载 operator<< 输出 Line 对象（需解决嵌套类访问）。

**原题**：定义 Line 类，内部嵌套 Point 类，实现 Line 的 print 方法，并重载 operator<< 输出 Line 对象（需解决嵌套类访问）。

```cpp
#include <iostream>

class Line {
public:
    class Point {                          // 嵌套类
        int _x, _y;
    public:
        Point(int x = 0, int y = 0) : _x(x), _y(y) {}
        int x() const { return _x; }
        int y() const { return _y; }
    };

    Line(const Point& a, const Point& b) : _a(a), _b(b) {}

    void print() const { std::cout << *this; }   // print 复用 operator<<

    friend std::ostream& operator<<(std::ostream& os, const Line& l) {
        os << '[' << l._a.x() << ',' << l._a.y() << "] -> ["
           << l._b.x() << ',' << l._b.y() << ']';
        return os;
    }
private:
    Point _a, _b;
};

int main() {
    Line::Point p1(0, 0), p2(3, 4);        // 通过 Line::Point 访问嵌套类
    Line line(p1, p2);
    line.print();
    std::cout << '\n';
    std::cout << line << '\n';             // 重载的 <<
    return 0;
}
```

要点：嵌套类用 `Line::Point` 访问；`operator<<` 作为友元访问 Line 私有成员，再通过 Point 的公有访问器读坐标（这里为避免友元传递问题用 getter，也可把 `operator<<` 同时声明为 Point 的友元——体现"友元不继承、需各自授权"）。

---

### 5. 实现一个单例 Singleton 类，使用静态局部对象（Meyer's Singleton）和堆对象（带 pthread_once）两种方式，验证自动释放。

**原题**：实现一个单例 Singleton 类，使用静态局部对象（Meyer's Singleton）和堆对象（带 pthread_once）两种方式，验证自动释放。

```cpp
#include <iostream>
#include <pthread.h>

// 方式一：Meyer's Singleton（C++11 推荐，线程安全 + 自动析构）
class SingletonA {
    SingletonA()  { std::cout << "SingletonA 构造\n"; }
    ~SingletonA() { std::cout << "SingletonA 析构(自动)\n"; }
public:
    SingletonA(const SingletonA&) = delete;
    SingletonA& operator=(const SingletonA&) = delete;
    static SingletonA& instance() {
        static SingletonA s;      // 首次调用创建；C++11 保证线程安全；程序结束自动析构
        return s;
    }
};

// 方式二：懒汉式 + pthread_once（堆对象）
class SingletonB {
    static SingletonB* _p;
    static pthread_once_t _once;
    SingletonB()  { std::cout << "SingletonB 构造\n"; }
    ~SingletonB() { std::cout << "SingletonB 析构(pthread_once + 手动释放)\n"; }
    static void init() { _p = new SingletonB; }   // 只执行一次
public:
    SingletonB(const SingletonB&) = delete;
    SingletonB& operator=(const SingletonB&) = delete;
    static SingletonB* instance() {
        pthread_once(&_once, init);
        return _p;
    }
    static void destroy() { delete _p; _p = nullptr; }  // 需要手动释放（或 atexit 注册）
};
SingletonB* SingletonB::_p = nullptr;
pthread_once_t SingletonB::_once = PTHREAD_ONCE_INIT;

int main() {
    SingletonA& a1 = SingletonA::instance();   // 构造
    SingletonA& a2 = SingletonA::instance();   // 不再构造（同一实例）
    std::cout << "a1 == a2 ? " << (&a1 == &a2) << '\n';

    SingletonB* b1 = SingletonB::instance();
    SingletonB* b2 = SingletonB::instance();
    std::cout << "b1 == b2 ? " << (b1 == b2) << '\n';
    SingletonB::destroy();                     // 手动释放

    std::cout << "main 结束\n";
    // SingletonA 的静态局部对象在此之后自动析构
    return 0;
}
```

**现代规范提示**：方式一（Meyer's）无需 pthread_once、无需手动释放、线程安全，是 C++11+ 的标准答案；方式二体现"pthread_once 保证懒汉单线程安全 + 需要配套释放手段（手动/atexit/嵌套 GC）"的经典思路。编译：`g++ -std=c++17 -pthread demo.cpp`。

---

# 第五部分：std::string 与 std::vector

## 一、问答题

### 1. C 风格字符串（char*）与 C++ std::string 的主要区别（安全性、内存管理、操作便利性）。

**原题**：C 风格字符串（char*）与 C++ std::string 的主要区别（安全性、内存管理、操作便利性）。

**答案**：

| 维度            | C 风格字符串 `char*` / `char[]`                              | C++ `std::string`                                            |
| --------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 本质            | 字符数组 + `'\0'` 结尾约定                                   | 封装字符串的**类对象**（内部管理 char 缓冲区）               |
| 安全性          | 低：越界、忘记 `\0`、`strcpy` 溢出等全靠程序员自觉           | 高：自动管理大小、越界可用 `at()` 检查、`c_str()` 保证 `\0` 结尾 |
| 内存管理        | 手动：`malloc/strcpy`、栈数组大小固定；易泄漏/溢出           | 自动：构造/析构自动分配释放，随用随扩                        |
| 操作便利性      | 需函数库：`strcpy/strcat/strcmp/strlen/strtok...`，拼接要先算长度（易错） | 运算符重载：`+`、`+=`、`==`、`<`、`[]`，查找/替换/子串等成员函数丰富 |
| 边界/越界       | `[]` 越界静默，未定义行为                                    | `[]` 同内建（不检查）；`at()` 抛 `std::out_of_range`         |
| 空字符串        | 常靠 `""`/NULL 约定                                          | 语义明确（`empty()`、默认构造即空）                          |
| 性能            | 轻量、可直接与 C 库互操作                                    | 略多一层封装（现代实现有 SSO 小字符串优化，多数场景开销可忽略） |
| 与 C 接口互操作 | 原生                                                         | 用 `c_str()` 拿 `const char*`（C++17 还有 `data()`）         |

**现代规范**：除非要与 C 库接口/嵌入式极简场景互操作，**一律用 `std::string`**（CppCoreGuidelines 明确"优先 string 而非 char*"）。C++17 起 `string_view` 可作为"只读字符串视图"参数类型（避免拷贝、不持有所有权），适合只读参数。

**通俗解释**：`char*` 字符串像"自己搬砖盖的仓库"——仓库大小、隔间、防火（\0）都要自己管，稍不注意就烧穿（越界）；`std::string` 像"专业物业管理的公寓"——大小自动调整、有门禁（边界检查）、搬家（内存管理）全自动。

---

### 2. std::string 常用的构造方式（无参、C字符串、拷贝、子串、重复字符、迭代器范围）。

**原题**：std::string 常用的构造方式（无参、C字符串、拷贝、子串、重复字符、迭代器范围）。

**答案**：

```cpp
#include <string>
using namespace std;

string s1;                    // 无参：空串
string s2("hello");           // C 字符串
string s3("hello", 3);        // C 字符串前 3 个字符 → "hel"
string s4(s2);                // 拷贝构造
string s5(s2, 1, 3);          // 子串：从下标 1 取 3 个 → "ell"
string s6(5, 'a');            // 重复字符 → "aaaaa"
string s7({'h','i'});         // 初始化列表 → "hi"
string s8(s2.begin(), s2.begin()+2);  // 迭代器范围 → "he"
string s9 = s2 + "!";         // 拼接构造（移动语义）
string s10(std::move(s2));    // 移动构造（s2 被掏空）
```

补充：`std::string` 还有 `substr(pos, len)`（子串成员函数）、`assign` 等；C++17 后 `std::string_view` 可另作视图。另外 string 的底层现代实现普遍采用 **SSO（Small String Optimization）**：小字符串直接存在对象内部，不堆分配。

**通俗解释**：构造方式 = "用不同素材搭字符串的多种方法"：空手（无参）、拿现成字（C 串）、裁一截（子串）、复印（拷贝）、批量打点（重复）、从半成品里挑（迭代器范围）。

---

### 3. std::string 的遍历方式（下标、范围 for、迭代器）。

**原题**：std::string 的遍历方式（下标、范围 for、迭代器）。

**答案**：

```cpp
#include <iostream>
#include <string>

int main() {
    std::string s = "hello";

    // 1) 下标
    for (std::size_t i = 0; i < s.size(); ++i)
        std::cout << s[i];
    std::cout << '\n';

    // 2) 范围 for（C++11，推荐）
    for (char c : s)                  // 按值：只读
        std::cout << c;
    std::cout << '\n';
    for (char& c : s)                 // 按引用：可修改
        c = std::toupper(c);
    std::cout << s << '\n';

    // 3) 迭代器
    for (auto it = s.begin(); it != s.end(); ++it)
        std::cout << *it;
    std::cout << '\n';
    // 反向迭代器
    for (auto it = s.rbegin(); it != s.rend(); ++it)
        std::cout << *it;             // 逆序
    std::cout << '\n';
    return 0;
}
```

比较：下标最直观但需手动控制边界；**范围 for 最简洁安全**（C++11+ 首选）；迭代器最通用（也适用于其他容器），支持反向/插入删除等操作。

---

### 4. std::vector 的底层结构（三个指针：_start、_finish、_end_of_storage），如何计算 size() 和 capacity()？

**原题**：std::vector 的底层结构（三个指针：_start、_finish、_end_of_storage），如何计算 size() 和 capacity()？

**答案**：

**底层结构**：`std::vector<T>` 内部通常用**三个指针**管理一段连续堆内存（实现相关，但三指针模型最典型）：

```
_start = begin()          指向已分配内存的起始
_finish = end()           指向"最后一个有效元素之后"（size 位置）
_end_of_storage           指向"已分配容量"的末尾
```

```
内存: [ 元素0 | 元素1 | 元素2 | ........... | 空闲 ]
       ^_start      ^_finish              ^_end_of_storage
```

**计算公式**：
- `size() = _finish - _start`（当前元素个数）；
- `capacity() = _end_of_storage - _start`（当前能容纳的元素个数，即已分配内存大小）；
- `empty()` 即 `size() == 0`，`end() == _finish`。

**补充**：`std::vector<bool>` 是特化，按位压缩存储（每元素 1 bit），`operator[]` 返回代理对象而非 `bool&`——这是容易踩坑的点。现代 C++ 建议：真要存 bool 序列且要位压缩，用 `std::bitset`（定长）或容器明确语义。

**通俗解释**：vector 像"装货的连续仓库"，三个指针分别是"仓库门牌（起始）""已放货的终点""仓库墙（容量终点）"。size 是"已放多少箱"，capacity 是"仓库能放多少箱"。

---

### 5. vector 的扩容机制（GCC 2倍，VS 1.5倍），扩容时发生了什么（分配新空间、拷贝/移动元素、释放旧空间）？

**原题**：vector 的扩容机制（GCC 2倍，VS 1.5倍），扩容时发生了什么（分配新空间、拷贝/移动元素、释放旧空间）？

**答案**：

**触发条件**：`push_back` 等操作发现 `size() == capacity()` 时，需要扩容。

**扩容倍数（实现相关）**：
- **libstdc++（GCC）**：常见策略是 **capacity 翻倍（×2）**（`_M_check_len` 里 `size + max(size, n)` 的经典增长）；
- **MSVC STL**：历史上是 **×1.5**（`_Grow_to` 策略），同时小容量时有保底增长；
- **libc++（Clang）**：通常也 ×2；
- 这些都是**实现细节、非标准规定**，不同版本/平台可能变化。现代 MSVC STL 也在演进，不必死记硬背，理解"**指数/超线性增长**以摊还 O(1) 的 push_back 均摊代价"即可。

**扩容时发生了什么**：
1. **分配新内存**：申请一块更大的连续空间（新容量）；
2. **搬移元素**：把旧元素**移动**（C++11 起，若类型可移动；否则拷贝）到新空间——注意：**元素地址全变了**；
3. **释放旧空间**：销毁旧元素、归还旧内存（析构旧对象 + operator delete）；
4. **更新指针**：`_start/_finish/_end_of_storage` 指向新空间。

**关键后果**：扩容后**所有迭代器、指针、引用全部失效**（指向旧内存）。若提前知道元素个数，用 `reserve()` 预分配避免多次扩容（减少搬移开销）。

**均摊复杂度**：每次扩容翻倍 → push_back 的**均摊时间 O(1)**；若每次只 +1（线性扩容），均摊 O(n)，所以标准库都采用指数增长。

**通俗解释**：仓库满了要进货，就"另租一间更大的仓库，把货搬过去，退掉旧仓库"。搬一次货很贵（搬元素），所以聪明做法是"新仓库一次租得大点（翻倍）"，避免每进一箱就搬一次家。翻倍后"平均每进一箱的成本"很低。

---

### 6. vector 的常用操作（push_back、pop_back、reserve、shrink_to_fit、clear）。

**原题**：vector 的常用操作（push_back、pop_back、reserve、shrink_to_fit、clear）。

**答案**：

```cpp
#include <vector>
#include <iostream>

int main() {
    std::vector<int> v;
    v.push_back(10);                    // 尾部追加（可能扩容）
    v.push_back(20);
    std::cout << "size=" << v.size() << " cap=" << v.capacity() << '\n';

    v.reserve(100);                     // 预分配容量≥100（不改变 size，只增不减）
    std::cout << "after reserve cap=" << v.capacity() << '\n';

    v.pop_back();                       // 删除最后一个元素（不缩小容量）
    std::cout << "after pop size=" << v.size() << " cap=" << v.capacity() << '\n';

    v.push_back(30);
    v.shrink_to_fit();                  // 请求把 capacity 缩到 size（非强制，实现相关）
    std::cout << "after shrink size=" << v.size() << " cap=" << v.capacity() << '\n';

    v.clear();                          // 清空元素，size=0（capacity 通常不变）
    std::cout << "after clear size=" << v.size() << " cap=" << v.capacity() << '\n';
    return 0;
}
```

**各操作说明**：
- `push_back(x)`：尾部追加，可能扩容（可能导致迭代器失效）；
- `pop_back()`：移除末尾元素（调用其析构），**不改变 capacity**；
- `reserve(n)`：**只增加** capacity 到 ≥n（`n < capacity` 时无操作），不改变 size、不构造元素；用于预分配避免多次扩容；
- `shrink_to_fit()`：请求把 capacity 缩至接近 size（**非强制**，编译器可忽略；会搬移元素使迭代器失效）；
- `clear()`：移除所有元素（逐个析构），size=0，**capacity 保留**（内存不归还）；
- 其他：`emplace_back`（C++11，原地构造，避免临时对象拷贝/移动，**推荐**）、`insert/erase`（注意迭代器失效）、`at`（越界抛异常）、`front/back`、`data()`（拿底层指针）。

**现代规范**：能用 `emplace_back` 就用；迭代中删除用"erase-remove 惯用法"；数组语义直接用 `std::vector`（C++11 起可用 `std::array` 定长栈数组）。

**通俗解释**：`push_back` 往尾巴放货（满了搬大仓库）；`pop_back` 把最后一箱拿走（仓库还是那么大）；`reserve` 提前把仓库租大（免得搬来搬去）；`shrink_to_fit` 申请"把大仓库换成刚好的小仓库"（不一定成功）；`clear` 把货全清空（仓库保留，下次直接放）。

---

## 二、编程题

### 1. 使用多种方式构造 string 对象，并测试 c_str()、size()、empty()、append、find。

**原题**：使用多种方式构造 string 对象，并测试 c_str()、size()、empty()、append、find。

```cpp
#include <iostream>
#include <string>

int main() {
    std::string s1;                            // 无参
    std::string s2("hello");                   // C 字符串
    std::string s3(s2);                        // 拷贝
    std::string s4(s2, 1, 3);                  // 子串 "ell"
    std::string s5(3, 'x');                    // 重复 "xxx"

    std::cout << "s1.empty() = " << s1.empty() << '\n';
    std::cout << "s2 = " << s2 << ", size = " << s2.size() << '\n';
    std::cout << "s2.c_str() = " << s2.c_str() << '\n';    // 取 C 字符串（const char*）

    s2.append(" world");                       // 追加
    std::cout << "append: " << s2 << '\n';

    s2 += "!";                                 // 也用 +=
    std::cout << "+=: " << s2 << '\n';

    std::string::size_type pos = s2.find("world");
    std::cout << "find(\"world\") = " << pos << '\n';       // 下标 6
    std::cout << "find(\"zzz\") = " << s2.find("zzz") << " (== npos? "
              << (s2.find("zzz") == std::string::npos) << ")\n";
    return 0;
}
```

要点：`find` 找不到返回 `std::string::npos`；`c_str()` 返回以 `\0` 结尾的 `const char*`（C++17 起 `data()` 不再保证 `\0` 但通常也是）。

---

### 2. 遍历 string 三种方式，并演示 at() 与 [] 越界行为的区别。

**原题**：遍历 string 三种方式，并演示 at() 与 [] 越界行为的区别。

```cpp
#include <iostream>
#include <string>
#include <stdexcept>

int main() {
    std::string s = "abc";

    // 三种遍历
    for (std::size_t i = 0; i < s.size(); ++i) std::cout << s[i];
    std::cout << '\n';
    for (char c : s) std::cout << c;
    std::cout << '\n';
    for (auto it = s.begin(); it != s.end(); ++it) std::cout << *it;
    std::cout << '\n';

    // [] 越界：未定义行为，不报错（可能读到垃圾/崩溃）
    std::cout << "s[100] = " << s[100] << " (未定义行为)\n";

    // at() 越界：抛 std::out_of_range
    try {
        std::cout << s.at(100) << '\n';
    } catch (const std::out_of_range& e) {
        std::cout << "at() 抛异常: " << e.what() << '\n';
    }
    return 0;
}
```

要点：`[]` 不检查（快但危险）；`at()` 检查（慢但安全、抛 `std::out_of_range`）。这也是 vector/string 统一的行为差异。

---

### 3. 使用 vector<int> 存储若干整数，观察 push_back 后 size 和 capacity 的变化，验证扩容倍数。

**原题**：使用 vector<int> 存储若干整数，观察 push_back 后 size 和 capacity 的变化，验证扩容倍数。

```cpp
#include <iostream>
#include <vector>

int main() {
    std::vector<int> v;
    std::cout << "初始 size=" << v.size() << " cap=" << v.capacity() << '\n';
    for (int i = 0; i < 20; ++i) {
        v.push_back(i);
        if (i < 6 || (i + 1 == v.capacity()))   // 打印关键点
            std::cout << "push " << i << ": size=" << v.size()
                      << " cap=" << v.capacity() << '\n';
    }
    // 观察 cap 变化序列（libstdc++ 通常 1,2,4,8,16,32... 即 ×2）
    return 0;
}
```

在 GCC/libstdc++ 下 capacity 通常按 1→2→4→8→16→32 翻倍；MSVC 历史实现约 1.5 倍（会看到 1→2→3→4→6→9→13→19...）。**再次强调**：这是实现细节，不必在不同编译器间强求一致。

---

### 4. 创建 vector<string>，用迭代器和范围 for 遍历，并尝试插入和删除。

**原题**：创建 vector<string>，用迭代器和范围 for 遍历，并尝试插入和删除。

```cpp
#include <iostream>
#include <vector>
#include <string>

int main() {
    std::vector<std::string> v{"apple", "banana", "cherry"};

    // 迭代器遍历
    for (auto it = v.begin(); it != v.end(); ++it)
        std::cout << *it << ' ';
    std::cout << '\n';

    // 范围 for
    for (const auto& s : v)
        std::cout << s << ' ';
    std::cout << '\n';

    // 插入
    v.insert(v.begin() + 1, "avocado");       // 在"banana"前插入
    v.push_back("date");
    // 删除
    v.erase(v.begin() + 2);                    // 删除现在的下标2 "banana"

    for (const auto& s : v) std::cout << s << ' ';
    std::cout << '\n';                         // apple avocado cherry date

    // 迭代时删除的安全做法：erase 返回下一个有效迭代器
    for (auto it = v.begin(); it != v.end(); ) {
        if ((*it).size() > 5)
            it = v.erase(it);                  // 删除并更新迭代器
        else
            ++it;
    }
    for (const auto& s : v) std::cout << s << ' ';   // date（? 5字符不删）
    std::cout << '\n';
    return 0;
}
```

要点：`insert/erase` 会导致其后所有迭代器失效，务必使用返回值更新迭代器；删除长度>5 的字符串演示"迭代中删除"惯用法。

---

### 5. 编写程序，读取一个文本文件的所有行到 vector<string>，然后逆序输出。

**原题**：编写程序，读取一个文本文件的所有行到 vector<string>，然后逆序输出。

```cpp
#include <iostream>
#include <fstream>
#include <vector>
#include <string>

int main() {
    std::ifstream in("data.txt");
    if (!in) {
        std::cerr << "无法打开文件\n";
        return 1;
    }
    std::vector<std::string> lines;
    std::string line;
    while (std::getline(in, line))     // 逐行读入
        lines.push_back(line);

    // 逆序输出：反向迭代器
    for (auto it = lines.rbegin(); it != lines.rend(); ++it)
        std::cout << *it << '\n';
    return 0;
}
```

（文件里先写几行文本测试。）要点：`getline` 读整行（不含换行符）；逆序用 `rbegin/rend` 最简洁，也可用下标倒序。**现代规范**：C++20 有 `std::ranges` 更简洁，但 C++17 下反向迭代器即可。

---

# 第六部分：继承

## 一、问答题

### 1. 继承的作用和定义方式。三种继承方式（public、protected、private）对基类成员在派生类中的访问权限有何影响（总结表）？

**原题**：继承的作用和定义方式。三种继承方式（public、protected、private）对基类成员在派生类中的访问权限有何影响（总结表）？

**答案**：

**继承的作用**：实现**代码复用**（复用基类的数据与行为）、建立 **is-a（是一种）** 关系、为**多态**提供基础。定义方式：
```cpp
class Derived : public Base { ... };        // 公有继承
class Derived : protected Base { ... };     // 保护继承
class Derived : private Base { ... };       // 私有继承
```

**三种继承方式对访问权限的影响（总结表）**：

规则：**继承方式决定了基类成员在派生类中的访问级别**——取"基类成员原本的访问级别"与"继承方式"中**更严格**者。

| 基类成员访问级别 | 公有继承 `public` 后在派生类中 | 保护继承 `protected` 后在派生类中 | 私有继承 `private` 后在派生类中 |
| ---------------- | ------------------------------ | --------------------------------- | ------------------------------- |
| public           | **public**                     | protected                         | private                         |
| protected        | **protected**                  | protected                         | private                         |
| private          | 不可访问（仍属于基类私有）     | 不可访问                          | 不可访问                        |

补充说明：
- **private 成员在任何继承方式下都不能被派生类直接访问**（只能通过基类的公有/保护接口间接访问）；
- **公有继承（public）最常用**，表达"is-a"语义，支持基类指针指向派生类（多态基础）；
- **protected/private 继承**表达"has-a（实现复用）"语义，派生类能复用基类实现但对外不暴露 is-a 关系；现代规范建议优先用**组合**而非 private 继承（CppCoreGuidelines）。

**通俗解释**：继承方式像"传给后代的'家产交接规则'"——同样一份家产（成员），公开交接（public）保留原样公开；设限交接（protected）对外只算"内部资产"；秘密交接（private）到了后代手里就变成后代的私房钱（对外彻底隐藏）。而基类里本来就是私房钱（private）的东西，怎么交接后代都摸不到。

---

### 2. 派生类对象的构造和析构顺序（构造：先基类后派生类；析构相反）。如果基类没有默认构造，派生类构造如何调用基类有参构造？

**原题**：派生类对象的构造和析构顺序（构造：先基类后派生类；析构相反）。如果基类没有默认构造，派生类构造如何调用基类有参构造？

**答案**：

**构造顺序**：
1. 先构造**基类子对象**（按基类声明顺序）；
2. 再按声明顺序构造**成员对象**；
3. 最后执行**派生类构造函数体**。
（若有多继承，按基类**声明顺序**逐个构造基类。）

**析构顺序**：**严格逆序**——先执行派生类析构体，再析构成员对象（逆声明序），最后析构基类子对象。原因：派生类对象销毁时，先销毁"更具体（派生）的部分"再销毁"更基础（基类）的部分"，保证派生类析构里还能安全访问基类成员。

**基类没有默认构造时**：派生类构造函数必须在**初始化列表里显式调用基类的有参构造**，否则编译错误：
```cpp
class Base {
public:
    Base(int x) { ... }   // 没有默认构造
};
class Derived : public Base {
public:
    Derived(int a, int b) : Base(a) { ... }   // 初始化列表调用 Base(a)
};
```
规则：若基类**没有可调用的默认构造**，派生类的**每个**构造函数都必须显式在初始化列表里调用基类的某个构造；若不写，编译器默认会尝试调用基类默认构造，找不到就报错。

**通俗解释**：盖楼先打地基（基类）再砌上层（派生类），拆楼相反——先拆上层再拆地基。若"地基图纸（基类）只支持带参数版本"（没有默认构造），盖楼时必须明确说"用几号图纸（显式传参）"，不能指望默认图纸。

---

### 3. 什么是隐藏（同名成员变量/函数）？派生类定义同名函数（即使参数不同）会隐藏基类所有同名函数，如何访问被隐藏的基类成员？

**原题**：什么是隐藏（同名成员变量/函数）？派生类定义同名函数（即使参数不同）会隐藏基类所有同名函数，如何访问被隐藏的基类成员？

**答案**：

**隐藏（name hiding / hide）**：派生类中定义了与基类**同名**的成员（变量或函数），则基类中该**名字**在派生类作用域内被"遮蔽"——在派生类里直接写这个名字，指的是派生类自己的成员。

**关键规则**：派生类只要定义了同名函数，**无论参数列表是否相同、无论是否 virtual**，都会**隐藏基类中所有同名重载**。例如基类有 `void f(int); void f(double);`，派生类只写 `void f();`，则在派生类里 `f(int)`、`f(double)` 都不可直接调用（被隐藏）。

**如何访问被隐藏的基类成员**：
1. **用作用域限定符**：`Base::f(1)`、`this->Base::x`；
2. **用 using 声明把基类重载"引进"派生类作用域**：
```cpp
class Derived : public Base {
    using Base::f;        // 把基类所有 f 重载引入，与派生类 f 共存
public:
    void f();             // 自己的版本
};
```
这样 `d.f(1)` 也能调用基类的 `f(int)`。

**隐藏 vs 覆盖（override）的区别**（这里先对比，多态部分详解）：
- **隐藏**：只要同名即可（不管参数/返回/是否 virtual），作用域内"名字遮蔽"；
- **覆盖**：必须是**虚函数**、**参数和返回类型完全一致**（返回可协变）、派生类重写实现，构成多态。

**通俗解释**：隐藏像"公司里同名同姓的两个人"——在公司（派生类作用域）里喊"小张"，默认是那个新人（派生类成员），老张（基类成员）要喊"部门号+小张"（`Base::`）才能叫到。用 `using Base::f;` 像"给老张也挂了块名牌，喊全名都能找到"。

---

### 4. 多继承的构造顺序（按声明顺序）和析构顺序（逆序）。多继承中成员名冲突如何解决（作用域限定）？

**原题**：多继承的构造顺序（按声明顺序）和析构顺序（逆序）。多继承中成员名冲突如何解决（作用域限定）？

**答案**：

**多继承**：一个类继承多个基类：
```cpp
class D : public B, public C { ... };
```

**构造顺序**：**按基类在声明列表中的书写顺序**依次构造（B 先、C 后），然后构造 D 的成员，最后 D 的构造体。
**析构顺序**：**与构造严格相反**——D 析构体 → D 成员（逆声明序）→ C 析构 → B 析构（逆着基类声明序）。

**成员名冲突如何解决**：当 B、C 都有同名成员（如都有 `_value` 或 `void show()`），在 D 里直接写 `_value` / `show()` 会**产生二义性**（编译错误）。解决方式：
1. **作用域限定符**：`B::_value`、`C::_value`、`B::show()`、`C::show()`，明确从哪个基类取；
2. 在 D 中**重新定义**同名成员（新定义会隐藏基类的），再在实现里用 `B::`/`C::` 分别调用；
3. `using B::show;` 等把某个基类的版本引入。

**多继承的潜在问题**：
- 名字二义性、冗余复制（菱形继承）；
- 布局复杂、行为难以预期。现代规范（CppCoreGuidelines）建议：**尽量用组合/接口继承，避免深层次多继承**；需要"接口多态"时可用多继承多个"纯接口类"。

**通俗解释**：多继承像"同时继承爸爸的房产和妈妈的存款"，但两边都有个叫"小金库"的东西时，你得说清楚"爸爸的小金库 / 妈妈的小金库"（`B::` / `C::`），不然就乱了。盖楼/拆楼也按"先列的图纸先盖、拆时反着来"。

---

### 5. 菱形继承（D ← B, C ← A）带来的问题（存储二义性、冗余）。虚继承如何解决？虚继承后对象内存布局有何变化（虚基类指针）？

**原题**：菱形继承（D ← B, C ← A）带来的问题（存储二义性、冗余）。虚继承如何解决？虚继承后对象内存布局有何变化（虚基类指针）？

**答案**：

**菱形继承结构**：
```
    A
   / \
  B   C
   \ /
    D
```
即 `B : public A`、`C : public A`、`D : public B, public C`。

**带来的问题**：
1. **冗余**：D 中会包含**两份 A 的子对象**（一份来自 B、一份来自 C），浪费内存；
2. **二义性**：访问 `D` 对象的 `A::_a` 时，编译器无法确定取 B 带的那份还是 C 带的那份 → 编译错误；
3. 语义混乱（D 与 A 本应是 is-a 关系，却有两份 A 的数据）。

**虚继承解决**：让 B、C 都**虚继承** A：
```cpp
class B : virtual public A { ... };
class C : virtual public A { ... };
class D : public B, public C { ... };
```
虚继承后：
- D 中**只有一份** A 子对象（共享）→ 解决冗余；
- `D` 直接访问 `A::_a` **不再有二义性**（只有一个 A 子对象）；
- 所有直接/间接继承 A 的类（B、C、D）共享这**唯一的 A 子对象**。

**内存布局变化（虚基类指针）**：
- 普通（非虚）继承时，A 子对象**内嵌**在 B/C 中，位置固定；
- 虚继承后，**共享的 A 子对象被"抬到"对象的末尾**（典型实现），各派生类通过**虚基类指针/偏移量（vbptr 或偏移表）**来定位它——因为布局不再是简单嵌套，需要运行时"间接寻址"找到那份共享的 A；
- 因此 D 对象中通常包含：B 部分、C 部分各自的 vbptr（指向偏移表）+ 唯一的 A 子对象，对象**变复杂、sizeof 变大**；
- 不同编译器实现细节不同（有些用 vbptr 存指针，有些直接存虚基类地址/负偏移），理解"**共享 + 间接定位**"即可。

**通俗解释**：菱形继承像"爸爸和妈妈都从爷爷那继承了祖宅（A）"，结果你的家（D）里有两套一模一样的祖宅（冗余+二义性：你说'去祖宅'到底是哪套？）。虚继承 = "两家协议：祖宅只有一套，共同持有"，但因为是共享的，地址得记在"祖宅共享簿（vbptr）"上才能找到——这就是为什么布局变复杂。

---

### 6. 派生类对象向基类对象/指针/引用的转换（向上转型）是否安全？基类指针指向派生类对象时能访问哪些成员？向下转型（基类→派生类）有哪些风险？dynamic_cast 的作用？

**原题**：派生类对象向基类对象/指针/引用的转换（向上转型）是否安全？基类指针指向派生类对象时能访问哪些成员？向下转型（基类→派生类）有哪些风险？dynamic_cast 的作用？

**答案**：

**向上转型（派生类 → 基类，upcast）**：
- 指针/引用：`Base* pb = &d;`、`Base& rb = d;` —— **安全**，派生类是基类的一种，编译器自动完成（隐式）。指针/引用"看"的是对象的基类部分，通过它只能访问**基类成员**；
- **对象（切片）**：`Base b = d;` —— 语法允许但**有切片**：把派生类对象**拷贝成基类对象**，派生类特有的成员被"切掉"，语义要小心（不是多态）。
- 结论：指针/引用向上转型**安全**；按对象赋值会切片、丢失派生类信息。

**基类指针指向派生类对象时能访问哪些成员**：只能访问**基类中声明的成员**（和基类的虚函数——通过虚表实现多态）。**不能**通过基类指针直接访问派生类**新增**的成员（编译期不知道也不允许）。

**向下转型（基类 → 派生类，downcast）风险**：
- 把一个基类指针**强转**为派生类指针后，若该指针**实际并不指向派生类对象**（比如指向的是另一个基类对象/别的派生类），那么访问派生类成员会**越界/错误解释内存 → 未定义行为、崩溃**；
- C 风格强转/`static_cast` 的向下转型**不做运行时检查**，风险完全自担。

**dynamic_cast 的作用**：
- 用于**运行时安全的向下转型 / 跨继承层次转换**，**要求基类多态（含虚函数）**（依赖 RTTI）；
- 若实际对象类型匹配，返回派生类指针/引用；**不匹配**时：指针返回 `nullptr`（可判断），引用**抛 `std::bad_cast`**；
- 例：`Derived* pd = dynamic_cast<Derived*>(pb); if (pd) { ... }`；
- 现代规范：能用多态（虚函数+基类接口）解决的问题就别用 dynamic_cast（CppCoreGuidelines：避免 dynamic_cast 链式判断，优先多态设计）；必须做类型分派时才用。

**通俗解释**：向上转型像"介绍朋友时只说他是个'人'"——绝对安全，但只说人话（只能访问基类成员）。向下转型像"你手里拿着一张'动物'卡片，硬说是'狗'"——如果真是狗没问题（dynamic_cast 验明正身），如果其实是猫，你用狗的方式对待它就出事了（未定义行为）。

---

### 7. 派生类的拷贝构造和赋值运算符函数如何正确实现（显式调用基类的拷贝构造和赋值运算符）？

**原题**：派生类的拷贝构造和赋值运算符函数如何正确实现（显式调用基类的拷贝构造和赋值运算符）？

**答案**：

**正确实现要点**：派生类实现拷贝控制时，**必须显式处理基类部分**：
1. **拷贝构造**：在初始化列表里**显式调用基类拷贝构造** `Base(other)`，否则编译器默认调用基类**默认构造**，基类部分会**丢失拷贝**（基类成员不会被拷贝，被默认初始化）；
2. **拷贝赋值**：在函数体里**显式调用基类赋值运算符** `Base::operator=(other)`，否则只给派生类成员赋值，**基类成员不会被更新**；
3. 注意调用 `Base::operator=` 时要用 `this` 限定（`Base::operator=(other)`），避免被自身重载遮蔽造成递归。

```cpp
class Base {
    int _b;
public:
    Base(int b = 0) : _b(b) {}
    Base(const Base& o) : _b(o._b) {}
    Base& operator=(const Base& o) { _b = o._b; return *this; }
};

class Derived : public Base {
    int _d;
public:
    Derived(int b, int d) : Base(b), _d(d) {}
    Derived(const Derived& o) : Base(o), _d(o._d) {}      // 显式调用基类拷贝构造
    Derived& operator=(const Derived& o) {
        if (this != &o) {
            Base::operator=(o);        // 显式调用基类赋值，拷贝基类成员
            _d = o._d;                 // 再拷贝派生类成员
        }
        return *this;
    }
};
```

**现代规范**：
- 若成员都是 RAII 类型（string/vector/smart_ptr）且基类拷贝控制也是默认的，可直接 `= default`；
- 更优雅的是 **copy-and-swap** 惯用法（临时拷贝 + swap），天然处理自赋值与异常安全；
- 牢记 Rule of Five：一旦自定义任一个，就考虑全组（析构/拷贝构/拷贝赋/移动构/移动赋）。

**通俗解释**：拷贝派生类 = "把地基和楼层都复刻"。如果不显式"复制地基（调用基类拷贝）"，新房子只有新楼层、地基却是空的（默认构造）——盖出来是歪的。赋值同理，只更新楼层不动地基，地基还是旧数据。

---

## 二、编程题

### 1. 定义 Base 和 Derived 类，Base 有 _base 成员，Derived 有 _derived 成员，分别测试三种继承方式下派生类内部和外部对基类成员的访问权限。

**原题**：定义 Base 和 Derived 类，Base 有 _base 成员，Derived 有 _derived 成员，分别测试三种继承方式下派生类内部和外部对基类成员的访问权限。

```cpp
#include <iostream>

class Base {
public:
    int _pub = 1;
protected:
    int _prot = 2;
private:
    int _priv = 3;
};

class PubDerived : public Base {          // 公有继承
public:
    void test() {
        _pub = 10;                        // OK
        _prot = 20;                       // OK（protected 在派生类可访问）
        // _priv = 30;                    // 错误：private 成员派生类不可访问
    }
};
class ProtDerived : protected Base {      // 保护继承
public:
    void test() {
        _pub = 10;   // OK；且在外部，_pub 变成 protected
        _prot = 20;  // OK
    }
};
class PrivDerived : private Base {        // 私有继承
public:
    void test() {
        _pub = 10;   // OK；在外部，_pub 变成 private
        _prot = 20;  // OK
    }
};

int main() {
    PubDerived pd;
    pd._pub = 100;                        // OK：公有继承保留 public
    // pd._prot = 200;                    // 错误：类外不可访问 protected

    ProtDerived ptd;
    // ptd._pub = 1;                      // 错误：保护继承后 _pub 变 protected，类外不可访问

    PrivDerived prd;
    // prd._pub = 1;                      // 错误：私有继承后 _pub 变 private，类外不可访问
    std::cout << "编译通过，权限验证完成\n";
    return 0;
}
```

（实际观察：逐个取消注释看编译错误，体会三种继承下基类成员在"派生类内部 / 外部"的可见性差异，与上一题总结表一一对应。）

---

### 2. 创建 Base 和 Derived 对象，观察构造和析构调用顺序；给 Base 添加有参构造，修改 Derived 构造初始化列表显式调用。

**原题**：创建 Base 和 Derived 对象，观察构造和析构调用顺序；给 Base 添加有参构造，修改 Derived 构造初始化列表显式调用。

```cpp
#include <iostream>

class Base {
    int _b;
public:
    Base(int b = 0) : _b(b) { std::cout << "Base 构造(" << _b << ")\n"; }
    ~Base() { std::cout << "Base 析构\n"; }
};

class Derived : public Base {
    int _d;
public:
    Derived(int b, int d) : Base(b), _d(d) {   // 显式调用基类有参构造
        std::cout << "Derived 构造(" << _d << ")\n";
    }
    ~Derived() { std::cout << "Derived 析构\n"; }
};

int main() {
    Derived d(100, 200);
    std::cout << "--- main 结束 ---\n";
    return 0;
}
```

输出顺序：
```
Base 构造(100)
Derived 构造(200)
--- main 结束 ---
Derived 析构
Base 析构
```
构造：先基类后派生类；析构：先派生类后基类。若把 `Derived` 初始化列表里的 `Base(b)` 去掉（且 Base 没有默认构造），编译会报错——这正是"基类无默认构造需显式调用"的验证。

---

### 3. 实现菱形继承（A 有 _a，B 和 C 虚继承 A，D 继承 B 和 C），观察 D 对象大小和访问 _a 的二义性解决。

**原题**：实现菱形继承（A 有 _a，B 和 C 虚继承 A，D 继承 B 和 C），观察 D 对象大小和访问 _a 的二义性解决。

```cpp
#include <iostream>

struct A { int _a; A(int a=0):_a(a){} };
struct B : virtual public A { int _b; };
struct C : virtual public A { int _c; };
struct D : public B, public C { int _d; };

// 对比：非虚继承
struct B2 : public A { int _b; };
struct C2 : public A { int _c; };
struct D2 : public B2, public C2 { int _d; };

int main() {
    D d;
    d._a = 42;              // 虚继承：唯一 A 子对象，无二义性
    d._b = d._c = d._d = 0;
    std::cout << "D 大小 = " << sizeof(D) << " (虚继承，含 vbptr)\n";

    // D2 d2;
    // d2._a = 1;           // 错误：非虚菱形继承，_a 有二义性（来自 B2 和 C2）
    // d2.B2::_a = 1;       // 必须作用域限定才能访问

    std::cout << "D2 大小 = " << sizeof(D2) << " (非虚，含两份 A)\n";
    return 0;
}
```

观察：非虚菱形 `D2` 含**两份** A（`sizeof` 更大，且 `_a` 访问有二义性）；虚继承 `D` 只有**一份** A、通过 vbptr 间接定位（对象内有虚基类偏移信息），`_a` 访问无歧义。

---

### 4. 编写基类指针指向派生类对象，并尝试向下转型（分别用 C 风格转换和 dynamic_cast，需基类有虚函数），观察结果。

**原题**：编写基类指针指向派生类对象，并尝试向下转型（分别用 C 风格转换和 dynamic_cast，需基类有虚函数），观察结果。

```cpp
#include <iostream>

class Base {
public:
    virtual ~Base() {}              // 必须有虚函数，dynamic_cast 才能工作
};
class Derived : public Base {
public:
    void say() { std::cout << "Derived::say\n"; }
};
class Other : public Base {};

int main() {
    Derived d;
    Base* pb = &d;                  // 向上转型（安全）

    // 1) C 风格强转：不检查，编译放行
    Derived* pd1 = (Derived*)pb;
    pd1->say();                     // 正常（实际确实是 Derived）

    Base* pOther = new Other;
    Derived* pdBad = (Derived*)pOther;   // C 风格：明知是 Other 也强转
    // pdBad->say();                // 未定义行为！可能崩溃/乱输出（危险）

    // 2) dynamic_cast：运行时检查
    Derived* pd2 = dynamic_cast<Derived*>(pb);
    if (pd2) std::cout << "dynamic_cast 成功\n";
    else    std::cout << "dynamic_cast 失败\n";

    Derived* pd3 = dynamic_cast<Derived*>(pOther);   // 实际是 Other
    if (pd3) std::cout << "pd3 成功\n";
    else    std::cout << "pd3 = nullptr（dynamic_cast 失败，安全）\n";

    delete pOther;
    return 0;
}
```

要点：`dynamic_cast` 需要基类多态（本例虚析构即满足）；指向 Other 却 C 风格转 Derived 是**未定义行为**，而 dynamic_cast 会返回 nullptr 让你安全判断——这是两种方式的本质区别。

---

### 5. 为 Base 和 Derived 实现拷贝构造和赋值运算符（基类和派生类都有指针成员），正确调用基类的复制控制函数。

**原题**：为 Base 和 Derived 实现拷贝构造和赋值运算符（基类和派生类都有指针成员），正确调用基类的复制控制函数。

```cpp
#include <iostream>
#include <cstring>

class Base {
    char* _data;
public:
    Base(const char* s = "") {
        _data = new char[std::strlen(s) + 1];
        std::strcpy(_data, s);
    }
    Base(const Base& o) {                       // 基类深拷贝构造
        _data = new char[std::strlen(o._data) + 1];
        std::strcpy(_data, o._data);
    }
    Base& operator=(const Base& o) {            // 基类深拷贝赋值
        if (this != &o) {
            delete[] _data;
            _data = new char[std::strlen(o._data) + 1];
            std::strcpy(_data, o._data);
        }
        return *this;
    }
    ~Base() { delete[] _data; }
    const char* data() const { return _data; }
};

class Derived : public Base {
    int _n;
public:
    Derived(const char* s, int n) : Base(s), _n(n) {}
    Derived(const Derived& o) : Base(o), _n(o._n) {}        // 显式调基类拷贝构造
    Derived& operator=(const Derived& o) {
        if (this != &o) {
            Base::operator=(o);      // 关键：显式调基类赋值，拷贝基类指针成员
            _n = o._n;
        }
        return *this;
    }
};

int main() {
    Derived a("hello", 1);
    Derived b = a;            // 拷贝构造：基类部分也深拷贝
    Derived c("", 0);
    c = a;                    // 赋值：先调 Base::operator= 再拷贝派生部分
    std::cout << "b.data=" << b.data() << " c.data=" << c.data() << '\n';
    return 0;
}
```

要点：**派生类拷贝构造初始化列表必须写 `Base(o)`**（拷贝基类部分），**赋值必须写 `Base::operator=(o)`**；否则基类指针成员不拷贝（构造时被默认构造、赋值时不被更新），导致数据丢失/共享/双重释放。

---

# 第七部分：多态与虚函数

## 一、问答题

### 1. 什么是多态（静态多态 vs 动态多态）？动态多态的实现依赖什么（虚函数）？

**原题**：什么是多态（静态多态 vs 动态多态）？动态多态的实现依赖什么（虚函数）？

**答案**：

**多态（polymorphism）**：同一个"接口/调用"，在不同的对象上表现出**不同的行为**。C++ 分两类：

**静态多态（编译期多态）**：
- **函数重载**、**运算符重载**、**模板（函数/类模板）**；
- 在**编译期**根据参数类型/模板实参确定具体调用哪个函数；
- 无运行时开销（但代码膨胀）。

**动态多态（运行期多态）**：
- 基于**虚函数 + 继承 + 基类指针/引用**；
- 在**运行期**根据对象**实际类型**决定调用哪个版本的虚函数（通过虚表）；
- 例子：`Base* p; p->draw();`——p 指向 Rectangle 就画矩形，指向 Circle 就画圆，运行时才确定；
- 有运行时开销（一次间接寻址虚表）。

**动态多态的实现依赖什么**：**虚函数（virtual）+ 虚函数表（vtable）**。含虚函数的类，每个对象隐藏一个**虚函数指针（vptr）**指向类的虚表；虚表存着各虚函数的实际地址。通过基类指针调用虚函数时，编译器生成"通过 vptr → vtable → 函数地址"的**间接调用**，从而在运行时选中正确版本。

**现代视角**：模板（静态多态）和虚函数（动态多态）各有适用场景——需要"运行时插件/统一接口、二进制分发"用动态多态；追求零开销、编译期确定类型用模板（即模板元编程/CRTP 等）。C++ 常"混合使用"。

**通俗解释**：静态多态像"点菜时菜单已经写死每种菜怎么炒（编译期定）"；动态多态像"喊'各位请坐'，每个人（不同的对象）坐下时姿势不同（运行期各按各的习惯反应）"——同一个口令，实际行为由"你是谁"决定。

---

### 2. 虚函数在类中的内存布局变化（虚函数指针、虚函数表）。虚表存放在哪个段？

**原题**：虚函数在类中的内存布局变化（虚函数指针、虚函数表）。虚表存放在哪个段？

**答案**：

**内存布局变化**：
- 类中**有虚函数**后，对象在**开头**（典型实现）多一个隐藏成员 **vptr（虚函数指针）**，指向该类（或该继承层次）的**虚函数表（vtable）**；
- **vtable** 是一个**函数指针数组**，按虚函数声明顺序存放各虚函数的**真实地址**；
- 因此 `sizeof(对象)` 增加一个指针大小（32 位 4B / 64 位 8B），且成员对齐可能改变；
- 每个**类**有一张自己的虚表（**对象共享**同一张表，vptr 值相同）；派生类覆盖的虚函数，在虚表中对应位置替换为派生类版本。

```
对象 d:
┌─────────┐      ┌────────────────────┐
│  vptr ───────→ │ vtable (该类的)      │
│ 成员... │      │  &Base::vf1  (可被覆盖)│
└─────────┘      │  &Base::vf2         │
                 └────────────────────┘
```

**虚表存放在哪个段**：
- **实现相关**，但主流程程（GCC/Clang/MSVC）通常把 **vtable 放在只读数据段（.rodata）**（只读、只放函数地址）；vptr 在对象内（对象在栈/堆/数据段）；
- 老实现也有放数据段的变体，但规范不做规定；概念上记住"**vtable 是只读的、全局共享的、属于类而非对象**"即可。
- 补充：RTTI 类型信息（`typeid` 用）通常也放 .rodata 附近。

**通俗解释**：vptr 像"每个对象口袋里装的'本类行为手册'的地址"；vtable 像"这本手册本身（一页页写着各方法在哪执行）"。手册是**按"类"印刷的、全校共用**（只读、全局一份），每个学生（对象）只是揣着指向自己班级手册的索引（vptr）。手册在"图书室（.rodata）"保管。

---

### 3. 动态多态被激活的五个条件是什么？请结合代码说明。

**原题**：动态多态被激活的五个条件是什么？请结合代码说明。

**答案**：

常见"五条件"表述（不同教材归纳略有差异，核心是：**虚函数 + 继承 + 基类引用/指针调用**）：

1. **基类中声明了虚函数**（`virtual`）；
2. **派生类覆盖（override）了该虚函数**（参数/返回一致，返回可协变）；
3. **必须有继承关系**，且通过**基类的指针或引用**来指向/绑定**派生类对象**；
4. **通过该基类指针/引用调用虚函数**（若用对象直接调用，是静态绑定）；
5. **虚函数调用发生在"运行期决议"的上下文中**（不能是构造/析构期间等静态绑定场合）。

```cpp
#include <iostream>
class Shape {
public:
    virtual void draw() const { std::cout << "Shape\n"; }   // 条件1：虚函数
};
class Circle : public Shape {
public:
    void draw() const override { std::cout << "Circle\n"; } // 条件2：覆盖
};

int main() {
    Circle c;
    Shape& rs = c;       // 条件3：基类引用绑定派生类对象
    Shape* ps = &c;      // 条件3：基类指针指向派生类对象

    rs.draw();           // 条件4：经基类引用调用虚函数 → Circle（多态激活）
    ps->draw();          // 条件4：经基类指针调用 → Circle

    Circle c2;
    c2.draw();           // 直接对象调用：也调用 Circle（但这是"普通成员调用"，
                         // 不经基类接口，非"通过基类指针/引用"这一多态场景）
    Shape s = c;         // 对象切片：s 是 Shape，调用 Shape::draw（静态绑定）
    s.draw();            // → Shape
    return 0;
}
```

关键：多态的必要场景是"**基类接口 + 派生类实现**"，通过**基类指针/引用**才真正体现运行时多态；直接用对象（或切片）调用是静态绑定。

**通俗解释**：五个条件像"远程指挥的五个环节"：①有个总指挥岗位（虚函数）②不同部队有自己的打法（覆盖）③你拿着"总指挥证"（基类指针）指着某支部队（派生对象）④你喊"冲锋"（调用虚函数）⑤而且是在**战斗现场**喊（运行期、非训练场/非教练时间——即不能在构造/析构里喊）。满足这些，才按"对象实际是谁"来执行。

---

### 4. 覆盖（override）与隐藏（oversee）的区别？override 关键字的作用？

**原题**：覆盖（override）与隐藏（oversee）的区别？override 关键字的作用？

**答案**：

**覆盖（override）**：
- 派生类函数与基类**虚函数**签名**完全一致**（参数、const 限定一致，返回类型相同或**协变**）；
- 通过**基类指针/引用**调用时实现**多态**（运行期选派生类版本）；
- 是"真正的重写实现"。

**隐藏（hide）**：
- 派生类定义同名成员，**不管参数是否一致、不管是否 virtual**，都隐藏基类的该名字（详见第六部分第 3 题）；
- 与多态无关，只是**名字遮蔽**；
- 调用时按"静态类型"选择，不经虚表。

**核心区别表**：

| 对比             | 覆盖 override                  | 隐藏 hide                  |
| ---------------- | ------------------------------ | -------------------------- |
| 前提             | 基类是**虚函数**，签名完全一致 | 只需**同名**（参数可不同） |
| 是否多态         | 是（运行期分派）               | 否（名字遮蔽）             |
| 通过基类指针调用 | 调派生类版本                   | 调基类版本                 |
| 是"写错"高发点   | 签名写错就不覆盖（变隐藏）     | 无意间造成                 |

**override 关键字的作用**：C++11 起在派生类函数声明后写 `override`，**显式声明"我要覆盖基类虚函数"**，编译器会检查：若签名不匹配/基类无此虚函数 → **编译错误**。这能把"本想覆盖却因签名写错变成隐藏"的隐蔽 bug 提前暴露在编译期。现代规范（CppCoreGuidelines）：**覆盖虚函数必须写 `override`**，并尽量把基类虚函数声明为 `final`/`override` 控制设计。

**通俗解释**：覆盖 = "真把爸爸的方法换成了自己的版本（连签名都一样）"；隐藏 = "给自己办公室也摆了个同名文件柜，把爸爸的同名文件柜挡在身后（名字被遮住）"。`override` = "在文件柜上贴标签'这确实是要替换爸爸那个的'"，贴错了（签名不对）工作人员（编译器）当场喊停。

---

### 5. 为什么构造函数不能是虚函数？为什么静态成员函数不能是虚函数？为什么 inline 不能是虚函数（但可以同时声明，会忽略 inline）？

**原题**：为什么构造函数不能是虚函数？为什么静态成员函数不能是虚函数？为什么 inline 不能是虚函数（但可以同时声明，会忽略 inline）？

**答案**：

**为什么构造函数不能是虚函数**：
1. **vptr 尚未就绪**：对象在构造函数执行期间，vptr 才被逐步初始化（先指向基类虚表、构造完成后指向本类虚表）。构造函数被调用时**对象的 vptr 还没建立/还没指向本类虚表**，根本无法通过虚表做动态分派；
2. **语义矛盾**：虚函数依赖"对象已存在、已有实际类型"，而构造是"对象尚未完成创建"，不存在"按实际类型分派"的对象；
3. 若构造函数是虚的，调用它时还没有对象可供分派（"从哪来"的问题）。所以构造函数**不能虚**，只能按静态类型直接调用。

**为什么静态成员函数不能是虚函数**：
- 虚函数调用需要 **this 指针**去访问对象的 vptr，从而查虚表；
- **静态成员函数没有 this**，不依附于对象，无法访问 vptr/虚表，自然无法做运行期分派；
- 而且静态函数本来就属于类、按类调用，语义上也不需要多态。

**为什么 inline 不能是虚函数（但可同时声明，inline 被忽略）**：
- `inline` 是**编译期**"在调用点展开"的建议；而虚函数是**运行期**通过虚表**间接调用**——编译期无法确定调用点到底展开哪个版本（要运行才知道对象类型）；
- 所以即使写 `virtual inline void f()`，编译器也会**忽略 inline**（inline 只是提示，编译器也不予展开虚调用）；
- 补充：虚函数**可以声明为 inline**（语法允许、不报错），编译器直接忽略内联意图；极小概率在编译器确认静态绑定（如直接对象调用）时也可能内联，但那是优化决策。

**通俗解释**：构造 = "房子还没盖好，当然没法按'这是公寓还是别墅'来决定装修方式（vptr 还没装）"；静态函数 = "公司公告，不需要知道是对哪个员工说的（无 this）"，公告自然不存在"对不同员工有不同播法"；inline 想"提前印好小抄（编译期展开）"，而虚函数是"开考才知道题目（运行期类型）"，小抄印不出来，只好作罢。

---

### 6. 在构造函数或析构函数中调用虚函数，会发生什么（静态绑定，调用当前类的版本）？

**原题**：在构造函数或析构函数中调用虚函数，会发生什么（静态绑定，调用当前类的版本）？

**答案**：

**结论**：在**构造函数或析构函数内调用虚函数**，**不会产生多态**，执行的是**静态绑定——调用当前正在构造/析构的这个类的版本**（本类或它"上面"已构造完成的基类版本），而**不会**调用"最底层派生类"的覆盖版本。

**原因**：构造/析构期间，对象的 vptr 指向的虚表是"**当前构造/析构阶段的类的虚表**"：
- 基类构造函数执行时，vptr 指向**基类的虚表**（派生类部分还没构造）；
- 等派生类构造函数执行时 vptr 才更新指向**派生类的虚表**；
- 析构过程相反：派生类析构体执行时 vptr 仍指派生类虚表，之后进入基类析构时 vptr 改指基类虚表。

因此在这些阶段调用虚函数，实际调用的是"vptr 当前指向的虚表"里的版本——即**当前类**的版本。

**为什么不调派生类版本（对设计很重要）**：
- 如果在基类构造中调用派生类的覆盖版本，派生类部分的成员**尚未初始化**，使用它们会出问题——语言刻意避免这种"半成品对象上跑最终版本"的危险，故规定为静态绑定。

**实践指导**：**不要在构造函数/析构函数里调用虚函数**（CppCoreGuidelines 明确警告），应调用本类的非虚实现/私有工具函数。若确实需要"初始化时的行为差异"，可用模板/CRTP 或显式传参等编译期方案。

**通俗解释**：盖房子时，"打地基阶段"按"地基的施工手册（基类虚表）"施工，此时你喊"按最终户型装修"（虚函数）根本不会生效——因为楼还没盖到那层，vptr 还指着地基阶段的图。拆房子时反过来，拆到哪层用哪层的图。所以施工中（构造/析构）你喊的都是"当前这层的手册"。

---

### 7. 纯虚函数的作用？包含纯虚函数的类称为抽象类，抽象类能否实例化？纯虚函数可以有实现吗？

**原题**：纯虚函数的作用？包含纯虚函数的类称为抽象类，抽象类能否实例化？纯虚函数可以有实现吗？

**答案**：

**纯虚函数**：在虚函数声明后加 `= 0`：
```cpp
class Shape {
public:
    virtual double area() const = 0;   // 纯虚函数
};
```
作用：
1. **定义接口规范**：规定派生类"必须实现"的接口，强制派生类覆盖；
2. 使类成为**抽象类**，表达"此类不能实例化、只作为接口/骨架"的语义；
3. 是实现**多态接口设计**的核心手段（类似 Java 的 interface）。

**抽象类能否实例化**：**不能**。含纯虚函数的类是抽象类，**不能创建对象**（`Shape s;` 编译错误）。但可以：
- 用**派生类**（实现了所有纯虚函数）实例化；
- 声明**指针/引用**（`Shape* p = new Circle;`）——这是多态的标准用法；
- 若派生类**没有实现全部纯虚函数**，派生类仍是抽象类。

**纯虚函数可以有实现吗**：**可以**。纯虚函数可以有函数体（定义），派生类必须**仍然覆盖它**才能实例化，但可以在派生类实现里**显式调用 `Base::pureFunc()`** 复用默认逻辑：
```cpp
class Shape {
public:
    virtual double area() const = 0 { return 0; }  // 纯虚函数带默认实现
};
```
（注意语法：`= 0 { ... }` 或声明后类外定义 `double Shape::area() const { ... }`。通常**类外定义**更清晰。）
带实现的纯虚函数适用场景：提供"默认兜底实现"同时仍强制派生类必须覆盖（哪怕只调基类默认）。

**补充**：C++11 起还有**抽象类 + 纯虚析构**的用法：基类析构可以是纯虚的，但**必须有定义**（否则链接错误），目的是让基类"不可实例化"且强制派生类实现自己的析构。

**通俗解释**：纯虚函数 = "合同里写死的条款：凡签这份合同（继承该类）的，必须提供这个能力（实现 area）"。抽象类 = "只卖合同、不卖现货"——你不能直接要一个"图形"，只能要"具体的圆/矩形（派生类）"。合同条款可以附带"参考示例（默认实现）"，但签约方仍必须自己写一遍条款。

---

### 8. 析构函数为什么推荐设为虚函数？如果不设为虚函数，通过基类指针删除派生类对象会出现什么问题（内存泄漏）？

**原题**：析构函数为什么推荐设为虚函数？如果不设为虚函数，通过基类指针删除派生类对象会出现什么问题（内存泄漏）？

**答案**：

**推荐设为虚函数**：
- 当用**基类指针/引用**管理派生类对象时（多态的标准场景），`delete` 基类指针时**必须调用派生类的析构函数**，才能完整释放派生类资源；
- 基类析构为 **virtual** 后，`delete pb` 会通过虚表**动态分派**到派生类析构 → 先执行派生类析构体、再执行基类析构，释放完整、顺序正确；
- 因此**凡是会作为多态基类使用的类，析构函数都应声明为 `virtual`**（CppCoreGuidelines 明确要求"基类析构应为 public virtual 或 protected non-virtual"）。

**不设为虚函数的问题（内存泄漏/未定义行为）**：
```cpp
class Base { public: ~Base() {} };        // 非虚析构
class Derived : public Base { char* _p; public: Derived(){ _p=new char[100];} ~Derived(){ delete[] _p; } };

Base* pb = new Derived;
delete pb;     // 问题！只调用 Base::~Base()，不调用 Derived::~Derived()
```
- `delete pb` 按**静态类型（Base）**决定调用的析构——**只调用基类析构**，**不调用派生类析构**；
- 结果：派生类的 `_p` 那块堆内存**没有被释放** → **内存泄漏**（派生类资源全部泄漏）；
- 严格说，删除指向派生类对象的基类指针而非虚析构，属于**未定义行为**（标准层面），实际表现通常是泄漏。

**正确做法**：把基类析构设为 `virtual`。现代规范还建议：抽象基类析构可以是 **protected 非虚**（避免通过基类指针 delete），或 public virtual；派生类析构写 `override`。

**通俗解释**：删对象 = "退房"。如果基类析构不是虚的，就像"退房登记簿只认'房客'这一栏（Base 类型）"，前台按房客信息退房，结果忘了"这房客还租了地下车库（派生类资源）"，车库一直没退、一直扣钱（泄漏）。虚析构 = "登记簿能按实际房客类型退全套（先退车库再退房）"。

---

### 9. 多继承下多个虚函数表如何组织？派生类覆盖虚函数时，所有虚表中相应位置都会被覆盖吗（实际第一个虚表直接覆盖，其他虚表通过跳转）？

**原题**：多继承下多个虚函数表如何组织？派生类覆盖虚函数时，所有虚表中相应位置都会被覆盖吗（实际第一个虚表直接覆盖，其他虚表通过跳转）？

**答案**：

**多继承下的虚表组织**：
- 一个含虚函数的类，每**继承一个"含虚函数的基类"**，其对象通常就有**一个对应的 vptr**；多继承 N 个含虚函数的基类，派生类对象就含 **N 个 vptr**（每个 vptr 对应一条"基类链"的虚表）；
- 派生类对象布局（典型实现）：先按声明序排列各个基类子对象（每个子对象带自己的 vptr），再放派生类自己的成员；
- 每条基类链对应一张虚表，存"以该基类类型视角能看到的虚函数"。

**派生类覆盖虚函数时，所有虚表相应位置都被覆盖吗**：
- **第一个（主基类，声明在前的第一个含虚函数基类）的虚表：直接覆盖**——把该虚函数地址替换为派生类版本；
- **其他基类的虚表：不直接覆盖成普通函数地址**，而是放一个**跳转（thunk）**——一个小的适配函数，它先做"**this 指针调整**"（把指向该基类子对象的 this 修正为指向派生类整体对象），再**跳转到派生类的覆盖实现**。

**为什么需要 thunk/跳转**：
- 派生类覆盖函数期望的 `this` 是**派生类对象整体地址**；
- 但通过第 2、3 个基类指针（如 `C*`）调用时，传入的 this 指向的是**该基类子对象偏移处**，不是整体开头；
- 所以需要 thunk 在跳转前**减去偏移量**把 this 修正回来（`this - offset`），否则成员访问错位（未定义行为）。

**结论**：第一条基类链的虚表**直接指向派生类实现**；其余基类链的虚表**指向 thunk**（thunk 修正 this 后跳转到派生类实现）——两者最终都"调用"派生类覆盖版本，只是第 2+ 条路径多一步 this 修正。

**通俗解释**：多继承 = "一个人同时有两个上级（B 和 C）"，公司里有**两本通讯录（两张虚表）**，各按各的上级视角记。老板 B 直接打电话给本人（第一虚表直接指向本人手机）；老板 C 要找人时，先拨"分机转接（thunk）"——把"找 C 部门那个人"的呼叫**校正一下内部工号（this 偏移）**，再转接到本人手机。

---

## 二、编程题

### 1. 编写 Base 和 Derived，在 Base 中定义虚函数 display，Derived 覆盖它。用基类指针指向派生类对象调用 display，验证多态。

**原题**：编写 Base 和 Derived，在 Base 中定义虚函数 display，Derived 覆盖它。用基类指针指向派生类对象调用 display，验证多态。

```cpp
#include <iostream>

class Base {
public:
    virtual void display() const { std::cout << "Base::display\n"; }
    virtual ~Base() {}
};
class Derived : public Base {
public:
    void display() const override { std::cout << "Derived::display\n"; }
};
class Derived2 : public Base {
public:
    void display() const override { std::cout << "Derived2::display\n"; }
};

int main() {
    Base* arr[] = { new Base, new Derived, new Derived2 };
    for (Base* p : arr) {
        p->display();      // 运行时按实际类型分派 → 多态
        delete p;
    }
    // 输出：Base::display / Derived::display / Derived2::display
    return 0;
}
```

要点：同一个 `p->display()` 在运行期根据 p 指向的实际对象决定调用哪个版本——这就是动态多态；三个对象经统一基类接口处理，正是"基类指针数组统一调用"的经典模式。

---

### 2. 验证虚表的存在：通过 long* 强制转换对象地址，打印虚函数指针和虚表内容，并调用虚表函数。

**原题**：验证虚表的存在：通过 long* 强制转换对象地址，打印虚函数指针和虚表内容，并调用虚表函数。

```cpp
#include <iostream>

class Base {
public:
    virtual void f1() { std::cout << "Base::f1\n"; }
    virtual void f2() { std::cout << "Base::f2\n"; }
};
class Derived : public Base {
public:
    void f1() override { std::cout << "Derived::f1\n"; }
};

int main() {
    Base b;
    Derived d;

    // 取出对象首部的 vptr：把对象地址当作 long* 取第一个元素
    long* bptr = *(long**)&b;    // b 的虚表地址
    long* dptr = *(long**)&d;    // d 的虚表地址

    std::cout << "b 的 vptr = " << bptr << '\n';
    std::cout << "d 的 vptr = " << dptr << '\n';
    std::cout << "b 虚表[0] = " << (void*)bptr[0]
              << " 虚表[1] = " << (void*)bptr[1] << '\n';

    // 通过虚表直接调用虚函数（模拟编译器动态分派）
    typedef void (*VFPTR)();
    VFPTR vf1 = (VFPTR)bptr[0];   // b 的 f1
    vf1();                        // Base::f1

    VFPTR df1 = (VFPTR)dptr[0];   // d 的 f1（已被覆盖）
    df1();                        // Derived::f1 —— 虚表里存的是覆盖后的地址
    return 0;
}
```

要点：对象首部第一个"字"就是 vptr（`*(long**)&obj` 取它）；虚表是函数指针数组；**派生类覆盖后，虚表对应槽位存的是派生类实现地址**（d 的虚表[0] 与 b 的虚表[0] 不同）。这直观验证了虚表机制。

---

### 3. 声明一个纯虚基类 Figure（getArea 纯虚），派生 Rectangle、Circle、Triangle 实现面积计算，用基类指针数组统一调用。

**原题**：声明一个纯虚基类 Figure（getArea 纯虚），派生 Rectangle、Circle、Triangle 实现面积计算，用基类指针数组统一调用。

```cpp
#include <iostream>
#include <vector>
#include <memory>
#include <cmath>

class Figure {                       // 抽象基类
public:
    virtual double getArea() const = 0;   // 纯虚函数
    virtual ~Figure() = default;
};
class Rectangle : public Figure {
    double _w, _h;
public:
    Rectangle(double w, double h) : _w(w), _h(h) {}
    double getArea() const override { return _w * _h; }
};
class Circle : public Figure {
    double _r;
public:
    Circle(double r) : _r(r) {}
    double getArea() const override { return 3.141592653589793 * _r * _r; }
};
class Triangle : public Figure {
    double _base, _height;
public:
    Triangle(double b, double h) : _base(b), _height(h) {}
    double getArea() const override { return 0.5 * _base * _height; }
};

int main() {
    std::vector<std::unique_ptr<Figure>> figs;     // 现代：unique_ptr 管理基类指针
    figs.emplace_back(new Rectangle(4, 5));
    figs.emplace_back(new Circle(1));
    figs.emplace_back(new Triangle(6, 4));

    double total = 0;
    for (const auto& f : figs) {
        double a = f->getArea();      // 多态统一调用
        std::cout << "面积 = " << a << '\n';
        total += a;
    }
    std::cout << "总面积 = " << total << '\n';
    return 0;
}
```

要点：`Figure` 抽象、三个派生实现 `getArea`，`vector<unique_ptr<Figure>>` 统一管理并多态调用；`unique_ptr` 自动负责删除（体现现代智能指针 + 虚析构的配合）。

---

### 4. 将基类析构函数设为虚函数，通过基类指针 delete 派生类对象，观察析构顺序；去掉 virtual 再试，比较差异。

**原题**：将基类析构函数设为虚函数，通过基类指针 delete 派生类对象，观察析构顺序；去掉 virtual 再试，比较差异。

```cpp
#include <iostream>

class Base {
public:
    Base() { std::cout << "Base 构造\n"; }
    virtual ~Base() { std::cout << "Base 析构\n"; }   // 虚析构
};
class Derived : public Base {
    int* _p;
public:
    Derived() : _p(new int(0)) { std::cout << "Derived 构造\n"; }
    ~Derived() override {
        delete _p;
        std::cout << "Derived 析构\n";
    }
};

int main() {
    Base* p = new Derived;
    delete p;     // 虚析构：先 Derived 析构，再 Base 析构，_p 被释放
    return 0;
}
```

**输出（虚析构）**：Base 构造 → Derived 构造 → **Derived 析构 → Base 析构**（完整）。

把 `virtual ~Base()` 改成 `~Base()`（去掉 virtual）后：输出变成 Base 构造 → Derived 构造 → **只有 Base 析构**，Derived 析构不执行、`_p` 泄漏（可用 valgrind/ASan 观察 definitely lost）。这就是"基类析构必须虚"的活证据。

---

### 5. 在派生类构造和析构函数中调用虚函数，观察实际调用的版本。

**原题**：在派生类构造和析构函数中调用虚函数，观察实际调用的版本。

```cpp
#include <iostream>

class Base {
public:
    virtual void who() const { std::cout << "Base::who\n"; }
    Base()  { std::cout << "Base 构造，调用 who: "; who(); }
    virtual ~Base() { std::cout << "Base 析构，调用 who: "; who(); }
};
class Derived : public Base {
public:
    void who() const override { std::cout << "Derived::who\n"; }
    Derived() { std::cout << "Derived 构造，调用 who: "; who(); }
    ~Derived() override { std::cout << "Derived 析构，调用 who: "; who(); }
};

int main() {
    Derived d;
    std::cout << "--- 结束 ---\n";
    return 0;
}
```

**输出**：
```
Base 构造，调用 who: Base::who      ← 基类构造中：静态绑定，调 Base 版（vptr 还指基类虚表）
Derived 构造，调用 who: Derived::who  ← 派生类构造中：vptr 已指向派生类虚表
--- 结束 ---
Derived 析构，调用 who: Derived::who  ← 派生类析构中：仍调派生版
Base 析构，调用 who: Base::who      ← 基类析构中：vptr 已切回基类虚表
```

验证结论：**构造/析构中调用虚函数都是"当前阶段所在类的版本"**（静态绑定），不会触发到"最底层"多态。

---

# 第八部分：模板

## 一、问答题

### 1. 模板的作用是什么？函数模板和类模板分别解决什么问题？

**原题**：模板的作用是什么？函数模板和类模板分别解决什么问题？

**答案**：

**模板的作用**：实现**代码复用**与**泛型编程（generic programming）**——"用类型作参数"写一份代码，让它适用于多种类型，由编译器按需**实例化**成具体类型的版本。避免为每种类型重复写几乎一样的代码（如分别写 int/double/string 的 max）。

**函数模板**解决"**算法/函数逻辑与具体类型无关**"的问题：
```cpp
template <typename T>
T myMax(const T& a, const T& b) { return a > b ? a : b; }
// myMax(3,5)、myMax(2.5,1.5)、myMax(string("a"), string("b")) 均可
```
一次定义，多种类型可用。

**类模板**解决"**数据结构/容器与元素类型无关**"的问题：
```cpp
template <typename T>
class Stack { ... T* _data; ... };
// Stack<int>、Stack<std::string> 都是独立实例
```
标准库 `vector<T>`、`list<T>`、`map<K,V>` 全是类模板。

**共同特点**：
- 编译期实例化（生成具体类型代码），**零运行时抽象开销**（相对虚函数动态多态）；
- 代码膨胀（每种类型一份代码）；
- 错误信息常冗长（现代编译器有改进，C++20 concepts 大幅改善）；
- 模板代码通常放头文件（见第 4 题）。

**通俗解释**：模板像"一张可填的空格合同/万能模具"：函数模板 = "一个万能配方，填不同食材（类型）就能炒不同菜"；类模板 = "一个万能仓库图纸，标上'装什么货'就能建相应仓库"。写一次，编译器帮你按每种"填法"复制出对应版本。

---

### 2. 函数模板的隐式实例化与显式实例化有什么区别？模板实参推导的规则？

**原题**：函数模板的隐式实例化与显式实例化有什么区别？模板实参推导的规则？

**答案**：

**隐式实例化（implicit instantiation）**：调用时编译器**从实参自动推导**模板实参并生成对应特化：
```cpp
template <typename T> T myMax(const T& a, const T& b) { ... }
myMax(3, 5);          // 编译器自动 T=int
myMax(2.5, 1.5);      // 自动 T=double
```

**显式实例化（explicit instantiation）**：
- 调用时**显式指定模板实参**：
```cpp
myMax<int>(3, 5);     // 明确 T=int
```
- 或在源文件里显式生成实例（用于"声明实现分离"方案）：
```cpp
template double myMax<double>(const double&, const double&);   // 显式实例化定义
```
作用是"提前/强制生成某类型的实例"，常配合 .h/.cpp 分离时避免链接错误。

**模板实参推导规则（要点）**：
1. 从函数**实参类型**推导 T（如 `const T&` 从实参推导）；
2. 推导时**忽略引用/顶层 const**（`const T&` 参数推导 T 时去掉引用与顶层 const）；
3. **无法从参数推导**的 T 必须显式给出（如 `template <typename T, typename U> U f(T);`，U 需显式指定）；
4. 实参类型必须**一致**——`myMax(3, 2.5)` 两个实参推导出 `int` 和 `double` **不一致** → 推导失败（除非显式 `myMax<double>(3,2.5)` 触发隐式转换）；
5. 数组/函数名作为实参时会**退化为指针**（除非参数是引用）；
6. 可结合默认模板实参（C++11 起）使用。

**通俗解释**：隐式实例化 = "编译器看你怎么用（实参），自己猜 T 是啥"；显式实例化 = "你明确告诉它 T 是啥"。推导规则就像"看货猜价"：从你递过来的东西（实参）反推类型；两边递来的东西长得不一样（int 和 double），就猜不出来，得你明说。

---

### 3. 函数模板与普通函数重载时，调用优先级如何（普通函数优先）？函数模板之间重载的注意事项？

**原题**：函数模板与普通函数重载时，调用优先级如何（普通函数优先）？函数模板之间重载的注意事项？

**答案**：

**函数模板 vs 普通函数重载的优先级**：当调用点既能匹配普通函数、又能通过模板实例化匹配时：
- 如果**普通函数能形成（同样好的）匹配**，则**优先选择普通函数**（非模板函数优先于模板特化——C++ 重载决议规则：**非模板候选优先于模板候选，当两者匹配程度相同**）；
- 但若**模板能形成更好的匹配**（如普通函数需要隐式转换，而模板精确匹配），则模板胜出；
- 也可**显式指定模板实参**强制走模板（`max<int>(...)`）。

```cpp
int max(int a, int b) { ... }                 // 普通函数
template <typename T> T max(T a, T b) { ... } // 模板
max(3, 5);       // 普通函数（完美匹配，优先）
max(3.0, 5.0);   // 模板（普通函数要 double→int 转换，模板精确匹配，模板胜）
max<int>(3, 5);  // 显式指定 → 走模板
```

**函数模板之间重载的注意事项**：
1. **部分偏特化（partial specialization）函数模板不支持**——函数模板不能偏特化，只能"全特化"或"重载"（用不同参数形状的重载表达"偏特化"意图）；
2. 模板重载决议依据"**更特化（更具体）的模板优先**"：如 `template<typename T> void f(T)` 与 `template<typename T> void f(T*)`，传指针时后者（更特化）优先；
3. 多个模板都可行时选"最佳匹配"，否则二义性报错；
4. 模板重载要注意不要造成意外匹配（用 `enable_if`/`if constexpr`/concepts 控制候选）。

**现代规范**：C++20 concepts 可精确约束模板重载；C++17 的 `if constexpr` 让模板内分支更清晰。

**通俗解释**：模板和普通函数同名时，像"点菜既有'标准套餐（普通函数）'也有'万能套餐（模板）'"——标准套餐能完全对上就优先用标准套餐（非模板优先）；标准套餐需要"将就一下（转换）"而万能套餐正好合口，就用万能套餐。模板之间则比"谁更具体"（特化程度高的赢）。

---

### 4. 为什么模板的声明和实现通常都放在头文件中（或 .hpp）？如果分离到 .h 和 .cpp 会出现链接错误，如何解决（#include "template.cpp"）？

**原题**：为什么模板的声明和实现通常都放在头文件中（或 .hpp）？如果分离到 .h 和 .cpp 会出现链接错误，如何解决（#include "template.cpp"）？

**答案**：

**为什么声明和实现放头文件（.hpp）**：
- 模板是"**编译期实例化**"的：调用模板时，编译器必须**看到完整定义**才能生成具体类型的实例（它需要在调用点做类型推导并"展开"代码）；
- 若把实现放 .cpp，则**使用模板的 .cpp**（翻译单元）里只能看到声明、**看不到定义**，无法实例化 → 编译器在该处只生成"调用"，但**没有地方生成该模板实例的定义** → **链接期报"未定义引用（undefined reference）"**；
- 所以模板的"定义"必须在使用它的每个翻译单元里可见 → 放头文件（或被包含）。
- 例外：**显式实例化**可在 .cpp 里把定义藏起来、在头文件只声明（见下）。

**分离到 .h/.cpp 的链接错误与解决**：
- 错误现象：`undefined reference to ...`（函数模板实例找不到定义）。
- 解决方式：
  1. **最简单/推荐**：定义直接放头文件或 `.hpp`（模板在头文件里实现）；
  2. 旧式技巧：`.cpp` 里实现，在 `.h` 末尾 `#include "foo.cpp"`（把定义"拉进"每个使用处）——可行但风格不佳（把 .cpp 当头文件 include）；
  3. **显式实例化方案**（现代推荐）：`.cpp` 里定义模板，并在**该 .cpp 末尾显式实例化**需要的类型；头文件只放声明：
     ```cpp
     // foo.h
     template <typename T> T myMax(const T&, const T&);   // 只声明
     // foo.cpp
     template <typename T> T myMax(const T& a, const T& b) { return a>b?a:b; }
     template double myMax<double>(const double&, const double&);  // 显式实例化
     template int    myMax<int>(const int&, const int&);
     ```
     缺点：**需要显式列出所有用到的类型**，否则用新类型就链接错误；维护成本高。
  4. 类模板同理：可在类外定义成员函数模板，但通常整个类模板放头文件。

**现代规范**：C++20 的 modules 可优雅解决模板封装问题（声明与实现分离且不外泄实现细节），但传统构建仍是"头文件内实现"为主。

**通俗解释**：模板 = "餐厅要按你点的菜现场炒（编译期实例化），厨师必须看得见完整菜谱（定义）"。你把菜谱锁在 .cpp 的保险柜里，点菜窗口（使用模板的 .cpp）只有菜单没有菜谱，厨师想炒却没料——链接时就喊"这道菜不会做（undefined reference）"。解决：把菜谱直接贴窗口（头文件实现），或提前把"会做的几种菜"各做一份放冷藏室（显式实例化）。

---

### 5. 模板特化的作用？全特化与偏特化（偏特化仅类模板支持）的区别？

**原题**：模板特化的作用？全特化与偏特化（偏特化仅类模板支持）的区别？

**答案**：

**模板特化（template specialization）**：为**特定类型/特定模板参数**提供**专门实现**，覆盖通用模板的行为。作用：对某些特殊类型给出更优/更正确的实现（如 `std::vector<bool>` 特化为位压缩、`std::hash<自定义类型>` 特化、数值模板对 `void` 特化等）。

**全特化（explicit/full specialization）**：**所有模板参数都指定为具体类型**：
```cpp
template <typename T> class Stack { ... };       // 通用模板
template <> class Stack<bool> { ... };           // 全特化：T = bool
// 函数模板全特化
template <> int myMax<int>(int, int) { ... }
```

**偏特化（partial specialization）**：**部分参数被特化**（只保留部分自由参数）：
```cpp
template <typename T, typename U> class Pair { ... };
template <typename U> class Pair<int, U> { ... };   // 偏特化：第一个参数固定为 int
template <typename T> class Stack<T*> { ... };      // 偏特化：指针类型的专门实现
```

**关键区别**：
- **全特化**：参数全部确定（`template <>`）；
- **偏特化**：参数部分确定、仍留有自由参数（`template <typename U>` 等）；
- **函数模板只能全特化，不能偏特化**（标准规定；函数模板的"偏特化需求"用**函数重载**实现）；
- **类模板（及类模板成员）支持全特化和偏特化**；
- 特化要放在**原模板声明之后**，否则编译错误；类模板的特化版本可以与被特化的通用版本有**完全不同**的成员（合法）。

**通俗解释**：通用模板像"万能菜单"，特化像"给某些特殊客人开的小灶"：全特化 = "给'只吃素的'（bool）单独开一页全素菜单"；偏特化 = "给'所有用左手写字的人'（T* 这类）单独出一套左手版用具"，但仍留一部分可调（比如写的内容 T 还能变）。函数模板不让做"左手版（偏特化）"，只能整个换菜（全特化）或直接加一道新菜（重载）。

---

### 6. 非类型模板参数的限制（必须是整型常量，不能是浮点、类类型）？如何给非类型参数设默认值？

**原题**：非类型模板参数的限制（必须是整型常量，不能是浮点、类类型）？如何给非类型参数设默认值？

**答案**：

**非类型模板参数（non-type template parameter）**：模板参数不是类型，而是**一个值**：
```cpp
template <typename T, int N>   // N 是非类型参数
class Array { T _data[N]; };
Array<int, 100> a;
```

**限制（分标准版本，题面偏 C++11 前）**：
- **C++11/14/17**：非类型参数可以是——**整型/枚举常量**（int、char、enum 等）、**指针**、**指向成员的指针**、**左值引用**（绑定到具有内部/外部链接的对象）、`std::nullptr_t`。**浮点（float/double）和类类型对象不允许**作为非类型模板参数（题面说法在此范围内成立）。
- **C++20 起已放宽**：允许**浮点类型**和**字面量类类型（literal class type）**作为非类型模板参数（前提是满足某些约束）。这是"时兴"的更新，题面"不能是浮点"在 C++20 下不再成立。
- 整型参数必须是**编译期常量**（constexpr 表达式），不能是运行时变量。

**给非类型参数设默认值**：语法与类型参数默认值相同，写 `= 值`：
```cpp
template <typename T = int, int N = 10>
class Array { ... };
Array<> a;            // T=int, N=10
Array<double, 5> b;
```

**典型用途**：编译期定长数组（`std::array<T,N>`）、模板里做编译期计算（如 `constexpr int Fibonacci<N>`）、位宽/对齐参数、策略参数（`template<int Align> class Allocator`）等。

**通俗解释**：非类型参数 = "模板里的一个'编译期写死的数字'"，像"仓库图纸上写死'要建 100 个货位'"。旧规矩（C++17 前）：数字只能是整数/枚举这类"朴素值"，小数和自定义对象不让写；C++20 起规矩放宽了，小数和部分"纯字面量类"也能写了。默认值 = "图纸上不写就默认 10 个货位"。

---

### 7. 成员函数模板的定义方式？为什么成员函数模板不能是虚函数？

**原题**：成员函数模板的定义方式？为什么成员函数模板不能是虚函数？

**答案**：

**成员函数模板**：类内的成员函数本身是模板（可为类模板的成员，也可为普通类的成员）：
```cpp
class Printer {
public:
    template <typename T>             // 成员函数模板
    void print(const T& v) const { std::cout << v << '\n'; }
};
Printer p; p.print(42); p.print(std::string("hi"));

template <typename T>
class Wrapper {
public:
    template <typename U>             // 类模板的成员函数模板
    void convert(const U& u) { /* ... */ }
};
```
- 定义方式：类内可直接写模板头+定义；类外定义要写**两套模板参数**：
```cpp
template <typename T>
class Box {
    template <typename U>
    void set(const U& u);
};
template <typename T>
template <typename U>
void Box<T>::set(const U& u) { /* ... */ }
```

**为什么成员函数模板不能是虚函数**：
- 虚函数是**按类**建立一张虚表、在**运行期**按对象类型分派；虚表里要**固定列出**每个虚函数的地址；
- 而成员函数模板只有在**被实例化后**（才知道 U 是啥）才产生一个具体的"函数实例"——**实例化发生在编译期、且数量取决于用法（无限多）**；
- 编译器无法在定义类时**预先知道要生成多少个虚表槽位**（"U 的所有可能类型"无法枚举），虚表的大小/布局就固定不了；
- 因此标准规定：**成员函数模板不能声明为 virtual**（编译报错）。
- 相反，虚函数可以是非模板的普通成员函数，两者不能同时是"虚的模板成员函数"。

**通俗解释**：虚表像"公司固定工位表，岗位（虚函数）数量是盖楼时定死的"；成员函数模板像"按需上岗的临时工，来多少活（不同 U）才招多少人（实例化）"。固定工位表没法预留"无穷多个临时工位"，所以"临时工"不能占固定工位（不能虚）。

---

### 8. 可变参数模板（variadic templates）的语法（typename... Args），如何递归解包参数包？递归出口如何设计？

**原题**：可变参数模板（variadic templates）的语法（typename... Args），如何递归解包参数包？递归出口如何设计？

**答案**：

**语法**：
```cpp
template <typename... Args>   // Args 是"参数包"（pack）
void func(Args... args);      // args 是参数包的实参列表
```
- `typename... Args`：类型参数包（可含零或多个类型）；
- `Args... args`：函数参数包；
- `sizeof...(Args)`：编译期获取参数个数。

**递归解包**：每次取**第一个参数**处理，剩余参数包继续递归调用：
```cpp
// 出口（base case）：无参版本
void printAll() { std::cout << '\n'; }

// 递归：处理一个参数 + 递归处理剩余
template <typename T, typename... Args>
void printAll(const T& first, const Args&... rest) {
    std::cout << first << ' ';
    printAll(rest...);          // 解包：把 rest 传给下一层
}
```
调用 `printAll(1, 2.5, "hi")`：
1. `T=int`、rest=(double, const char*) → 打印 1，递归 `printAll(2.5,"hi")`
2. `T=double`、rest=(const char*) → 打印 2.5，递归 `printAll("hi")`
3. `T=const char*`、rest=() → 打印 hi，递归 `printAll()` → 出口，结束。

**递归出口设计**：
- **空参版本重载**：`void printAll() {}`（上面的例子）——当参数包为空时调用它，停止递归；
- 或**单参数出口**（在需要确保至少一个参数时）；
- 出口必须能被重载决议选中（空包时无参版本是唯一匹配）。

**补充（现代）**：
- **折叠表达式（fold expressions，C++17）**：很多递归可简化为单行：
```cpp
template <typename... Args>
void printAll(const Args&... args) {
    ((std::cout << args << ' '), ...);   // C++17 一元右折叠
}
```
无需递归、无需出口，更简洁高效；
- 也可用 `std::tuple` + `std::apply`，或 `if constexpr`（C++17）在函数体内做出口判断。

**通俗解释**：参数包像"一盒数量不定的积木"。递归解包 = "每次从盒里拿最上面一块玩，把剩下的盒子再递给自己（递归）"，直到盒子空了（无参出口）就停。C++17 的折叠表达式更像"直接拿一根绳子把所有积木串起来一口气处理"，不用一块块递来递去。

---

## 二、编程题

### 1. 编写函数模板 max 返回两个值中的较大者，测试 int、double、string。

**原题**：编写函数模板 max 返回两个值中的较大者，测试 int、double、string。

```cpp
#include <iostream>
#include <string>

template <typename T>
T myMax(const T& a, const T& b) {
    return a > b ? a : b;
}

int main() {
    std::cout << myMax(3, 5) << '\n';                    // int: 5
    std::cout << myMax(2.5, 1.5) << '\n';                // double: 2.5
    std::cout << myMax(std::string("abc"),
                       std::string("abd")) << '\n';      // string: abd（字典序）
    // 显式指定
    std::cout << myMax<double>(3, 2.5) << '\n';          // 3
    return 0;
}
```

要点：`const T&` 传参避免拷贝；`std::string` 重载了 `>`（字典序）所以直接可用。标准库已有 `std::max`，此处仅为演示模板。

---

### 2. 编写函数模板 add，处理不同类型时使用显式实例化 add<int>(a,b)，并演示模板与普通函数重载的优先级。

**原题**：编写函数模板 add，处理不同类型时使用显式实例化 add<int>(a,b)，并演示模板与普通函数重载的优先级。

```cpp
#include <iostream>
#include <string>

template <typename T>
T add(const T& a, const T& b) { return a + b; }

int add(int a, int b) { return a + b; }   // 普通函数（与模板重载）

int main() {
    int a = 1, b = 2;
    std::cout << add(a, b) << '\n';        // 普通函数优先（同样完美匹配）→ 3
    std::cout << add<int>(a, b) << '\n';   // 显式指定 → 强制走模板实例 → 3

    double x = 1.5, y = 2.5;
    std::cout << add(x, y) << '\n';        // 模板（普通函数需转换，模板精确匹配）→ 4

    std::cout << add(std::string("a"), std::string("b")) << '\n';  // 模板 → "ab"
    return 0;
}
```

要点：`add(a,b)`（两 int）时普通函数与模板都能完美匹配，**非模板优先**；`add<int>(a,b)` 显式指定模板；`add(x,y)`（double）普通函数要 double→int 转换，模板精确匹配 → 模板胜出。

---

### 3. 实现类模板 Stack<T, int Capacity>，包含 push、pop、top、empty、full，并测试存放 int 和 string。

**原题**：实现类模板 Stack<T, int Capacity>，包含 push、pop、top、empty、full，并测试存放 int 和 string。

```cpp
#include <iostream>
#include <string>

template <typename T, int Capacity>
class Stack {
    T _data[Capacity];      // 编译期定长（非类型参数）
    int _top = -1;
public:
    bool empty() const { return _top == -1; }
    bool full()  const { return _top == Capacity - 1; }
    void push(const T& v) {
        if (full()) throw std::overflow_error("stack full");
        _data[++_top] = v;
    }
    void pop() {
        if (empty()) throw std::underflow_error("stack empty");
        --_top;
    }
    T& top() {
        if (empty()) throw std::underflow_error("stack empty");
        return _data[_top];
    }
    std::size_t size() const { return _top + 1; }
};

int main() {
    Stack<int, 5> si;
    si.push(1); si.push(2); si.push(3);
    std::cout << "top=" << si.top() << " size=" << si.size() << '\n';
    si.pop();
    std::cout << "after pop top=" << si.top() << '\n';

    Stack<std::string, 3> ss;
    ss.push("hello");
    ss.push("world");
    std::cout << "top=" << ss.top() << " full? " << ss.full() << '\n';
    return 0;
}
```

要点：类模板 + 非类型模板参数 `int Capacity`；同一份模板实例化出 `Stack<int,5>` 和 `Stack<string,3>` 两种独立类型。

---

### 4. 对 Stack 进行全特化（例如 Stack<bool>）实现不同存储方式。

**原题**：对 Stack 进行全特化（例如 Stack<bool>）实现不同存储方式。

（完整代码，接上文）

```cpp
#include <iostream>

// 通用模板（简化：固定容量 64 演示）
template <typename T, int Capacity = 64>
class Stack {
    T _data[Capacity];
    int _top = -1;
public:
    void push(const T& v) { _data[++_top] = v; }
    void pop() { --_top; }
    T top() const { return _data[_top]; }
    bool empty() const { return _top == -1; }
};

// 全特化：Stack<bool> 用按位压缩存储（8 个 bool 装 1 字节）
template <int Capacity>
class Stack<bool, Capacity> {
    unsigned char _bits[(Capacity + 7) / 8];
    int _top = -1;
public:
    void push(bool v) {
        ++_top;
        int byte = _top / 8, bit = _top % 8;
        if (v) _bits[byte] |= (1u << bit);
        else   _bits[byte] &= ~(1u << bit);
    }
    bool top() const {
        int byte = _top / 8, bit = _top % 8;
        return (_bits[byte] >> bit) & 1u;
    }
    void pop() { --_top; }
    bool empty() const { return _top == -1; }
};

int main() {
    Stack<int, 8> si;          // 通用模板：普通 int 数组
    Stack<bool, 20> sb;        // 全特化：按位压缩，20 个 bool 只占 3 字节

    sb.push(true); sb.push(false); sb.push(true);
    std::cout << "top=" << sb.top() << '\n';        // 1
    sb.pop();
    std::cout << "after pop top=" << sb.top() << '\n';  // 0
    std::cout << "sizeof(Stack<bool,20>) = " << sizeof(sb)
              << " 字节（20 bit 压缩存储）\n";        // 3
    return 0;
}
```

要点：**全特化写法是 `template <int Capacity> class Stack<bool, Capacity>`**（把被特化的 `bool` 固定、剩余参数保留）；特化版本可以有与通用版本完全不同的内部实现（这里是按位压缩，省内存）。这也正是标准库 `std::vector<bool>` 的思路。

---

### 5. 使用可变参数模板实现 print 函数，可以打印任意数量、任意类型的参数，并自动换行。

**原题**：使用可变参数模板实现 print 函数，可以打印任意数量、任意类型的参数，并自动换行。

```cpp
#include <iostream>
#include <string>

// 递归出口（base case）：无参版本
void print() { std::cout << '\n'; }

// 递归解包：打印第一个参数，剩下的继续递归
template <typename T, typename... Args>
void print(const T& first, const Args&... rest) {
    std::cout << first << ' ';
    print(rest...);          // 递归：把剩余参数包传给下一层
}

int main() {
    print();                                    // 空参数 → 出口
    print(42);                                  // 42
    print(1, 2.5, "hello", std::string("world"), '!');
    // 输出: 1 2.5 hello world !
    return 0;
}
```

**C++17 折叠表达式版本（更简洁，推荐）**：
```cpp
template <typename... Args>
void print(const Args&... args) {
    ((std::cout << args << ' '), ...);   // C++17 一元右折叠
    std::cout << '\n';
}
```

两种都实现"任意数量、任意类型、自动换行"：递归版本需要**无参出口**终止；折叠表达式一行搞定、无需出口。调用链分析（递归版 `print(1, 2.5, "hi")`）：`T=int, rest=(double, const char*)` → 打印 1 → 递归 `print(2.5, "hi")` → `T=double, rest=(const char*)` → 打印 2.5 → 递归 `print("hi")` → 打印 hi → 递归 `print()` → 出口换行。

---

# 第九部分：移动语义与智能指针（现代 C++ 核心）

## 一、问答题

### 1. 左值与右值的概念？如何区分（能否取地址）？右值引用的语法（T&&）与左值引用的区别？

**原题**：左值与右值的概念？如何区分（能否取地址）？右值引用的语法（T&&）与左值引用的区别？

**答案**：

**左值（lvalue）**：**有名字、有持久存储、可以取地址**的表达式。如变量名、数组元素、解引用结果 `*p`、`obj.member`、函数返回左值引用等。可以出现在赋值号左边。
**右值（rvalue）**：**临时对象、字面量等没有持久存储、通常不可取地址**的表达式。如 `3`、`'a'`、`"hi"`、`a+b` 的结果、`std::string("tmp")` 这种纯右值（prvalue）。C++11 还细分出 **xvalue（将亡值）**，如 `std::move(x)`、`static_cast<T&&>(x)` 的结果——它"即将销毁但还有值可偷"。

**如何区分（核心判据）**：**能否取地址**。`&expr` 合法 → 左值；`&expr` 非法（不能对右值取地址）→ 右值。（引用绑定规则、`decltype` 规则等只是辅助。）

**右值引用 `T&&` 与左值引用 `T&` 的区别**：

| 维度       | 左值引用 `T&`                         | 右值引用 `T&&`（C++11）                                   |
| ---------- | ------------------------------------- | --------------------------------------------------------- |
| 绑定对象   | 只能绑定**左值**                      | 只能绑定**右值**（临时对象/将亡值）                       |
| 目的       | 传参免拷贝、修改实参                  | **延长临时对象生命周期**、实现**移动语义**                |
| 典型场景   | `void f(const T&)` 参数               | 移动构造/移动赋值参数、完美转发                           |
| const 变体 | `const T&` 可绑定右值（绑定到临时量） | `const T&&` 基本无意义（绑定后仍按 const 处理，不能移动） |

**三个易错点**：
1. **具名的右值引用变量本身是左值**：`T&& r = std::move(x);` 之后 `r` 是左值（有名字、可取地址）——这就是为什么"移动"要再套 `std::move(r)` 才能继续当右值传；
2. **`const T&` 能绑定右值**，但不能修改临时量；
3. 右值引用**不是**万能引用；只有出现在模板里的 `T&&`（推导上下文）才是**转发引用（forwarding reference）**，能绑左值也能绑右值，配合 `std::forward` 做**完美转发**。

**现代规范**：传参"只读"用 `const T&`，"要移动/转移所有权"用 `T&&` 配合 `std::move`，模板完美转发用 `T&&` + `std::forward`。

**通俗解释**：左值 = "有门牌号、能长期住的房子"（可取地址）；右值 = "路边的临时摊位/一次性纸杯"（用完即弃）。左值引用 = "挂在永久住址上的门牌（只能挂固定房子）"；右值引用 = "专门用来'顺走'临时摊位东西的手（把临时摊的东西搬到你这，搬完摊就撤）"。注意：你手里攥着的那张"搬运工工牌"（具名 T&&）本身也是个有名有姓的东西（左值）。

---

### 2. 移动构造函数和移动赋值运算符的特征（参数为右值引用，进行浅拷贝并置空源对象）。它们与拷贝构造/赋值运算符的优先级（移动优先于拷贝）？

**原题**：移动构造函数和移动赋值运算符的特征（参数为右值引用，进行浅拷贝并置空源对象）。它们与拷贝构造/赋值运算符的优先级（移动优先于拷贝）？

**答案**：

**标准形式**：
```cpp
String(String&& o) noexcept                // 移动构造
    : _str(o._str) {                       // 偷走指针（"资源转移"）
    o._str = nullptr;                      // 源对象置空
}
String& operator=(String&& o) noexcept {   // 移动赋值
    if (this != &o) {
        delete[] _str;                     // 释放自己的旧资源
        _str = o._str;                     // 转移资源
        o._str = nullptr;                  // 源置空
    }
    return *this;
}
```

**特征（要纠正题面的"浅拷贝"说法）**：
- 参数是**右值引用 `T&&`**（只对右值/将亡值调用）；
- 做的事情是**资源所有权转移（transfer）**：把源对象的**指针/句柄**"偷"过来（浅拷贝**指针本身**），同时**必须把源对象置为空/有效默认状态**（如 `nullptr`）；
- 之所以要**置空源对象**：源是临时对象，很快会被析构——若不置空，析构时源会**释放这块已被"偷走"的内存**，导致目标悬空、双重释放；
- 严格说这不叫"浅拷贝"——**浅拷贝会导致两个对象指向同一块内存、双重释放**；移动是"**所有权移交**"，移完后源对象不拥有该资源，是安全的；
- 通常标 **`noexcept`**：让 `std::vector` 扩容等场景能放心使用移动（若移动可能抛异常，标准库回退用拷贝）；
- 移动后源对象处于**有效但未指定**状态（通常为空），一般只能被重新赋值/析构，不应再读取其内容。

**移动优先于拷贝（优先级）**：
- 当实参是**右值**（临时对象、`std::move(x)` 结果）时，重载决议**优先选择移动版本**（移动比拷贝"更匹配"右值）；
- 当实参是**左值**时，只能选拷贝版本；
- 因此 `String s = std::move(t);` 走移动构造；`String s = t;` 走拷贝构造。

**隐式生成规则（易考点）**：若类**没有声明任何**析构/拷贝构造/拷贝赋值/移动构造/移动赋值，编译器会**隐式生成移动构造和移动赋值**；但**一旦自定义了析构、拷贝构造或拷贝赋值中的任一个**，编译器就**不再隐式生成**移动操作（Rule of Five）——这常是"定义了析构却忘了移动操作，导致意外走拷贝"的坑。

**通俗解释**：拷贝像"复印一份文件，原件还在"（深拷贝、开销大）；移动像"把这份文件连原件一起给你，原件从我这拿走（源置空）"——省了复印钱，但**不是**"复印两份"（浅拷贝才是复印两份却共用一页纸，会出事）。规则是"右手边的箱子是临时的（右值）"就优先"整个搬走"（移动）而不是"复印"（拷贝）。

---

### 3. 移动语义带来的性能优势（避免深拷贝）。std::move 的作用（将左值强制转为右值）？使用 std::move 后原对象的状态（通常为空或有效但未指定）？

**原题**：移动语义带来的性能优势（避免深拷贝）。std::move 的作用（将左值强制转为右值）？使用 std::move 后原对象的状态（通常为空或有效但未指定）？

**答案**：

**移动语义的性能优势**：对**持有堆内存/大资源**的类型（string、vector、自定义资源类），拷贝需要**深拷贝**（重新分配内存 + 逐字节复制，O(n) 且有分配开销）；移动只需**转移指针/句柄**（几个指针赋值，O(1)）+ 置空源，**完全避免深拷贝**。典型受益场景：
- 按值返回局部大对象（`return local;`，可被省略或移动）；
- `vector` 扩容搬移元素（C++11 起用移动替代拷贝）；
- 容器操作（`push_back` 临时对象、`insert`、`swap`）；
- 将大对象塞入容器而不复制底层数据。

**std::move 的作用**：
- `std::move(x)` **只是 `static_cast<T&&>(x)`**——把左值**"标记"成右值**（将亡值），告诉编译器"这个对象我允许你偷走它的资源"；
- **它本身不执行任何移动**（不拷贝、不搬数据），真正的搬移发生在移动构造函数/移动赋值里；
- 常用于：调用移动构造/移动赋值、往容器里转移资源、`std::unique_ptr` 转移所有权、完美转发场景。

**使用 std::move 后原对象的状态**：
- 进入"**有效但未指定（valid but unspecified）**"状态；
- 实际表现：通常**被掏空**（如 string 变空、指针置 `nullptr`、size 归 0），但**标准不保证**具体内容；
- 因此**不要假设源对象的具体值**，安全做法是：move 之后**不再读取源对象内容**，只允许重新赋值、销毁，或调用不依赖内容的操作（如 `clear()`）；
- 对 **const 对象 move 是无效的**：`const T` 的 `std::move` 结果是 `const T&&`，仍会调用拷贝构造（因为 const 不能搬），这是常见误用。

**现代规范**：不要**轻易**对"之后还要用"的左值 move；move 通常配合"用完即弃"语义；返回值/局部对象交给编译器 RVO 处理即可，**不必手动 `std::move(return 值)`**（C++17 强制省略拷贝，显式 move 反而可能抑制 RVO）。

**通俗解释**：移动 = "搬家时把整箱家具连箱搬走"（快，O(1)）；拷贝 = "把家具每件都复印一份"（慢）。`std::move` = "在箱子上贴张'此箱可搬走'的条子"——**贴条子本身不搬**，搬箱工人（移动构造）看到条子才来搬。搬走后原处就空了（源对象被掏空），所以别指望还能从原处拿东西。

---

### 4. 什么是 RAII（资源获取即初始化）？其核心思想是什么？RAII 类通常具备哪些特征（构造获取资源、析构释放、禁止复制、提供访问接口）？

**原题**：什么是 RAII（资源获取即初始化）？其核心思想是什么？RAII 类通常具备哪些特征（构造获取资源、析构释放、禁止复制、提供访问接口）？

**答案**：

**RAII（Resource Acquisition Is Initialization，资源获取即初始化）**：C++ 特有的资源管理范式——**把资源的生命周期与对象的生命周期绑定**：在**构造函数中获取资源**，在**析构函数中释放资源**。这样资源由"栈对象的自动析构"来保证释放，无论正常返回还是异常抛出，析构都会被调用，资源**必然**得到释放。

**核心思想**：让"资源管理"自动化、异常安全。由于**栈对象离开作用域必然析构**（包括异常栈展开时），只要资源绑定在栈对象上，就"想忘都忘不掉释放"。这是 C++ 相比"手写 open/close、malloc/free"的核心优势。

**RAII 类通常具备的特征**：
1. **构造函数获取资源**（打开文件、分配内存、加锁、连接数据库）；
2. **析构函数释放资源**（close、free、unlock、断开连接）；
3. **禁止/管理复制**：资源通常不能共享，拷贝会被**禁用**（`= delete`）或做**引用计数/深拷贝**（如 shared_ptr 计引用、自定义深拷贝类）；
4. **提供访问接口**：通过成员函数（`get()`、`operator*`、`lock()` 等）向外部暴露资源访问，但"释放权"由对象自己控制；
5. （C++11 起）常配合**移动语义**转移资源所有权（如 unique_ptr）。

**标准库 RAII 例子**：`std::unique_ptr`/`shared_ptr`/`weak_ptr`（内存）、`std::lock_guard`/`unique_lock`（锁）、`std::fstream`/`ifstream`（文件）、`std::string`/`vector`（内存缓冲）、`std::thread`（线程）。

**现代规范**：CppCoreGuidelines 明确"**用 RAII 管理所有资源**"（内存、文件、锁、socket、GPU 上下文等），是写出异常安全代码的基石。音视频领域尤其典型：解码器上下文、帧缓冲、GPU 纹理都应 RAII 化。

**通俗解释**：RAII = "进店就发手牌，出店凭手牌取物"——**资源的"领取"和"归还"被绑定在"进店/出店"（对象构造/析构）这两个必然发生的事件上**。就算店里着火（异常），人也一定会从门口跑出去（栈展开），手牌也一定会交回（析构释放资源），东西不会丢。不 RAII = "自己记着出门前还手牌"，一慌张（异常）就忘了。

---

### 5. unique_ptr 的特点（独享所有权、禁止拷贝、支持移动）？shared_ptr 的特点（共享所有权、引用计数）？weak_ptr 的作用（解决循环引用、不增加引用计数）？

**原题**：unique_ptr 的特点（独享所有权、禁止拷贝、支持移动）？shared_ptr 的特点（共享所有权、引用计数）？weak_ptr 的作用（解决循环引用、不增加引用计数）？

**答案**：

**unique_ptr（独占所有权）**：
- **同一时刻只有一个 unique_ptr 拥有该资源**；**禁止拷贝**（拷贝构造/赋值被 `= delete`）、**支持移动**（`std::move` 转移所有权，源指针置空）；
- 作用域结束自动 delete 资源；`release()` 交出裸指针、`reset()` 释放、`get()` 拿裸指针（**不要长期持有**）；
- 支持自定义删除器（删除器是**模板类型参数**）；
- `unique_ptr<T[]>` 支持动态数组（重载 `operator[]`、自动 `delete[]`）；
- **零额外开销**（与裸指针大小相同，无引用计数），是默认首选。

**shared_ptr（共享所有权）**：
- 多个 shared_ptr **共享同一资源**，内部有**引用计数（control block）**；
- 拷贝 → 计数 +1；析构/重置 → 计数 -1；**计数归 0 时释放资源**；
- 引用计数操作是**原子的**（线程安全保证"计数"本身安全），但**指向的对象不是自动线程安全**；
- `make_shared` 一次分配"对象 + 控制块"（更高效）；
- 有额外开销（一个控制块 + 原子计数），比 unique_ptr 重。

**weak_ptr（弱引用）**：
- **不增加引用计数**，只是"观察者"；
- 通过 `lock()` 获取一个 `shared_ptr`（若资源仍存活则成功，否则返回空）；`expired()` 判断资源是否已释放；
- 作用：**打破循环引用**、缓存/观察场景（不延长生命周期）；
- 不能直接解引用，必须 `lock()`。

**三者协作的典型**：缓存系统用 weak_ptr 观察（不延长生命周期），用 shared_ptr 持有。

**现代规范（重要纠偏）**：题面及旧教材常提的 `auto_ptr`（C++98）**已废弃**（C++17 移除），一律用 `unique_ptr` 替代——`unique_ptr` 语义更安全（禁拷贝、可移动）。CppCoreGuidelines：**默认用 unique_ptr 管理独占资源；需要共享才用 shared_ptr**。

**通俗解释**：unique_ptr = "独生子女证，孩子只有一个监护人，监护权可以过户（移动）但不能多办一张证（禁拷贝）"；shared_ptr = "多个股东按持股（引用计数）共同持有公司，最后一个退股（计数归零）才注销公司"；weak_ptr = "访客/观察员，可以看公司还开着没（lock/expired），但不持股，不阻止公司注销"。

---

### 6. 智能指针的循环引用问题如何产生（两个 shared_ptr 互相引用），如何用 weak_ptr 破解？

**原题**：智能指针的循环引用问题如何产生（两个 shared_ptr 互相引用），如何用 weak_ptr 破解？

**答案**：

**循环引用如何产生**：
- 两个对象各自持有指向对方的 `shared_ptr`，构成**环**：
```cpp
struct Node { shared_ptr<Node> next; };   // 前后指针形成环
auto a = make_shared<Node>();
auto b = make_shared<Node>();
a->next = b;
b->next = a;        // a↔b 互相持有 shared_ptr
```
- 此时 `a` 的计数 = 2（外部 a + b->next），`b` 的计数 = 2（外部 b + a->next）；
- 当外部变量 `a`、`b` 离开作用域被销毁后：`a` 的计数 2→1，`b` 的计数 2→1，**永远不会降到 0**——因为 `a->next` 还持有 b、`b->next` 还持有 a，**谁都无法触发释放**，两个对象连同内存**永远泄漏**；
- 即"**你指着我、我指着你，谁都不松手**"。

**如何用 weak_ptr 破解**：
- 把环中的**一个方向**（或"逻辑上弱的一方"）改为 `weak_ptr`：
```cpp
struct Node {
    shared_ptr<Node> next;      // 强引用方向
    weak_ptr<Node> prev;        // 弱引用方向：不增加计数，打破环
};
```
- `weak_ptr` **不增加引用计数**，因此环中只要有**一处弱引用**，就能打断"谁也到不了 0"的死锁；
- 需要访问 `prev` 时用 `lock()` 临时提升为 `shared_ptr`（安全：若对象已释放则返回空，不会悬空）。

**典型场景**：双向链表、树/图的父指针、观察者模式中被观察者持观察者。原则：**"上游拥有下游、下游引用上游"**——被拥有的方向用 `shared_ptr`（所有权），反向引用用 `weak_ptr`（非所有权）。

**通俗解释**：循环引用 = "A 和 B 各欠对方一笔账，互相担保，谁都不还（谁也不释放）"——两个都认为"对方还欠着我呢，我不能消失"，结果一起烂账（泄漏）。weak_ptr = "把其中一个人的账改成'口头承诺（不记账）'"——不记账就不构成"还欠着"的死锁，想查账（lock）时确认一下对方还在不在就行。

---

### 7. 自定义删除器（Deleter）的必要性（如管理 FILE*），unique_ptr 和 shared_ptr 设置删除器的方式有何不同（unique_ptr 是模板参数，shared_ptr 是构造函数参数）？

**原题**：自定义删除器（Deleter）的必要性（如管理 FILE*），unique_ptr 和 shared_ptr 设置删除器的方式有何不同（unique_ptr 是模板参数，shared_ptr 是构造函数参数）？

**答案**：

**自定义删除器的必要性**：
- 智能指针默认用 `delete`（或 `delete[]`）释放——这只适用于"new 出来的对象"；
- 很多资源不是 new 出来的：`FILE*`（要 `fclose`）、`socket`（要 `close`）、`sqlite3*`（要 `sqlite3_close`）、`mmap`（要 `munmap`）、malloc 的内存（要 `free`）等，**默认 delete 是错的**，必须提供**自定义删除器**指定正确的释放方式；
- 也常用于：带日志的释放、资源池归还（不是真释放而是"还回池子"）、对齐内存释放等。

**unique_ptr 与 shared_ptr 设置删除器的不同**：

1. **unique_ptr：删除器是"模板类型参数"**（编译期确定，是 unique_ptr 类型的一部分）：
```cpp
// 删除器类型写在模板第二个参数里
std::unique_ptr<FILE, decltype(&std::fclose)> fp(std::fopen("a.txt","r"), &std::fclose);
// 或自定义函数对象类型
struct FileCloser { void operator()(FILE* f) const { if (f) std::fclose(f); } };
std::unique_ptr<FILE, FileCloser> fp(std::fopen("a.txt","r"), FileCloser{});
```
- 因为删除器是类型的一部分，**两个删除器类型不同则 unique_ptr 类型不同**（不能互转）；`std::unique_ptr<FILE, D>` 比 `std::unique_ptr<FILE>` 更大/占更多空间（无状态删除器可用 EBO 优化为 0 开销）；
- `make_unique` 不支持自定义删除器。

2. **shared_ptr：删除器是"构造函数参数"**（运行时传入、**类型擦除**，不是类型的一部分）：
```cpp
std::shared_ptr<FILE> fp(std::fopen("a.txt","r"), &std::fclose);
// 删除器类型不影响 shared_ptr<FILE> 的类型；任何删除器的 shared_ptr<FILE> 可互相赋值
```
- 删除器被**存在控制块里**，运行时调用；类型被擦除（存为 `std::function<void(T*)>` 之类）；
- `make_shared` 不支持自定义删除器（它只做默认 delete），有自定义删除器时必须直接构造。

**总结**：unique_ptr 的删除器**编译期定死**（零开销、类型相关）；shared_ptr 的删除器**运行时传入、类型擦除**（灵活、类型无关、有额外存储）。需要 `unique_ptr` + 自定义删除器时，删除器类型要写对（`decltype`/函数对象）。

**通俗解释**：默认删除器 = "统一按'退房'流程处理（delete）"；自定义删除器 = "有些房是'还钥匙'（fclose）、有些是'还车'（close socket）"，得按各自的规矩办。unique_ptr 把规矩**写进'房型编号'里**（模板参数，不同编号不能混住）；shared_ptr 把规矩**写进物业合同里**（构造参数，任何合同的住户类型都一样）。

---

### 8. 智能指针的常见误用（用同一个裸指针初始化多个智能指针），如何安全地从 this 获取 shared_ptr（使用 enable_shared_from_this）？

**原题**：智能指针的常见误用（用同一个裸指针初始化多个智能指针），如何安全地从 this 获取 shared_ptr（使用 enable_shared_from_this）？

**答案**：

**常见误用：用同一个裸指针初始化多个智能指针**：
```cpp
int* p = new int(42);
std::unique_ptr<int> u1(p);
std::unique_ptr<int> u2(p);   // 危险！两个 unique_ptr 都要 delete p → 双重释放
// 或
std::shared_ptr<int> s1(p);
std::shared_ptr<int> s2(p);   // 危险！两个独立控制块，各自计数，各自释放 → 双重释放
```
- **unique_ptr**：两个同时指向同一裸指针，作用域结束时**都 delete** → **双重释放**（未定义行为、崩溃）；
- **shared_ptr**：用裸指针分别构造会创建**两个独立的控制块**（各自计数 1），都以为"自己是唯一所有者"，释放时**双重释放**；
- 正确做法：
  - unique_ptr：**只从一个裸指针创建一个**，其余用 `std::move` 转移；
  - shared_ptr：**从同一个已有的 shared_ptr 拷贝**（计数共享同一控制块），或用 `make_shared` 统一创建；
  - 原则：**裸指针只"过手"一次，所有权立即交给一个智能指针**。

**如何安全地从 this 获取 shared_ptr（enable_shared_from_this）**：
- 场景：类的成员函数里需要返回"指向自己的 shared_ptr"（如把 this 塞进回调、注册表）。
- **错误做法**：`return std::shared_ptr<T>(this);` —— 这会为 `this` **再创建一个独立控制块**，与外部已有的 shared_ptr 各管各的 → **双重释放**。
- **正确做法**：继承 `std::enable_shared_from_this<T>`，用 **`shared_from_this()`**：
```cpp
class Widget : public std::enable_shared_from_this<Widget> {
public:
    std::shared_ptr<Widget> getShared() {
        return shared_from_this();   // 返回与现有控制块共享的 shared_ptr
    }
};
auto w = std::make_shared<Widget>();   // 必须先由 shared_ptr 管理
auto s = w->getShared();               // 与 w 共享同一控制块（计数+1），安全
```
- 原理：`enable_shared_from_this` 内部存了一个 **weak_ptr（指向本对象）**，`shared_from_this()` 就是 `weak_ptr::lock()`——拿到与现有控制块共享的 shared_ptr；
- **前提**：该对象**必须已经由某个 shared_ptr 管理**（控制块已存在）时才可调用；否则 `shared_from_this()` 抛 `std::bad_weak_ptr`（或未定义行为）。**不要在构造函数里调用**（此时控制块还没绑定）。
- 现代规范：**凡是需要在内部返回自身 shared_ptr 的类，就继承 enable_shared_from_this**，绝不用裸 this 构造 shared_ptr。

**通俗解释**：多个智能指针拿同一个裸指针 = "一张房契，两个人各自复印了一份还各拿去办了一次过户，最后两拨人都来拆房"（双重释放）。`enable_shared_from_this` = "公司里想拿'自己的名片'，正确做法是找前台（shared_from_this）领一张与公司档案同源的（共享控制块），而不是自己在家印一张"（各自为政会出乱子）。

---

## 二、编程题

### 1. 为 Day3 的 String 类添加移动构造函数和移动赋值运算符，并测试 String s = "hello";（临时对象）调用的构造函数，对比拷贝构造的开销。

**原题**：为 Day3 的 String 类添加移动构造函数和移动赋值运算符，并测试 String s = "hello";（临时对象）调用的构造函数，对比拷贝构造的开销。

```cpp
#include <iostream>
#include <cstring>

class String {
    char* _str;
public:
    // 构造
    String(const char* s = "") : _str(new char[std::strlen(s) + 1]) {
        std::strcpy(_str, s);
        std::cout << "构造\n";
    }
    // 拷贝构造（深拷贝，O(n)）
    String(const String& o) : _str(new char[std::strlen(o._str) + 1]) {
        std::strcpy(_str, o._str);
        std::cout << "拷贝构造(深拷贝)\n";
    }
    // 移动构造（偷指针，O(1)）
    String(String&& o) noexcept : _str(o._str) {
        o._str = nullptr;                  // 源置空
        std::cout << "移动构造(偷指针)\n";
    }
    // 拷贝赋值
    String& operator=(const String& o) {
        if (this != &o) {
            delete[] _str;
            _str = new char[std::strlen(o._str) + 1];
            std::strcpy(_str, o._str);
            std::cout << "拷贝赋值\n";
        }
        return *this;
    }
    // 移动赋值
    String& operator=(String&& o) noexcept {
        if (this != &o) {
            delete[] _str;
            _str = o._str;
            o._str = nullptr;
            std::cout << "移动赋值\n";
        }
        return *this;
    }
    ~String() { delete[] _str; std::cout << "析构\n"; }
};

int main() {
    // 临时对象右值 → 走移动构造（C++17 也可能直接省略，见下）
    String s1 = String("hello");
    std::cout << "---\n";

    String s2("world");
    String s3 = std::move(s2);     // 显式 move → 移动构造
    std::cout << "s2 被掏空: " << (s2.c_str() == nullptr ? "(空)" : s2.c_str()) << '\n';

    String s4;
    s4 = std::move(s3);            // 移动赋值
    return 0;
}
```

要点：
- `String s1 = String("hello");` 在 **C++17 下走"强制复制省略（mandatory copy elision）"**——临时对象直接原地构造，既不拷贝也不移动（连移动构造都可能不打印）；C++11/14 下（`-std=c++11`）可能打印"移动构造"或"拷贝构造"（取决于优化）；
- **对比开销**：拷贝构造要 `new + strlen + strcpy`（O(n)）；移动构造只做两次指针赋值（O(1)）+ 置空源，**完全避免深拷贝**——这就是移动语义的收益；
- 移动后 `s2` 处于"有效但未指定"状态（这里置 nullptr），**不要再读取其内容**。

（补充：`c_str()` 方法被简化省略，可自行补上返回 `_str`。）

---

### 2. 使用 std::move 将左值转为右值，演示移动语义，并观察源对象状态。

**原题**：使用 std::move 将左值转为右值，演示移动语义，并观察源对象状态。

```cpp
#include <iostream>
#include <string>
#include <utility>

int main() {
    std::string a = "I am a long string with enough data to defeat SSO...";
    std::string b = a;                // 拷贝：a 不变
    std::cout << "拷贝后 a.size() = " << a.size() << '\n';

    std::string c = std::move(a);     // 移动：把 a 的资源偷给 c
    std::cout << "移动后 a.size() = " << a.size()
              << "（有效但未指定，通常为空）\n";
    std::cout << "c = " << c << '\n';

    // 验证"std::move 本身不搬东西，只是标记"
    std::string d = "hi";
    std::string&& r = std::move(d);   // r 绑定到 d 的右值"化身"
    std::cout << "d 仍是 " << d << "（std::move 只是转换，没搬）\n";

    // 安全做法：move 之后不再读源，只重新赋值或销毁
    d = "new value";                  // 重新赋值 OK
    std::cout << "d = " << d << '\n';
    return 0;
}
```

要点：`std::move(a)` 本身不搬（只是 `static_cast<T&&>`），真正的搬发生在 `std::string` 的**移动构造**里；移动后 `a` 进入"有效但未指定"状态（`size()` 通常变 0，SSO 短串可能保留内容——标准不保证，**不要依赖具体内容**）。

---

### 3. 实现一个 RAII 类 FileRAII 管理 FILE*，自动关闭文件。

**原题**：实现一个 RAII 类 FileRAII 管理 FILE*，自动关闭文件。

```cpp
#include <cstdio>
#include <stdexcept>
#include <iostream>

class FileRAII {
    FILE* _fp;
public:
    // 构造：获取资源（打开文件）
    explicit FileRAII(const char* path, const char* mode)
        : _fp(std::fopen(path, mode)) {
        if (!_fp) throw std::runtime_error("打开文件失败");
    }
    // 析构：释放资源（关闭文件）——必然执行，异常安全
    ~FileRAII() {
        if (_fp) std::fclose(_fp);
    }
    // 禁止复制（资源独占）
    FileRAII(const FileRAII&) = delete;
    FileRAII& operator=(const FileRAII&) = delete;
    // 移动转移所有权（可选增强）
    FileRAII(FileRAII&& o) noexcept : _fp(o._fp) { o._fp = nullptr; }
    // 访问接口
    FILE* get() const { return _fp; }
    void write(const char* s) const {
        if (std::fputs(s, _fp) == EOF) throw std::runtime_error("写入失败");
    }
};

int main() {
    try {
        FileRAII f("out.txt", "w");
        f.write("hello RAII\n");
        // 无论这里抛不抛异常，离开作用域时 fclose 必然被调用
    } catch (const std::exception& e) {
        std::cerr << e.what() << '\n';
    }
    std::cout << "文件已由 RAII 自动关闭\n";
    return 0;
}
```

要点：构造打开（获取资源）、析构关闭（释放资源）、禁拷贝（`= delete`）、提供 `write/get` 接口——完全符合 RAII 五特征；即使 main 里中途抛异常，栈展开也会析构 `f`、关闭文件。这也是"用智能指针/RAII 代替裸 fopen/fclose"的现代实践（或用 `std::unique_ptr<FILE, FileCloser>`，见题 6）。

---

### 4. 使用 unique_ptr 管理动态数组 int[]，并正确释放（可用 unique_ptr<int[]>）。

**原题**：使用 unique_ptr 管理动态数组 int[]，并正确释放（可用 unique_ptr<int[]>）。

```cpp
#include <iostream>
#include <memory>

int main() {
    // 方式一：unique_ptr<T[]> 专门管理数组（推荐，C++11 起支持）
    std::unique_ptr<int[]> arr(new int[5]);
    for (int i = 0; i < 5; ++i) arr[i] = i * i;   // 重载了 operator[]
    std::cout << "arr[3] = " << arr[3] << '\n';   // 9

    // 方式二：make_unique 数组（C++14 起）
    auto arr2 = std::make_unique<int[]>(5);
    for (int i = 0; i < 5; ++i) arr2[i] = i + 1;
    std::cout << "arr2[4] = " << arr2[4] << '\n'; // 5

    // 释放：作用域结束自动 delete[]，无需手动
    arr.reset();          // 也可显式 reset 提前释放
    std::cout << (arr ? "arr 仍有效" : "arr 已释放") << '\n';
    return 0;
}
```

要点：`unique_ptr<int[]>` 或 `make_unique<int[]>(n)` 管理动态数组，自动用 `delete[]` 释放（**不能用 `unique_ptr<int>` 管数组**——那样会用 `delete` 而不是 `delete[]`，是未定义行为）。移动转移所有权：`auto arr3 = std::move(arr2);`。

---

### 5. 使用 shared_ptr 构建一个双向链表节点（前后指针），演示循环引用问题，并用 weak_ptr 解决。

**原题**：使用 shared_ptr 构建一个双向链表节点（前后指针），演示循环引用问题，并用 weak_ptr 解决。

```cpp
#include <iostream>
#include <memory>

// 有问题的版本：前后都用 shared_ptr → 循环引用泄漏
struct BadNode {
    std::shared_ptr<BadNode> next;
    std::shared_ptr<BadNode> prev;
    ~BadNode() { std::cout << "BadNode 析构\n"; }
};

// 正确版本：一方用 weak_ptr 打破环
struct Node {
    std::shared_ptr<Node> next;
    std::weak_ptr<Node> prev;      // 弱引用：不增加计数
    ~Node() { std::cout << "Node 析构\n"; }
};

int main() {
    {
        auto a = std::make_shared<BadNode>();
        auto b = std::make_shared<BadNode>();
        a->next = b;
        b->prev = a;               // 循环引用形成
        std::cout << "BadNode 版本：离开作用域……\n";
    }   // a、b 计数都为 1（互相持有），永远不析构 → 泄漏（无"Node 析构"输出）
    std::cout << "BadNode 未析构（泄漏）\n\n";

    {
        auto x = std::make_shared<Node>();
        auto y = std::make_shared<Node>();
        x->next = y;
        y->prev = x;               // prev 是 weak_ptr，不构成环
        std::cout << "Node 版本：离开作用域……\n";
    }   // x、y 计数都降为 0，正常析构（打印两次 "Node 析构"）
    return 0;
}
```

要点：**BadNode 版本两个对象互相持有 shared_ptr → 引用计数永远 ≥1 → 永不析构（内存泄漏）**，运行看不到"BadNode 析构"；**Node 版本把 `prev` 设为 weak_ptr → 不增加计数 → 离开作用域正常析构**。需要访问 `prev` 时用 `prev.lock()`（安全判断是否存活）。

---

### 6. 为 FILE* 自定义删除器，分别用 unique_ptr 和 shared_ptr 管理文件，确保 fclose 调用。

**原题**：为 FILE* 自定义删除器，分别用 unique_ptr 和 shared_ptr 管理文件，确保 fclose 调用。

```cpp
#include <cstdio>
#include <memory>
#include <iostream>

int main() {
    // 1) unique_ptr：删除器是模板类型参数（decltype(&fclose)）
    std::unique_ptr<FILE, decltype(&std::fclose)> up(
        std::fopen("a.txt", "w"), &std::fclose);
    if (up) std::fputs("hello from unique_ptr\n", up.get());
    // 作用域结束：自动 fclose（输出见下）

    // 2) shared_ptr：删除器是构造函数参数（类型擦除）
    std::shared_ptr<FILE> sp(std::fopen("b.txt", "w"), &std::fclose);
    std::fputs("hello from shared_ptr\n", sp.get());

    // 3) 函数对象删除器（更灵活，可带状态，适合 unique_ptr）
    struct FileCloser {
        void operator()(FILE* f) const {
            if (f) {
                std::fclose(f);
                std::cout << "  文件已通过自定义删除器关闭\n";
            }
        }
    };
    std::unique_ptr<FILE, FileCloser> up2(std::fopen("c.txt", "w"), FileCloser{});
    std::fputs("hello from custom deleter\n", up2.get());

    std::cout << "main 结束，三个文件均由删除器自动 fclose\n";
    return 0;
}
```

要点：
- **unique_ptr** 的删除器写在**模板参数**里：`std::unique_ptr<FILE, decltype(&std::fclose)>`（或用函数对象类型）；
- **shared_ptr** 的删除器作为**构造参数**传入：`std::shared_ptr<FILE> sp(fp, &std::fclose)`——删除器类型不进入 shared_ptr 的类型；
- 两者作用域结束/引用归零时都自动调用 `fclose`，杜绝"忘关文件"（裸 fopen 常见泄漏点）。

---

### 7. 使用 enable_shared_from_this 实现一个类成员函数返回自身的 shared_ptr，避免误用裸指针。

**原题**：使用 enable_shared_from_this 实现一个类成员函数返回自身的 shared_ptr，避免误用裸指针。

```cpp
#include <iostream>
#include <memory>

class Widget : public std::enable_shared_from_this<Widget> {
    int _id;
public:
    explicit Widget(int id) : _id(id) { std::cout << "Widget 构造\n"; }
    ~Widget() { std::cout << "Widget 析构\n"; }

    std::shared_ptr<Widget> getShared() {
        return shared_from_this();    // 与现有控制块共享，安全
    }
    void show() const { std::cout << "id = " << _id << '\n'; }
};

// 演示"错误做法"（双重释放）：不要这样写
// class Bad {
// public:
//     std::shared_ptr<Bad> getShared() { return std::shared_ptr<Bad>(this); } // 危险！
// };

int main() {
    std::shared_ptr<Widget> w = std::make_shared<Widget>(100);   // 必须先由 shared_ptr 管理

    std::shared_ptr<Widget> w2 = w->getShared();  // 与 w 共享同一控制块（计数 2）
    w2->show();

    std::cout << "use_count = " << w.use_count() << '\n';   // 2（w + w2）

    // 都离开作用域后，计数归 0，只析构一次（无双重释放）
    return 0;
}
```

要点：
- 类继承 `std::enable_shared_from_this<Widget>`，内部自动保存一个 `weak_ptr`；
- `shared_from_this()` 通过内部 weak_ptr `lock()` 返回与**现有控制块共享**的 shared_ptr——**避免 `shared_ptr<Widget>(this)` 造成的独立控制块 + 双重释放**；
- **前提**：对象必须先被某个 shared_ptr 管理，**不能在构造函数里调用** `shared_from_this()`（会抛 `std::bad_weak_ptr`）；
- 正确演示：`w` 与 `w2` 的 `use_count` 合计共享同一控制块，对象只析构一次。

---

# 结语（现代 C++ 要点回顾）

这份题库完整覆盖了 **C++ 从 C 基础到现代 C++17 的面试核心**。最后把我在各题中强调的"时兴技术栈纠偏"汇总一下，方便你复习时对照：

1. **头文件禁 `using namespace`**（CppCoreGuidelines），作用域限定 `::` 最安全。
2. **const 常量优先；编译期常量用 `constexpr`** 替代 `#define`。
3. **Rule of Three 已演进为 Rule of Five**，首选 **Rule of Zero**（用 RAII 成员自动管理）。
4. **裸 new/delete、malloc/free 让位于智能指针**：独占用 `unique_ptr`，共享用 `shared_ptr`，观察用 `weak_ptr`；`auto_ptr` 已废弃。
5. **资源管理一律 RAII**：文件用 RAII 类/带自定义删除器的智能指针，锁用 `lock_guard`。
6. **单例用 Meyer's Singleton**（C++11 magic static，线程安全 + 自动释放），比手写 pthread_once/双检锁简洁安全。
7. **字符串用 `std::string`**（+ `string_view` 做只读参数），容器优先 `vector` + `emplace_back` + `reserve`。
8. **多态设计优先虚函数 + 基类接口**，`override` 关键字必写；析构函数对多态基类必须是虚的。
9. **模板：定义放头文件**；函数模板不能偏特化；C++17 用折叠表达式/`if constexpr`，C++20 引入 concepts 与 modules。
10. **移动语义与 `std::move`**：`std::move` 只是转换、不搬数据；移动后源对象"有效但未指定"；C++17 强制复制省略。
11. **异常用标准异常派生类 + 按 const 引用捕获**；析构函数默认 `noexcept`，不在析构里抛异常。
12. **动态内存检测**：valgrind 之外更推荐 **AddressSanitizer（`-fsanitize=address`）**，CI 友好、更快。

